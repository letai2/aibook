"""Practical post-training bridge and the measured system-level ending."""

from .schema import Lesson


LESSONS = [
    Lesson(
        "65a-sft-lab", 10, "از ادامه‌دهنده به دستیار",
        "همان Mini-GPT را روی پاسخ‌های مشخص تنظیم کنیم",
        "از یک مدل پیش‌آموزش‌دیده کپی می‌گیریم، فقط پاسخ را در Loss حساب می‌کنیم و نتیجه را روی عبارت‌های دیده‌نشده می‌سنجیم.",
        """<p>در درس قبل جای Loss پاسخ را پیدا کردید. حالا سؤال عملی این است: اگر همین Loss را به <code>backward</code> و <code>step</code> وصل کنیم، رفتار مدل چه تغییری می‌کند؟ برای دیدن پاسخ به مدل بزرگ یا فایل آماده نیاز نداریم. یک Mini-GPT کوچک را ابتدا روی متن خام آموزش می‌دهیم، از آن کپی می‌گیریم و نسخهٔ دوم را روی چهار پرسش دربارهٔ همین کتاب تنظیم می‌کنیم. نسخهٔ پایه دست‌نخورده می‌ماند تا مقایسه معنی داشته باشد.</p>
<h2>وظیفه را عمداً کوچک انتخاب کنیم</h2>
<p>قرارداد پاسخ فقط یک کاراکتر است: «ب» برای بله و «ن» برای نه. مثلاً «در آموزش وزن عوض می شود؟» پاسخ «ب» دارد و «در تولید وزن عوض می شود؟» پاسخ «ن». این آزمایشِ محدود، تنظیم رفتار با دادهٔ نظارت‌شده را نشان می‌دهد؛ پاسخ آزاد، خلاصه‌سازی و گفت‌وگوی عمومی را آموزش نمی‌دهد. حتی ممکن است مدل به یک واژهٔ راهنما تکیه کند. همین احتمال را باید با آزمون تازه بررسی کنیم، نه اینکه چهار پاسخ درست را توان عمومی بنامیم.</p>
<p><code>format_prompt</code> قالب ثابت <code>پرسش: ...\nپاسخ: </code> را می‌سازد. Tokenizer فقط از متن Pretraining و نمونه‌های آموزش ساخته و سپس ثابت می‌شود. دو نمونهٔ جدا، همان پرسش‌ها را با «آیا» شروع می‌کنند. این‌ها عبارت‌های ندیده‌اند، نه دانسته‌های تازه و نه یک Test معتبر برای همهٔ کاربردها. اگر مدام نتیجهٔ همین دو نمونه را ببینید و تنظیمات را تغییر دهید، آن‌ها نیز عملاً به Validation تبدیل می‌شوند.</p>
<h2>چه چیزی از حلقهٔ قبلی عوض شده است؟</h2>
<p>برای یک نمونه، <code>x</code> و <code>y</code> همچنان شکل <code>(1,T)</code> دارند و یک Token فاصله دارند. Logits شکل <code>(1,T,V)</code> دارد؛ Mask بولی مشخص می‌کند کدام Target پاسخ است. اول Logits را می‌گیریم، بعد <code>response_loss</code> میانگین Cross-Entropy همان Targetها را می‌سازد. معماری و Causal Mask عوض نشده‌اند. دادن <code>model(x,y)</code> به حلقه، Loss همهٔ موقعیت‌ها را می‌دهد و قرارداد این آزمایش نیست.</p>
<p>نمونه‌ها را یکی‌یکی آموزش می‌دهیم تا فعلاً Padding لازم نباشد. پس هر گام یک نمونه می‌بیند؛ در ارزیابی، Loss هر نمونه را در تعداد Tokenهای پاسخ ضرب و بر تعداد کل آن‌ها تقسیم می‌کنیم. اگر پاسخ‌ها هم‌طول نباشند، میانگین سادهٔ Loss نمونه‌ها با میانگین Tokenها برابر نیست. <code>eval</code> و <code>no_grad</code> نیز همان نقش قبلی را دارند و ارزیابی حالت پیشین مدل را برمی‌گرداند.</p>
<h2>گزارش را پیش از نتیجه‌گیری بخوانیم</h2>
<p>چهار گزارش داریم: پایه و تنظیم‌شده، هرکدام روی Training و عبارت‌های Held-out. هم Loss پاسخ را ثبت می‌کنیم و هم <dfn>Exact match (برابری دقیق پاسخ)</dfn> را: چند خروجی دقیقاً همان کاراکتر مورد انتظار بوده‌اند؟ بودجهٔ تولید از قبل یک Token است؛ طول جواب مرجع را به مدل نمی‌دهیم تا برای هر سؤال تصمیم بگیرد چقدر تولید کند. Mini-GPT فعلی EOS ندارد.</p>
<p>کاهش Loss Training نشان می‌دهد نمونه‌های هدف محتمل‌تر شده‌اند. بهبود روی دو عبارت تازه شاهدی محدودتر از دستورپذیری عمومی است. ردیف‌های غلط را هم نگه دارید. <a href="https://arxiv.org/abs/2203.02155">پژوهش InstructGPT</a> نمونه‌ای در مقیاسی بسیار متفاوت از استفاده از پاسخ‌های مطلوب برای تنظیم رفتار است؛ آزمایش ما بازتولید نتایج آن نیست. در درس بعد می‌پرسیم آیا برای چنین تنظیمی باید همهٔ وزن‌ها اجازهٔ تغییر داشته باشند.</p>""",
        "پیش از اجرا حدس بزنید کدام چهار مقدار تغییر می‌کنند. سپس در Notebook حلقهٔ Fine-Tuning را کامل کنید و ثابت‌ماندن نسخهٔ پایه را با مقایسهٔ Tensorها بررسی کنید.",
        "نسخهٔ تنظیم‌شده از کپی وزن‌های پایه آغاز می‌شود، اما Optimizer مستقل دارد. فقط نمونه‌های Training وارد update می‌شوند. معیارها باید واقعاً اندازه‌گیری شوند؛ هیچ شرطی نداریم که نتیجهٔ Held-out حتماً بهتر شود.",
        "چرا آموزش از وزن تصادفی روی همین چهار نمونه را نباید ادامهٔ Pretraining بنامیم؟ چرا پاسخ درست به دو بازنویسی، آزمون عمومی دانش نیست؟",
        "mini_gpt/instruction.py هستهٔ واقعی MiniGPT را بدون تغییر forward به یک آزمایش کوچک SFT وصل می‌کند. فایل‌های آموزش قبلی و وزن پایه دست‌نخورده می‌مانند.",
        code="""import torch
from mini_gpt.instruction import run_tiny_experiment

torch.set_num_threads(1)
experiment = run_tiny_experiment()
for name, report in experiment.reports.items():
    print(name, report['response_loss'], report['exact_match'])
    for row in report['rows']:
        print(row['instruction'], row['expected'], row['prediction'])
""",
        source="mini_gpt/instruction.py",
    ),
    Lesson(
        "81-system-eval", 14, "سنجش سامانه",
        "پاسخ درست بود؛ کدام مسیر آن را ساخته است؟",
        "شواهد بازیابی، پاسخ، ابزار و توقف را جدا می‌سنجیم و از جمع آن‌ها یک آزمون قابل تکرار برای سامانه می‌سازیم.",
        """<p>کنترل‌گر اجرا می‌شود و گاهی پاسخ خوبی هم می‌دهد. اما یک پاسخ درست ممکن است باوجود بازیابی خراب یا بدون اجرای ابزار لازم تولید شده باشد. برای تغییر بعدی، به چیزی بیشتر از «این بار خوب بود» نیاز داریم. چند درخواست کوچک با انتظار روشن ثبت می‌کنیم تا بدانیم کدام جزء کار می‌کند و کدام تغییر یک رفتار قبلی را خراب کرده است.</p>
<p>در <dfn>Evaluation fixture (نمونهٔ ثابت برای سنجش رفتار)</dfn>، ورودی، منابع مجاز و خروجی مورد انتظار مشخص‌اند. برای پرسش دربارهٔ Checkpoint، شاهد باید در زمینه جا شده باشد و پاسخِ این آزمون یک نقل دقیق از همان قطعه است. برای جمع دو و سه، نتیجهٔ ابزار باید ۵ باشد. برای موضوعی بیرون از سندها، پاسخ قراردادیِ «اطلاعات کافی ندارم» را می‌خواهیم. این سه مورد پوشش کامل نیستند؛ هرکدام یک مسیر متفاوت را قابل بررسی می‌کنند.</p>
<h2>یک نمره، چند سؤال را پنهان می‌کند</h2>
<p>چند چیز را جدا نگه دارید: آیا سند لازم پیدا شد؟ آیا در بودجهٔ Context جا شد؟ آیا پاسخ درست بود؟ آیا ارجاع واقعاً ادعا را پشتیبانی می‌کند؟ آیا ابزار با آرگومان درست اجرا شد؟ Loss و Perplexity پاسخ این سؤال‌ها را به‌تنهایی نمی‌دهند. در گزارش کوچک ما <code>retrieval_recall</code> سهم شناسه‌های شاهدِ مورد انتظار در خروجی بازیابی است. اگر دو شاهد لازم بوده و فقط یکی پیدا شده، مقدار ۰٫۵ است. <code>context_recall</code> سهم همان شاهدها را در Context نهایی می‌سنجد؛ ممکن است بازیابی درست باشد ولی محدودیت بودجه سند را کنار بگذارد. برای مسئلهٔ حساب که شاهد سندی تعریف نکرده‌ایم، مقدار <code>None</code> می‌گذاریم، نه صفر و نه یک.</p>
<p>برابری شناسهٔ ارجاع با منبع انتظاررفته، آزمونِ قرارداد است؛ داور معنای جمله نیست. برای پاسخ آزاد باید خودِ ادعا و عبارت پشتیبان را بخوانید و موارد «پشتیبانی‌شده»، «متناقض» و «بی‌شاهد» را با معیار روشن جدا کنید. یک پاسخ ممکن است چند ادعا داشته باشد. همچنین Exact match برای جواب کوتاه این تمرین مناسب است، اما دو توضیح درست با واژه‌بندی متفاوت را ممکن است نابرابر حساب کند. <a href="https://arxiv.org/abs/2211.09110">پژوهش HELM</a> نیز ارزیابی مدل زبان را به یک عدد محدود نمی‌کند.</p>
<p>برای Tool call فقط «عدد ۵ در پاسخ بود» کافی نیست. نام ابزار، آرگومان‌ها و مقدار برگشتی را از سابقهٔ اجرای واقعی بخوانید. در پایان، موفقیت مشترک یعنی همهٔ شرط‌های لازم همان مورد برقرار باشند. میانگین نمره‌های اجزا جای این شرط را نمی‌گیرد: بازیابی درست نمی‌تواند ابزار اشتباه را جبران کند.</p>
<h2>آزمون کنترل‌گر با آزمون مدل یکی نیست</h2>
<p>برای سنجش مسیر اجرا از <code>ScriptedFixture</code> استفاده می‌کنیم: پیشنهادهایش را از قبل نوشته‌ایم. بازیابی، ساخت Context، اعتبارسنجی و اجرای ابزار واقعی‌اند، ولی موفقیت این سناریوها توان آموختهٔ Mini-GPT را نشان نمی‌دهد. برچسب Backend در همهٔ سطرها باقی می‌ماند. سنجش مدل واقعی باید خروجی واقعی آن را نگه دارد؛ جایگزین‌کردن پاسخ غلط با جواب آماده، گزارش را بی‌اعتبار می‌کند.</p>
<p>پس از هر تغییر، همان شناسه‌های آزمون را دوباره اجرا کنید. <dfn>Regression (خراب‌شدن رفتاری که قبلاً درست کار می‌کرد)</dfn> را با نام مورد گزارش کنید، نه فقط تغییر میانگین. ممکن است دو خطا اصلاح و یک رفتار مهم خراب شود، درحالی‌که امتیاز کل بالاتر رفته است. برای توسعه، مجموعهٔ Validation و برای ادعای نهایی مجموعهٔ جدا لازم دارید؛ سناریوهای کوچک این درس تست مهندسی‌اند، نه مدرک آماریِ کیفیت عمومی.</p>""",
        "یک پاسخ درستِ دارای ارجاع غلط و یک پاسخ درستِ بدون اجرای ابزار لازم بسازید. پیش‌بینی کنید کدام ستون گزارش باید شکست بخورد، سپس سناریو را اجرا کنید.",
        "answer_exact می‌تواند True باشد، اما citations_exact یا tools_exact False باشد. passed تنها وقتی True است که همهٔ قراردادهای لازم و بررسی پاسخ برقرار باشند. نبود معیار برای یک مورد با شکست همان معیار فرق دارد.",
        "چرا نرخ موفقیت ۱ در سه سناریوی ازپیش‌نوشته‌شده، دقت ۱ برای مدل روی سؤال‌های کاربران نیست؟",
        "evaluation_system.py خروجی AssistantResult واقعی را می‌سنجد. قواعد قابل مشاهده‌اند و هیچ داور زبانی یا سرویس بیرونی پنهانی وجود ندارد.",
        code="""from mini_gpt.evaluation_system import course_fixture_suite, evaluate_cases

cases, run_case = course_fixture_suite()
report = evaluate_cases(cases, run_case)
for row in report['rows']:
    print(row)
print('Scripted controller fixtures, not model accuracy:', report['pass_rate'])
""",
        source="mini_gpt/evaluation_system.py",
    ),
    Lesson(
        "82-performance", 15, "هزینهٔ اجرا",
        "پیش از سریع‌ترکردن، چه چیزی را زمان بگیریم؟",
        "هزینهٔ تولید را روی کار ثابت اندازه می‌گیریم و تأخیر، توان عملیاتی و دقت عددی را با کیفیت پاسخ یکی نمی‌گیریم.",
        """<p>اکنون یک درخواست ممکن است بازیابی شود، از مدل پیشنهاد بگیرد، ابزار اجرا کند و دوباره به مدل برگردد. اگر پاسخ دیر برسد، گفتن «مدل کند است» هنوز تشخیص نیست. زمان بارگذاری، ساخت زمینه، محاسبهٔ نخستین Token، ادامهٔ تولید و انتظار در صف را جدا ببینید. بدون مشخص‌کردن مرز اندازه‌گیری، دو عدد زمان ممکن است اصلاً یک کار را نسنجند.</p>
<p><dfn>Latency (مدت انتظار برای پایان یک درخواست)</dfn> به تجربهٔ یک درخواست مربوط است. <dfn>Throughput (مقدار کار انجام‌شده در واحد زمان)</dfn> می‌تواند تعداد Token تولیدشده در ثانیه یا درخواست در ثانیه باشد؛ واحد را بنویسید. یک Batch بزرگ ممکن است مجموع Token در ثانیه را بیشتر کند ولی انتظار هر کاربر را کمتر نکند. عدد <code>B*N/time</code> برای B دنباله و N Token تازه در هر دنباله، نرخ مجموع تولید است، نه سرعت یک پاسخ.</p>
<h2>یک اندازه‌گیری کوچک، با مرز روشن</h2>
<p>تابع <code>benchmark_generation</code> خودِ <code>MiniGPT.generate</code> را روی CPU زمان می‌گیرد. وزن، Prompt، تعداد Token تازه و انتخاب Greedy ثابت‌اند. بارگذاری فایل و Tokenization بیرون این زمان‌اند؛ پس نتیجه را تأخیر کامل دستیار ننامید. یک اجرای Warm-up انجام می‌دهیم و چند بار اندازه می‌گیریم. میانه، کمینه و بیشینه را ثبت می‌کنیم؛ بهترین اجرای اتفاقی نمایندهٔ همهٔ درخواست‌ها نیست. تعداد Thread و نسخهٔ محیط را هم کنار عدد بنویسید.</p>
<p>زمان یک فراخوانیِ جدا با یک Token تازه نیز گزارش می‌شود. این فقط زمان نخستین Token در مرز همین مدل است، نه <dfn>Time to first token (زمان تا نخستین Token)</dfn> در یک سرویس کامل که صف و شبکه هم دارد. کتابخانه‌های GPU معمولاً عملیات را ناهمگام اجرا می‌کنند؛ زمان‌گیری سادهٔ CPU را بی‌تغییر به آن‌ها منتقل نکنید. <a href="https://docs.pytorch.org/tutorials/recipes/recipes/benchmark.html">راهنمای Benchmark در PyTorch</a> اثر Warm-up، Thread و همگام‌سازی را توضیح می‌دهد.</p>
<p>در آزمایش، فقط طول Prompt یا فقط Batch size را تغییر دهید. تعداد پارامترها ثابت می‌ماند ولی کار محاسباتی عوض می‌شود. انتظار نداریم هر افزایش کوچک، روی هر رایانه زمان را به نسبت دقیق افزایش دهد؛ سربار و نوسان اجرا هم وجود دارند. آزمون خودکار، صحت شمارش و خروجی را بررسی می‌کند، نه برتری سرعتی که به دستگاه وابسته است.</p>
<h2>بیت کمتر را واقعاً ببینیم</h2>
<p>برای Quantization ابتدا یک Tensor کوچک را بگیرید. در نمونهٔ متقارن ما، بزرگ‌ترین قدرمطلق را بر بزرگ‌ترین عدد صحیح مجاز تقسیم می‌کنیم تا <code>scale</code> ساخته شود. هر مقدار بر scale تقسیم، گرد و محدود می‌شود؛ بازسازی با ضرب در scale است. مثلاً با بازهٔ صحیح <code>[-7,7]</code>، فقط تعداد محدودی مقدار بازسازی‌شدنی داریم. خطای گردکردن را اندازه بگیرید و حالت Tensor صفر را جدا آزمایش کنید.</p>
<p>این تابع فقط نمایش عدد را نشان می‌دهد؛ Mini-GPT را به موتور تولید کم‌بیتی تبدیل نمی‌کند. حتی خروجی چهار‌بیتیِ قراردادی در کد ما داخل Tensor از نوع <code>int8</code> نگهداری می‌شود و واقعاً در نیم‌بایت بسته‌بندی نشده است. برای ادعای کاهش حافظه، اندازهٔ ذخیرهٔ واقعی و scale را حساب کنید؛ برای ادعای سرعت، Kernel مناسب و اندازه‌گیری لازم است. KV cache نیز هنوز در <code>generate</code> فعال نیست. کیفیت، حافظه و زمان سه گزارش جدا می‌خواهند.</p>""",
        "یک جدول با طول‌های Context متفاوت و تعداد Token خروجی ثابت بسازید. مرز زمان‌گیری، Thread، میانه و بازهٔ مشاهده را ثبت کنید. سپس خطای بازسازی یک Tensor را با ۴ و ۸ بیت مقایسه کنید.",
        "تغییر طول Context شمار پارامترها را عوض نمی‌کند ولی کار تولید را تغییر می‌دهد. تعداد بیت قراردادی با dtype ذخیرهٔ واقعی یکی نیست؛ این مثال هیچ شتاب کم‌بیتی را اثبات نمی‌کند.",
        "چرا نصف‌شدن زمان تولیدِ یک Batch لزوماً به معنی نصف‌شدن زمان انتظار هر درخواست در صف نیست؟",
        "performance.py روی همان generate بدون Cache اندازه‌گیری می‌کند؛ هیچ مسیر سریع مخفی یا نتیجهٔ زمان ساختگی نداریم.",
        code="""import torch
from mini_gpt.config import ModelConfig
from mini_gpt.model import MiniGPT
from mini_gpt.performance import benchmark_generation, quantize_symmetric

torch.set_num_threads(1)
torch.manual_seed(7)
model = MiniGPT(ModelConfig(12, 32, 16, 2, 1, 0.0))
for length in (4, 12):
    prompt = torch.ones(1, length, dtype=torch.long)
    print(benchmark_generation(model, prompt, new_tokens=4))
values = torch.tensor([-1.0, -0.2, 0.0, 0.3, 1.0])
for bits in (4, 8):
    integer, scale, restored = quantize_symmetric(values, bits)
    print(bits, integer, scale, (restored-values).abs().max().item())
""",
        source="mini_gpt/performance.py",
    ),
    Lesson(
        "83-deployment", 15, "اجرای قابل تکرار",
        "مدل را از دفتر آزمایش بیرون بیاوریم، با چه قراردادی؟",
        "یک بستهٔ سازگار برای تولید و یک مرز ورودی محدود می‌سازیم؛ آن را با سرویس عمومی آمادهٔ بهره‌برداری اشتباه نمی‌گیریم.",
        """<p>داخل Notebook همه‌چیز در حافظه حاضر است: مدل، Tokenizer، تنظیمات و شاید چند متغیر از Cellهای قبلی. کاربر بعدی فقط درخواستش را می‌دهد. برای اینکه همان محاسبه دوباره اجرا شود، باید وابستگی‌های پنهان را به یک بسته و قرارداد روشن تبدیل کنیم. اولین قدم این درس اجرای محلیِ قابل تکرار است، نه بازکردن پورت روی اینترنت.</p>
<p>یک بستهٔ Inference حداقل وزن‌ها، تنظیمات معماری و Vocabulary مرتب‌شده را با هم نگه می‌دارد. Tokenizer تازه ساختن پس از بارگذاری، با درست‌بودن Shapeها سازگاری را تضمین نمی‌کند. <code>save_instruction_bundle</code> بستهٔ کوچکِ درس SFT را ذخیره می‌کند و از بازنویسی فایل موجود جلوگیری می‌کند. این فایل برای تولید است؛ Optimizer، وضعیت تصادف و سابقهٔ داده ندارد و جای Checkpoint ادامهٔ آموزش نیست. فقط فایل محلیِ مورد اعتماد را بارگذاری کنید؛ محدودکردن بارگذاری با <code>weights_only=True</code> مجوز اعتماد به فایل ناشناس نیست.</p>
<h2>کار نامعتبر را پیش از تولید رد کنیم</h2>
<p><code>InferenceSession</code> مدل را یک بار می‌گیرد و برای درخواست‌های بعدی همان شیء را استفاده می‌کند. درخواست خالی، متن بیش از سقف، کاراکتر ناشناخته و بودجهٔ خروجی نامعتبر رد می‌شوند. همچنین مجموع طول Prompt و بودجهٔ خروجی باید در Context جا شود. <code>generate</code> اصلی می‌تواند پنجره را ببرد؛ این لایه عمداً به‌جای حذف بی‌خبرِ بخش مهم درخواست خطا می‌دهد. انتخاب قرارداد باید برای فراخواننده روشن باشد.</p>
<p>مثلاً اگر Context برابر ۶۴ و خروجی رزروشده ۸ Token باشد، حداکثر ۵۶ Token برای ورودی داریم. این عدد شامل قالب درخواست هم هست، نه فقط متن کاربر. در Tokenizer کاراکتری ما طول متن و تعداد Token به هم نزدیک‌اند، اما برای Tokenizer دیگر نباید شمار کاراکتر را جای شمار Token بگذارید. سقف کاراکتر فقط یک کنترل ورودی زودهنگام و جداست.</p>
<p>هنگام تولید از <code>eval</code> و <code>no_grad</code> استفاده می‌شود و وزن‌ها تغییر نمی‌کنند. خروجی شامل تعداد Token ورودی و خروجی است تا هزینه را بتوان ثبت کرد. برای لاگ عادی، شناسهٔ درخواست، نسخهٔ بسته، زمان و وضعیت معمولاً از ذخیرهٔ بی‌قید متن شخصی مناسب‌ترند؛ دربارهٔ نگهداری داده باید تصمیم مستقل بگیرید. این کد خودکار هیچ حافظه‌ای از کاربر نمی‌نویسد.</p>
<h2>این مرز چه چیزهایی را هنوز حل نکرده است؟</h2>
<p>سقف Token، Timeout واقعی نیست. این پیاده‌سازی همگام و تک‌درخواستی است؛ احراز هویت، صف، محدودکردن هم‌زمانی، قطع اجرای طولانی، مانیتورینگ و بازیابی پس از خرابی را ندارد. اجرا روی CPU یا GPU نیز باید با حافظه و اندازه‌گیری واقعی انتخاب شود؛ نام GPU تضمین تأخیر کمتر برای هر کار کوچک نیست. پیش از انتشار عمومی، طراحی سرویس و امنیت آن پروژهٔ جداگانه‌ای است.</p>
<p>برای تحویل آموزشی، یک آزمون Round-trip کافیِ مفید داریم: پیش از ذخیره و پس از بارگذاری، همان Prompt و Greedy را اجرا و خروجی را مقایسه کنید. سپس ورودی نامعتبر را امتحان کنید و مطمئن شوید اصلاً به تولید نرسیده است. موفقیت این آزمون به معنی بسته‌بندی سازگار است، نه کیفیت زبانی یا آمادگی سرویس برای کاربران واقعی.</p>""",
        "در پوشهٔ موقت یک بسته ذخیره کنید، آن را بارگذاری و خروجی ثابت را مقایسه کنید. سپس درخواست خالی، ناشناخته و بیش از Context را جدا امتحان کنید؛ هیچ‌کدام نباید بی‌خبر بریده یا پذیرفته شود.",
        "Round-trip باید Vocabulary و وزن‌ها را با هم حفظ کند. InferenceSession قبل از generate قرارداد را بررسی می‌کند. اجرای تکرارپذیر محلی هنوز سرویس هم‌زمان و امن عمومی نیست.",
        "چرا فایل کافی برای تولید ممکن است برای Resume آموزش کافی نباشد؟ کدام محدودیت این کد تعداد کار را می‌بندد ولی مدت انتظار را نه؟",
        "بستهٔ SFT فقط مسیر تولید دارد و با قالب Checkpoint آموزشی قبلی مخلوط نمی‌شود. Notebook از TemporaryDirectory استفاده می‌کند و هیچ اجرای قبلی را بازنویسی نمی‌کند.",
        code="""import tempfile
from pathlib import Path
import torch
from mini_gpt.instruction import (run_tiny_experiment, format_prompt,
    save_instruction_bundle, load_instruction_bundle)
from mini_gpt.performance import InferenceSession, RequestLimits

torch.set_num_threads(1)
experiment = run_tiny_experiment(pretrain_steps=2, sft_steps=4)
prompt = format_prompt('در آموزش هدف داریم؟')
before = InferenceSession(experiment.tuned, experiment.tokenizer).request(prompt)
with tempfile.TemporaryDirectory(prefix='aibook-inference-') as directory:
    path = Path(directory) / 'model.pt'
    save_instruction_bundle(path, experiment.tuned, experiment.tokenizer)
    model, tokenizer = load_instruction_bundle(path)
    after = InferenceSession(model, tokenizer, RequestLimits(128, 8)).request(prompt)
assert before == after
print(after, 'Round-trip only; this short run does not establish answer quality.')
""",
        source="mini_gpt/performance.py",
    ),
    Lesson(
        "84-capstone", 15, "پروژهٔ یکپارچه",
        "دستیار مطالعهٔ کوچک را با شواهد تحویل بدهیم",
        "مدل، بازیابی، حافظه، ابزار، ارزیابی و محدودیت اجرا را در یک پروژهٔ محلی کنار هم می‌گذاریم و برای هر ادعا شاهد مناسب می‌آوریم.",
        """<p>پرسش نهایی «چند اصطلاح یاد گرفته‌اید؟» نیست. آیا می‌توانید بگویید پاسخ از کجا آمده، کدام قطعه تصمیم گرفته، چه چیزی اجرا شده و چه چیزی هنوز قابل اعتماد نیست؟ پروژهٔ پایانی یک دستیار مطالعهٔ آفلاین است: سندهای همین دوره را می‌گیرد، زمینه را در بودجه جا می‌دهد، در صورت نیاز از ابزار حساب استفاده می‌کند و نتیجه را همراه سابقهٔ قابل بررسی برمی‌گرداند.</p>
<h2>دو مسیر را صادقانه کنار هم نگه داریم</h2>
<p>مسیر اول، مدل واقعی است. در درس SFT، Mini-GPT را واقعاً آموزش دادید و پاسخ‌های پایه و تنظیم‌شده را دیدید. <code>MiniGPTBackend</code> همان Tokenizer و <code>generate</code> را به رابط کنترل‌گر وصل می‌کند؛ پاسخ آماده‌ای جای خروجی ضعیف را نمی‌گیرد. مدل ۶۴موقعیتیِ آن درس برای دستور کنترل‌گر جا ندارد. این بار یک آزمایش مستقل با ظرفیت ۲۰۴۸ موقعیت می‌سازیم؛ از ابتدا کاراکترهای قالب JSON، دستور کنترل‌گر و سندهای دوره را در دادهٔ Pretraining مشخص می‌کنیم. Vocabulary را از سؤال آزمون نمی‌سازیم و جدول مدل قبلی را پس از آموزش بزرگ نمی‌کنیم.</p>
<p><code>run_system_experiment</code> فقط دو گام Pretraining و چهار گام SFT دارد؛ هدفش عبور خروجی واقعی مدل از رابط کنترل‌گر است، نه ساخت مدل خوب. بزرگ‌ترین دنبالهٔ آموزش در این تنظیم کوتاه، ۴۲ موقعیت دارد؛ موقعیت‌های دورتر ظرفیت تخصیص‌یافته‌اند و در این اجرا اصلاً در آموزش به کار نرفته‌اند. در گزارش، هم ظرفیت و هم بزرگ‌ترین طول واقعاً دیده‌شده را چاپ می‌کنیم. تولید واقعاً اجرا می‌شود، اما مدلِ چهارپرسشی JSON و استفاده از ابزار را نیاموخته است. خروجی نامعتبر ثبت و کنترل‌گر متوقف می‌شود؛ افزایش Context به‌تنهایی توان پیروی از پروتکل نمی‌سازد.</p>
<p>مسیر دوم، آزمون اجزای سامانه است. با <code>ScriptedFixture</code> پیشنهادهای ازپیش‌نوشته‌شده را وارد همان کنترل‌گر می‌کنیم تا Retrieval، Context، ابزار، وارسی و توقف را بسنجیم. این Fixture مدل زبان نیست. موفقیتش می‌گوید مسیر اجرایی در آن سناریو درست کار کرده؛ نمی‌گوید مدل خودبه‌خود چنین تصمیمی می‌گیرد. کنار هم گذاشتن این دو گزارش، صادقانه‌تر از نمایش یک جواب روان با منشأ نامعلوم است.</p>
<h2>چه چیزی باید در تحویل باشد؟</h2>
<p>یک درخواست مستند، یک درخواست حسابی و یک درخواست بی‌شاهد را اجرا کنید. برای هرکدام Backend، وضعیت پایان، شناسهٔ قطعه‌های واردشده به Context، Tool call و نتیجهٔ وارسی را ثبت کنید. یک ورودی خراب هم لازم است: ابزار نامجاز، ارجاعی که وارد Context نشده یا پیشنهاد تکراری. انتظار شکست کنترل‌شده را پیش از اجرا بنویسید. آزمون منفی بخشی از تحویل است، نه حاشیهٔ آن.</p>
<p>حافظهٔ بیرونی را با اجازه و عملیات صریح بسازید؛ مثلاً ترجیح «توضیح کوتاه» را با منبع ثبت کنید. برای ورود آن به Context، کلید مجاز را با <code>memory_keys</code> جدا انتخاب می‌کنیم؛ صرف داشتن Store اجازهٔ ارسال همهٔ رکوردها نیست. بازیابی رکورد در Context به معنی تغییر وزن نیست. همچنین دیدن یک ترجیح توسط مدل، تضمین رعایت آن نیست. یک بار رکورد را حذف و تفاوت زمینه را ببینید. فایل‌های تمرین باید در پوشهٔ موقت بمانند؛ هیچ اطلاعات شخصی برای این پروژه لازم نیست.</p>
<p>گزارش نهایی چهار بخش کوتاه دارد: نتیجهٔ واقعی SFT و محدودیت داده؛ سناریوهای ثابت کنترل‌گر و Regressionها؛ اندازه‌گیری هزینه با مرز روشن؛ و محدودیت‌های بستهٔ اجرا. زمان مدل تصادفی را می‌توان برای بررسی مکانیک اندازه گرفت، ولی نباید همان اجرا را شاهد کیفیت پاسخ نامید. اگر یک جزء بهتر شد، فقط یک عامل را تغییر دهید و همان مجموعهٔ سنجش را دوباره اجرا کنید.</p>
<h2>پروژه تمام می‌شود؛ مرز ادعا باقی می‌ماند</h2>
<p>اکنون مسیر متن تا Token، Embedding، Attention، Logits و Gradient را می‌شناسید. سپس دیدید چرا برای دستیار، دادهٔ دستور، زمینهٔ مناسب، سند، حافظهٔ بیرونی، ابزار، کنترل‌گر و ارزیابی هم لازم‌اند. این‌ها یک موجود پنهان داخل وزن‌ها نیستند؛ قطعه‌های مشخص با قراردادهای قابل مشاهده‌اند. قدم بعدی را از یک شکست واقعی انتخاب کنید: دادهٔ بهتر، ارزیابی تازه، Context مناسب‌تر یا اجرای کارآمدتر. پیش از تغییر، بنویسید چه شاهدی می‌تواند ادعای شما را رد کند.</p>""",
        "در Notebook، گزارش تحویل را از نتایج واقعی بسازید. یک Regression عمدی ایجاد و نام مورد خراب‌شده را ثبت کنید. نتیجهٔ Fixture و نتیجهٔ مدل را در دو ستون جدا نگه دارید.",
        "گزارش معتبر منبع هر خروجی را مشخص می‌کند، شکست مدل کوچک را پنهان نمی‌کند و تست‌های منفی دارد. موفقیت مسیر اجرایی، دستورپذیری مدل را اثبات نمی‌کند؛ هرکدام سنجش خودش را می‌خواهد.",
        "اگر فرد دیگری فقط گزارش شما را ببیند، آیا می‌تواند پاسخ تولیدشده توسط مدل را از پاسخ ازپیش‌نوشته‌شده تشخیص دهد؟ کدام ادعای مهم هنوز آزمون مستقل ندارد؟",
        "پروژهٔ نهایی از همان MiniGPT و ماژول‌های کوچک context، retrieval، memory، tools، assistant و evaluation_system ساخته می‌شود؛ هیچ API پولی، شبکه یا Framework عامل لازم نیست.",
        code="""import torch
from mini_gpt.instruction import run_system_experiment
from mini_gpt.assistant import MiniGPTBackend, run_assistant
from mini_gpt.evaluation_system import course_fixture_suite, evaluate_cases
from mini_gpt.retrieval import COURSE_DOCUMENTS, chunk_document

torch.set_num_threads(1)
experiment = run_system_experiment()
print('Actual training limits:', experiment.metadata)
backend = MiniGPTBackend(experiment.tuned, experiment.tokenizer, max_new_tokens=8)
chunks = [c for d in COURSE_DOCUMENTS for c in chunk_document(d, chunk_words=24, overlap_words=4)]
actual = run_assistant('Checkpoint', backend, chunks=chunks)
print('Actual tiny model path:', actual.status, actual.backend, 'generate calls:', backend.calls)
print('Actual proposals:', [e['text'] for e in actual.events if e['state'] == 'propose'])
cases, runner = course_fixture_suite()
fixture_report = evaluate_cases(cases, runner)
print('Scripted integration fixtures:', fixture_report['pass_rate'])
print('These are different kinds of evidence; do not combine them as model accuracy.')
""",
        source="mini_gpt/assistant.py",
    ),
]
