"""From a proposed action to a checked answer, without a framework."""
from .schema import Lesson


LESSONS = [
Lesson("75-tools",13,"اجرای کنترل‌شده","نوشتنِ نام یک تابع، اجرای آن نیست",
"درخواست محاسبه را به دادهٔ ساخت‌یافته تبدیل می‌کنیم و مرز پیشنهاد مدل با اجرای Python را می‌سازیم.",
"""<p>از دستیار راهنمای دوره می‌پرسید: «۴۸ جلسهٔ ۲۵دقیقه‌ای جمعاً چند دقیقه می‌شود؟» متنِ «۲۵ ضربدر ۴۸» هنوز هیچ تابعی را اجرا نکرده است. Mini-GPT همان Logits و Tokenهای آشنا را تولید می‌کند؛ نه ماشین‌حسابی در اختیار دارد و نه با نوشتن نام یک سرویس به آن وصل می‌شود. برای محاسبهٔ قابل وارسی، باید برنامهٔ بیرون مدل یک تابع مشخص را اجرا کند.</p>
<p>اول خودمان درخواست را می‌نویسیم: <code>{"name":"multiply","arguments":{"a":25,"b":48}}</code>. این یک شیء <dfn>JSON (قالب متنی برای دادهٔ ساخت‌یافته)</dfn> است. در Python با <code>json.loads</code> به دیکشنری تبدیل می‌شود. <code>name</code> تابع مجاز را مشخص می‌کند و <code>arguments</code> ورودی‌هایش را. هیچ رشته‌ای را به <code>eval</code> نمی‌دهیم و از نام تولیدشده برای import دلخواه استفاده نمی‌کنیم.</p>
<p><dfn>Tool calling یا Function calling (پیشنهاد فراخوانی ابزار)</dfn> همین ارتباط قراردادی را ممکن می‌کند: مدل یک درخواست می‌سازد؛ کد سامانه آن را بررسی و در صورت مجازبودن اجرا می‌کند؛ نتیجه دوباره به Context می‌رود. در این مثال، نتیجهٔ واقعی Python برابر ۱۲۰۰ است. اگر پاسخ نهایی «۱۲۰ دقیقه» باشد، اجرای درست ابزار به‌تنهایی پاسخ را درست نکرده است؛ مرحلهٔ استفاده از نتیجه هم باید بررسی شود.</p>
<p><dfn>Schema (قرارداد ساختار و نوع داده)</dfn> فقط می‌گوید چه شکل درخواستی پذیرفته می‌شود. قرارداد ما دو کلید دقیق، نام یکی از سه ابزار <code>add</code>، <code>subtract</code> یا <code>multiply</code> و دو عدد صحیح محدود می‌خواهد. برای مثال، <code>"48"</code> رشته است، نه عدد؛ <code>true</code> هم در JSON یک Boolean است. چون <code>bool</code> در Python زیرکلاس <code>int</code> است، آزمون سادهٔ <code>isinstance(value,int)</code> کافی نیست. عددهای خیلی بزرگ و کلیدهای اضافه را هم رد می‌کنیم.</p>
<p>پس سه پرسش جدا داریم: آیا متن، JSON معتبر است؟ آیا داده با Schema سازگار است؟ آیا این ابزار برای این درخواست مجاز است؟ معتبر بودن JSON پاسخی به دو سؤال بعدی نمی‌دهد. <dfn>Allowlist (فهرست صریح گزینه‌های مجاز)</dfn> پروژه فقط محاسبهٔ آفلاین دارد؛ تابع فایل، شبکه یا اجرای فرمان در آن نیست. متنِ سند بازیابی‌شده نیز مجوز تازه‌ای به این فهرست اضافه نمی‌کند.</p>
<p>تابع <code>execute_tool</code> خروجی ساخت‌یافته با <code>ok</code>، <code>name</code>، <code>value</code> و <code>error</code> می‌دهد. ورودی نامعتبر، نتیجهٔ موفقِ صفر نیست: <code>ok=False</code> است و <code>value=None</code>. این تفاوت اجازه می‌دهد سامانه بعداً تصمیم بگیرد اصلاح درخواست مجاز است یا باید متوقف شود. ابزار واقعیِ شبکه‌ای ممکن است Timeout، دسترسی نامجاز یا اطلاعات قدیمی داشته باشد؛ ماشین‌حساب ما این پیچیدگی را پنهان نمی‌کند، فقط هنوز آن را اجرا نمی‌کند.</p>
<p>این مرز را با توانایی انتخاب ابزار اشتباه نگیرید. درخواست مثال را انسان نوشته است؛ Mini-GPT کوچک ما با اضافه‌شدن این تابع ناگهان انتخاب‌گر قابل اعتماد نشده است. در آزمایشگاه، درخواست‌های صحیح و خرابِ معلوم را می‌سنجیم. بعداً خروجی یک Backend واقعی را هم از همین دروازه عبور می‌دهیم: اگر قالبش معتبر نباشد، پاسخ آماده‌ای جای آن نمی‌گذاریم.</p>""",
"درخواست ۲۵×۴۸ را دستی بنویسید. سپس فقط نوع b را به رشته تغییر دهید؛ نام ابزار را جداگانه به گزینه‌ای ناموجود تغییر دهید. مشخص کنید هر خطا در کدام مرحله باید متوقف شود.",
"درخواست عددیِ معتبر ۱۲۰۰ می‌دهد. رشتهٔ 48 خطای قرارداد نوع و نام ناشناخته خطای Allowlist است. هیچ‌کدام نباید به اجرای Python دلخواه یا پاسخ موفق تبدیل شود.",
"چرا افزودن یک تابع به برنامه با آموزش مدل برای انتخاب درست آن تابع یکسان نیست؟",
"tools.py بیرون Transformer است. Tokenهای مدل فقط پیشنهادند؛ validate_call و execute_tool مرز اجرای واقعی را می‌سازند.",
code='''from mini_gpt.tools import execute_tool
request = {"name":"multiply", "arguments":{"a":25, "b":48}}
result = execute_tool(request)
assert result.ok and result.value == 1200
bad = {"name":"multiply", "arguments":{"a":25, "b":"48"}}
assert not execute_tool(bad).ok
print(result, execute_tool(bad))
''', source="mini_gpt/tools.py",
review="یک ابزار درست داریم. مسئلهٔ بعدی این است که وقتی پاسخ به چند محاسبه وابسته است، ترتیب و معنای آن‌ها را چطور بررسی کنیم."),

Lesson("76-reasoning",13,"حل و وارسی","محاسبهٔ درست برای مسئلهٔ اشتباه",
"مسئله را به گام‌های کوچک می‌شکنیم، وابستگی‌ها را ثبت می‌کنیم و جواب را مستقل از برنامهٔ پیشنهادی می‌سنجیم.",
"""<p>هر روز ۲۵ دقیقه مطالعه و ۲۰ دقیقه آزمایش دارید؛ این برنامه را سه روز انجام می‌دهید. ابتدا بدون مدل حساب کنید: زمان روزانه ۴۵ و زمان کل ۱۳۵ دقیقه است. اکنون برنامه‌ای پیشنهاد شده که اول <code>20*3</code> و سپس <code>25+60</code> را اجرا می‌کند. هر دو محاسبه درست‌اند، ولی ۸۵ پاسخ سؤال ما نیست: زمان مطالعه فقط یک بار شمرده شده است.</p>
<p><dfn>Decomposition (شکستن مسئله به گام‌های کوچک)</dfn> باید وابستگی مسئله را حفظ کند، نه اینکه فقط تعداد خط‌ها را زیاد کند. نام گام اول را <code>daily</code> می‌گذاریم و نتیجهٔ آن را ورودی گام دوم می‌کنیم. فرمول کوتاه <code>total=(reading+lab)*days</code> می‌گوید هر دو زمان در تعداد روزها ضرب می‌شوند. همهٔ عددها به دقیقه‌اند؛ اگر یکی ساعت باشد، پیش از جمع باید واحدها را هماهنگ کنیم.</p>
<div class="math">daily = 25 + 20 = 45<br>total = daily × 3 = 135 minutes</div>
<p>در کد، هر گام یک شناسه، نام ابزار و Arguments دارد. یک Argument می‌تواند عدد یا شناسهٔ نتیجهٔ قبلی باشد. <code>execute_plan</code> فقط به گام‌های انجام‌شده ارجاع می‌دهد؛ نام تکراری، ارجاع به آینده یا برنامهٔ بیش از سقف مجاز خطاست. نتیجهٔ ثبت‌شدهٔ هر گام شامل ورودی‌های حل‌شده و مقدار واقعی ابزار است. این <dfn>Execution trace (گزارش مراحل واقعاً اجراشده)</dfn> را می‌توان دوباره محاسبه کرد؛ صرفاً داستانی دربارهٔ اجرا نیست.</p>
<p><dfn>Verification (وارسی با یک معیار مشخص)</dfn> را در دو سطح انجام دهید. وارسی محاسبه می‌گوید ابزار ۲۵ و ۲۰ را واقعاً جمع کرده است. وارسی مسئله می‌گوید پاسخ با تعداد جلسه‌ها و روزهای خواسته‌شده سازگار است. تابع <code>verify_study_answer</code> از ورودی اصلی <code>sessions</code> و <code>days</code> پاسخ مرجع می‌سازد؛ به ترتیب گام‌های پیشنهادی اعتماد نمی‌کند. اگر همان برنامهٔ خراب را هم اجرا و هم مرجع معرفی کنیم، خطای معنا از هر دو مسیر عبور می‌کند.</p>
<p>وقتی از <dfn>Reasoning (حل مسئله با ترکیب چند گام)</dfn> در مدل زبان حرف می‌زنیم، ادعا نمی‌کنیم مدل مانند انسان تجربهٔ ذهنی دارد. یک روش پژوهشی، <dfn>Chain-of-Thought prompting (ارائهٔ نمونه‌های دارای گام‌های میانی در Prompt)</dfn> است: در <a href="https://arxiv.org/abs/2201.11903">پژوهش اصلی</a>، چنین نمونه‌هایی در بعضی کارها و مدل‌های بزرگ به بهبود پاسخ کمک کردند. این نتیجه، تضمین توانایی Mini-GPT کوچک ما یا هر سؤال تازه نیست.</p>
<p>سه چیز را جدا نگه دارید: Activationهای مدل که در درس ردیابی دیدیم؛ متنی که مدل دربارهٔ راه‌حل تولید می‌کند؛ و گزارش اجرای ابزار که برنامه ثبت می‌کند. متن توضیح ممکن است قانع‌کننده ولی ناقص یا نادرست باشد. <a href="https://arxiv.org/abs/2305.04388">پژوهش دربارهٔ توضیح‌های نامطمئن</a> نشان می‌دهد توضیح تولیدشده لزوماً علت واقعی پاسخ مدل را بازگو نمی‌کند. پس گزارش محاسبهٔ آفلاین ما را «خواندن فکر مدل» ننامید.</p>
<p>در این مثال جواب مرجع ارزان و دقیق است؛ در خلاصه‌سازی یا پاسخ مستند چنین Oracle ساده‌ای نداریم. ممکن است آزمون واحد، بررسی واحد اندازه‌گیری، مقایسه با منبع یا قضاوت انسانی لازم شود. عبارت «خودت پاسخت را بررسی کن» می‌تواند پیشنهاد دیگری تولید کند، اما استقلالِ شاهد را تضمین نمی‌کند. مرز تواناییِ Verifier را به‌اندازهٔ مرز خود مدل صریح بنویسید.</p>""",
"دو برنامهٔ صحیح و ۸۵دقیقه‌ای را با ابزارها اجرا کنید. برای هرکدام جدا بنویسید آیا گام‌ها عددی درست اجرا شده‌اند و آیا پاسخ با مسئله تطابق دارد. سپس فقط تعداد روزها را تغییر دهید.",
"هر دو برنامه می‌توانند از نظر اجرای ابزار صحیح باشند. فقط برنامه‌ای که مجموع ۲۵ و ۲۰ را در سه ضرب می‌کند، پاسخ ۱۳۵ را می‌دهد. با چهار روز، جواب مرجع ۱۸۰ است؛ آزمون باید به ورودی تازه وابسته باشد.",
"چرا یک گزارشِ طولانی‌تر یا ظاهراً منطقی، جای آزمونِ مستقل پاسخ را نمی‌گیرد؟",
"reasoning.py برنامهٔ کوچک و Verifier را بیرون MiniGPT نگه می‌دارد. مدل می‌تواند پیشنهاد بدهد؛ منشأ پیشنهاد با صحت اجرای آن یکی نیست.",
code='''from mini_gpt.reasoning import execute_plan, verify_study_answer
plan = [
    {"id":"daily", "name":"add", "arguments":{"a":25,"b":20}},
    {"id":"total", "name":"multiply", "arguments":{"a":"daily","b":3}},
]
trace = execute_plan(plan)
assert trace[-1].value == 135
assert verify_study_answer([25,20],3,trace[-1].value)
assert not verify_study_answer([25,20],3,85)
for step in trace:
    print(step)
''', source="mini_gpt/reasoning.py"),

Lesson("77-candidates",13,"حل و وارسی","چند پاسخ پیشنهادی، کدام شاهد قوی‌تر؟",
"رأی میان پاسخ‌ها را با وارسی مستقل مقایسه می‌کنیم و هزینهٔ محاسبهٔ بیشتر هنگام استفاده را می‌شماریم.",
"""<p>برای مسئلهٔ ۱۳۵دقیقه‌ای سه پاسخ داریم: <code>[135,135,85]</code>. انتخاب پرتکرارترین عدد در این مثال موفق است. حالا فقط فهرست را به <code>[85,85,135]</code> تغییر دهید؛ همان قانون به پاسخ غلط می‌رسد. توافق بیشتر را مشاهده کرده‌ایم، نه یک مدرک مستقل برای درستی.</p>
<p>یک پاسخ پیشنهادی را <dfn>Candidate (گزینهٔ پاسخ)</dfn> می‌نامیم. می‌توان چند Candidate ساخت و سپس انتخاب کرد. در <dfn>Self-consistency (انتخاب با توجه به سازگاری پاسخ‌های چند مسیر)</dfn>، روش <a href="https://arxiv.org/abs/2203.11171">مقالهٔ اصلی</a> چند مسیر حل را نمونه‌گیری می‌کند و پاسخ‌های نهایی آن‌ها را کنار هم می‌سنجد. آزمایش کوچک ما فقط مرحلهٔ جمع‌بندی پاسخ‌ها را جدا کرده است: این فهرست‌ها دستی‌اند و نه خروجی نمونه‌گیری از Mini-GPT و نه بازتولید نتیجهٔ مقاله.</p>
<p>ابتدا با یک دیکشنری بشمارید هر عدد چند بار آمده است. برای <code>[135,85,135]</code> شمارش‌ها ۲ و ۱ هستند. اگر بیشترین شمارش مساوی باشد، تابع آموزشی پاسخ <code>None</code> می‌دهد؛ ترتیب فهرست نباید بی‌سروصدا رأی را تعیین کند. نام API ما <code>majority_answer</code> است، اما قرارداد دقیقش «پرتکرارترین پاسخ یکتا» است؛ لزوماً بیش از نصف رأی‌ها لازم نیست. خروجی همچنین تعداد رأی، کل نمونه‌ها و وجود تساوی را نگه می‌دارد.</p>
<p>Verifier درس قبل شاهد دیگری دارد: ورودی مسئله. به همین دلیل می‌تواند در فهرست <code>[85,85,135]</code> گزینهٔ ۱۳۵ را بپذیرد، هرچند کم‌تکرارتر است. تابع <code>select_verified</code> اگر هیچ پاسخ متمایز پذیرفته نشود، یا Verifier چند پاسخ متفاوت را بپذیرد، <code>None</code> برمی‌گرداند. این توقف بهتر از پنهان‌کردن ضعف معیار انتخاب است. البته اگر خود Verifier غلط باشد، همین مسیر هم می‌تواند شکست بخورد.</p>
<p>ساخت Candidateهای بیشتر بخشی از <dfn>Inference-time compute (محاسبهٔ صرف‌شده هنگام پاسخ‌دادن)</dfn> است. برای مثال، سه ادامهٔ ۲۰توکنی به‌جای یک ادامه، تا ۶۰ Token خروجی دارد و Prompt هم ممکن است سه بار پردازش شود. این شمارش، زمان دقیق اجرا نیست؛ طول‌های واقعی، سخت‌افزار، Batch و روش پیاده‌سازی مهم‌اند. در آزمایشگاه تعداد Candidate، پاسخ انتخاب‌شده و شمار فراخوانی Verifier را کنار هم گزارش می‌کنیم، نه فقط موفق‌ترین جواب را.</p>
<p>فقط بلندترکردن متن یا تکرار همان پاسخ Greedy، تنوع مفید ایجاد نمی‌کند. اگر همهٔ پیشنهادها از یک اشتباه مشترک بیایند، رأی آن اشتباه را تقویت می‌کند. تغییر Temperature می‌تواند تنوع را عوض کند، اما بدون سنجش، بهبود کیفیت نیست. برای مقایسهٔ روش‌ها، سؤال‌ها و بودجه را ثابت نگه دارید و سؤال‌های ندیده را از مثال‌هایی که تنظیم روش بر اساس آن‌ها انجام شده جدا کنید.</p>
<p>این مسیر را از تغییر وزن‌ها جدا کنید. Prompting، چند بار Generate کردن، اجرای ابزار و انتخاب پاسخ، در مثال ما هیچ <code>backward</code> یا <code>optimizer.step</code> ندارند. در مقابل، آموزش روی نمونه‌های حلِ بررسی‌شده یا بازخورد دربارهٔ پاسخ‌ها می‌تواند Parameterها را تغییر دهد؛ به بحث SFT و Preference training برگردید. پژوهش روی آموزشِ توانایی حل مسئله بسیار گسترده‌تر از این آزمایش است. اینجا تنها مرز «بهتر انتخاب‌کردن با وزن ثابت» و «آموزش‌دادن وزن‌ها» را روشن می‌کنیم.</p>""",
"برای فهرست صحیح، فهرستِ اکثراً غلط، حالت مساوی و فهرست خالی، نتیجهٔ رأی و Verifier را پیش‌بینی کنید. سپس فقط تعداد پاسخ‌های تکراریِ غلط را زیاد کنید؛ آیا شاهد مسئله تغییر کرده است؟",
"رأی ممکن است با تکرار پاسخ غلط عوض شود، اما Verifier درست هنوز ۱۳۵ را می‌پذیرد. تساوی یا نبود پاسخ، خروجی None می‌دهد. هیچ‌یک از این آزمایش‌ها وزن MiniGPT را تغییر نمی‌دهد.",
"اگر پاسخ‌ها و Verifier هر دو از یک فرض اشتباه استفاده کنند، چرا محاسبهٔ بیشتر لزوماً کمک نمی‌کند؟",
"reasoning.py انتخاب پاسخ را از تولید آن جدا کرده است. بعداً می‌توان Candidateها را از Backend واقعی گرفت؛ نتایج دستی این درس مدرکِ قابلیت زبانی آن Backend نیستند.",
code='''from mini_gpt.reasoning import majority_answer, select_verified, verify_study_answer
answers = [85,85,135]
vote = majority_answer(answers)
accepted = select_verified(answers, lambda a: verify_study_answer([25,20],3,a))
assert vote.answer == 85 and accepted == 135
assert majority_answer([85,135]).tied
assert select_verified([85],lambda a: a == 135) is None
print(vote, "independently verified:",accepted)
''', source="mini_gpt/reasoning.py",
review="اکنون پیشنهاد، اجرا و وارسی را جدا می‌شناسیم. در سامانهٔ بعدی باید روشن کنیم چه کسی این مرحله‌ها را تکرار می‌کند و چه وقت اجازهٔ ادامه ندارد."),
]
