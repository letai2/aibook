from .schema import Lesson

LESSONS = [
Lesson("47-loop",8,"آموزش روزمره","حلقهٔ آموزش را خودمان بنویسیم",
"حلقهٔ آموزش را می‌نویسیم و اندازه می‌گیریم که وزن‌ها در پایان آن چقدر تغییر کرده‌اند.",
"""<p>مدلی که ساخته‌ایم برای نشانه‌ها امتیاز تولید می‌کند، اما وزن‌هایش هنوز از داده چیزی یاد نگرفته‌اند. برای آموزش، باید خطای پیش‌بینی را حساب کنیم و از مشتق آن برای تغییر وزن‌ها استفاده کنیم. حلقهٔ ما این ترتیب را دارد: دریافت x/y، محاسبهٔ logits و loss، پاک‌کردن مشتق قبلی، پس‌انتشار و به‌روزرسانی. اجرای بی‌خطای این حلقه کافی نیست؛ یک وزن را پیش و پس از آموزش مقایسه می‌کنیم تا ببینیم واقعاً تغییر کرده است.</p>
<p>نرخ پایه را انتخاب کرده‌ایم، اما اندازه و سابقهٔ Gradient همهٔ وزن‌ها یکسان نیست. بعضی وزن‌ها در گام‌های متوالی Gradient بزرگ‌تری می‌گیرند. می‌توانیم این سابقه را نگه داریم و از آن برای تنظیم اندازهٔ update هر وزن استفاده کنیم. <dfn>AdamW (گونه‌ای از Adam با Weight decay جدا از محاسبهٔ Gradient)</dfn> روش Adam را با <dfn>Weight decay (کاهش تدریجی وزن)</dfn> ترکیب می‌کند. کاهش وزن، وزن‌ها را به سمت مقادیر کوچک‌تر سوق می‌دهد؛ کمک آن به تعمیم باید با ارزیابی سنجیده شود. <dfn>Regularization (محدودسازی برای کمک به تعمیم)</dfn> نام کلی روش‌هایی مانند همین کاهش وزن و dropout است؛ انتخاب مناسبشان نیاز به ارزیابی دارد.</p>
<p>در این پروژه برای سادگی، AdamW با نرخ 3e-4 و weight_decay=0.01 روی همهٔ پارامترها اعمال می‌شود. در بعضی مدل‌های بزرگ، bias و وزن‌های نرمال‌سازی را از کاهش وزن کنار می‌گذارند؛ در کد ما چنین گروه‌بندی‌ای وجود ندارد. هنگام خواندن کدهای دیگر به این تفاوت دقت کنید.</p>
<p><code>model.train()</code> حالت آموزش لایه‌ها را تنظیم می‌کند و <code>model.eval()</code> حالت ارزیابی را؛ هیچ‌کدام به‌تنهایی ثبت مشتق را روشن یا خاموش نمی‌کند. در مرجع، <code>detach().clone()</code> یک کپی مستقل از وزن آغازین، بیرون مسیر مشتق، نگه می‌دارد تا آخر کار تغییر را بسنجیم. پاک‌کردن مشتق با set_to_none=True به‌جای پرکردن صفر، آن را خالی می‌کند؛ backward گام بعد دوباره آن را می‌سازد.</p>
<p>Adam علاوه بر Gradient فعلی، میانگین متحرک Gradient و مربع آن را نگه می‌دارد؛ اندازهٔ مؤثر تغییر برای Parameterهای مختلف یکسان نیست. AdamW یک کشش جداگانه به صفر هم دارد: اگر فقط Weight decay را ببینیم، وزن ۲ با Learning Rate برابر ۰٫۱ و <code>weight_decay=0.01</code> در ۰٫۹۹۹ ضرب و به ۱٫۹۹۸ تبدیل می‌شود. این کل گام AdamW نیست؛ فقط سهم جداگانهٔ کاهش وزن است. به همین دلیل Resume به تاریخچهٔ Optimizer نیاز دارد.</p>
<h2>اولین اجرای واقعی، پیش از آزمایش‌های بعدی</h2>
<p>پس از [[windows.html|نصب و فعال‌کردن محیط]]، فرمان‌های زیر را از پوشه‌ای اجرا کنید که <code>mini_gpt</code> و <code>data</code> در آن هستند. هر فرمان را در یک خط وارد کنید. پوشهٔ خروجی تازه‌ای انتخاب کنید؛ برنامه برای Training تازه پوشهٔ غیرخالی را نمی‌پذیرد.</p>
<pre><code>python -m mini_gpt.train --text data/sample.txt --output runs/lesson47 --steps 6 --eval-every 2 --context-length 8 --embedding-dim 16 --num-heads 2 --num-layers 1 --batch-size 2 --device cpu --threads 1
python -m mini_gpt.evaluate --checkpoint runs/lesson47/last.pt --text data/sample.txt --device cpu --threads 1</code></pre>
<p>اینجا T=8، C=16، H=2 و B=2 است؛ <code>--steps 6</code> شش تغییر وزن و <code>--eval-every 2</code> ارزیابی در هر دو گام می‌خواهد. <code>last.pt</code> وضعیت آخر، <code>best.pt</code> بهترین ارزیابی دیده‌شده و <code>metrics.csv</code> گزارش است. هدف این شش گام سلامت مسیر است، نه کیفیت زبان. نرخ مرجعِ تک‌نمونه‌ای این درس ۰٫۰۰۳ است؛ مقدار پیش‌فرض اجرای واقعی ۰٫۰۰۰۳ است و این دو آزمایش عمداً یکسان نیستند.</p>""",
"پیش از مرجع، پنج خط مرکزی حلقه را بنویسید. ده گام روی یک نمونه اجرا کنید و تغییر یک وزن را قبل و بعد اندازه بگیرید.",
"ترتیب مرجع forward→loss→zero_grad→backward→step است. zero_grad پیش از forward هم مجاز است؛ مهم پاک‌شدن پیش از جمع مشتق گام تازه است.",
"اگر loss.backward را حذف کنیم ولی optimizer.step را نگه داریم، چه شواهدی انتظار داریم؟",
"v7 همین چرخه را با ارزیابی، گزارش و ذخیره گسترش می‌دهد؛ معماری v6 تغییر نمی‌کند.",
code="""import torch
from mini_gpt.stages.v6 import build
torch.manual_seed(2)
model = build(12)
optimizer = torch.optim.AdamW(model.parameters(),lr=0.003)
before = model.token_embedding.weight.detach().clone()
x,y = torch.tensor([[1,2,3,4]]),torch.tensor([[2,3,4,5]])
for step in range(10):
    model.train()
    _,loss = model(x,y)
    optimizer.zero_grad(set_to_none=True)
    loss.backward()
    optimizer.step()
print(loss.item())
change = (model.token_embedding.weight.detach() - before).abs().max().item()
assert change > 0
print("largest weight change:", change)
""",source="mini_gpt/train.py"),
Lesson("48-evaluate",8,"آموزش روزمره","ارزیابی دقیقاً چه چیزی را میانگین می‌گیرد؟",
"خطای همهٔ هدف‌ها را میانگین می‌گیریم؛ کوچک‌بودن دستهٔ آخر نباید سهم هر هدف را بیشتر کند.",
"""<p>فرض کنید یک دسته هشت پاسخ و دستهٔ دیگر دو پاسخ دارد. اگر میانگین خطای دو دسته را با هم جمع و حاصل را بر دو تقسیم کنیم، هر پاسخ دستهٔ دوم چهار برابر هر پاسخ دستهٔ اول اثر می‌گذارد. برای همین، هنگام ارزیابی باید تعداد پاسخ‌های هر دسته را هم در حساب بیاوریم. پیش از محاسبه، مطمئن می‌شویم خود ارزیابی وزن‌ها را تغییر نمی‌دهد: eval حذف تصادفی را خاموش می‌کند، no_grad گراف مشتق را نمی‌سازد و هیچ backward یا optimizer.step نداریم.</p>
<p>اگر دستهٔ اول ۸ هدف با میانگین ۱ و دستهٔ دوم ۲ هدف با میانگین ۳ داشته باشد، میانگین درست (8×1+2×3)/10=1.4 است، نه ۲. تابع evaluate پروژه loss هر دسته را در targets.numel ضرب، سپس بر مجموع هدف‌ها تقسیم می‌کند.</p>
<p>ارزیابی همهٔ پنجره‌های بخش کنارگذاشته‌شده را می‌خواند. به‌دلیل هم‌پوشانی، بعضی نشانه‌های متن در زمینه‌های مختلف چند بار هدف‌اند. این معیار برای مقایسهٔ تنظیمات ثابت مناسب است، اما آن را با ارزیابی تک‌گذری روی متن متفاوت یکسان ندانید.</p>
<p>برای تفسیر Loss، آن را به احتمال هدف‌ها وصل کنیم. اگر مدل به هر هدف احتمال 0.5 بدهد، معکوس این احتمال 2 است؛ برای توزیع یکنواخت روی V نشانه، این عدد V می‌شود. وقتی احتمال هدف‌ها متفاوت است، از معکوسِ میانگین هندسی آن‌ها استفاده می‌کنیم؛ میانگین هندسی از ریشهٔ حاصل‌ضرب به دست می‌آید. این معیار را <dfn>Perplexity (سرگشتگی؛ نمای میانگین خطای لگاریتمی)</dfn> می‌نامیم و در قرارداد ما exp(loss) است. برای نمونه، در توزیع یکنواخت loss=log(V) است و گرفتن exp همان V را برمی‌گرداند. این تعبیر به معنی شمردن گزینه‌های واقعی ذهن مدل نیست. روی داده و نشانه‌بند ثابت، کمتر بهتر است؛ مقایسهٔ مستقیم مقدار نویسه‌ای و زیرواژه‌ای منصفانه نیست.</p>
<p>هدفِ شکل (B,T) شامل B×T پیش‌بینی است. مثلاً (2,3) شش Target دارد. Loss بازگشتی مدل میانگین این شش مقدار است؛ ضرب در <code>targets.numel()</code> مجموع آن Batch را برمی‌گرداند. در پایان بر تعداد کل Targetها تقسیم می‌کنیم، نه تعداد Batchها.</p>""",
"مثال دو دسته را دستی حساب کنید. سپس ارزیابی یک مدل را با batch_size=1 و batch_size=3 انجام دهید؛ نتیجه باید تقریباً برابر باشد.",
"با dropout خاموش، تفاوت صرفاً گردکردن است. اگر mean دسته‌ها بدون وزن گرفته شود، دستهٔ آخر معیار را جابه‌جا می‌کند.",
"چرا perplexity پایین روی یک متن تکراری کوچک به معنی توان پاسخ‌گویی عمومی نیست؟",
"فرمان evaluate.py از فایل ذخیرهٔ مدل و همان دادهٔ ثبت‌شده، معیار کامل و نرخ ناشناخته را گزارش می‌کند.",
code="""import torch
from torch.utils.data import DataLoader
from mini_gpt.dataset import NextTokenDataset
from mini_gpt.stages.v6 import build
from mini_gpt.evaluate import evaluate
dataset = NextTokenDataset([1,2,3,4,5,6,7,8],3)
model = build(12)
a = evaluate(model,DataLoader(dataset,batch_size=1),torch.device("cpu"))
b = evaluate(model,DataLoader(dataset,batch_size=3),torch.device("cpu"))
assert abs(a-b) < 1e-6
print(a,b)
""",source="mini_gpt/evaluate.py"),
Lesson("49-rate",8,"آموزش روزمره","وقتی گام‌ها خیلی بزرگ یا کوچک‌اند",
"نوسان یک عدد را با شکست آموزش اشتباه نمی‌گیریم؛ نرخ، اندازهٔ مشتق و روند ارزیابی را کنار هم می‌خوانیم.",
"""<p>اگر خطای یک دسته از دستهٔ قبلی بیشتر شد، هنوز نمی‌توان گفت آموزش خراب شده است؛ نمونه‌های دو دسته ممکن است متفاوت باشند. با نرخ یادگیری خیلی کوچک، شاید در تعداد گامی که در اختیار دارید پیشرفت محسوسی نبینید. نرخ خیلی بزرگ هم ممکن است وزن‌ها را از مقادیر مناسب دور کند. برای تشخیص، روند چند ارزیابی روی دادهٔ ثابت را بررسی کنید، نه فقط خطای یک دسته را.</p>
<p>گاهی Gradient یک Batch بسیار بزرگ‌تر از دسته‌های اطرافش است. می‌خواهیم پیش از تحویل آن به Optimizer، اندازه‌اش را محدود کنیم، بی‌آنکه جهت بردار را عوض کنیم. <dfn>Gradient clipping (محدودکردن norm گرادیان)</dfn> در روش این پروژه، وقتی norm از حد بیشتر شود، همهٔ مؤلفه‌های گرادیان را با یک ضریب کمتر از یک مقیاس می‌کند. در حد ۱ و norm=5، ضریب حدود 1/5 است. جهت کلی حفظ می‌شود؛ تک‌تک مؤلفه‌ها جدا به [-1,1] بریده نمی‌شوند.</p>
<p><code>clip_grad_norm_</code> پس از backward و پیش از step می‌آید. پسوند <code>_</code> در بسیاری از نام‌های PyTorch یعنی مقدار درجا تغییر می‌کند. مقدار بازگشتی این تابع اندازهٔ گرادیان پیش از محدودسازی است؛ ستون gradient_norm گزارش پروژه همین را ثبت می‌کند. اگر هر بار عدد بسیار بزرگی می‌بینید، صرف وجود برش نباید شما را از بررسی علت منصرف کند.</p>
<p>محدودکردن مشتق، دادهٔ غلط یا هدفی را که یک خانه جلو نرفته درست نمی‌کند. اگر NaN دیدید، نرخ، finite بودن loss، تمام -inf شدن یک سطر پوشش و ورودی‌های نامعتبر را بررسی کنید. هر بار یک تغییر انجام دهید و تنظیم قبلی را در دفتر نگه دارید.</p>
<p>Gradient برابر <code>[3,4]</code> دارای Norm برابر ۵ است. سقف ۱ آن را تقریباً به <code>[0.6,0.8]</code> می‌برد، نه <code>[1,1]</code>. نسبت مؤلفه‌ها حفظ می‌شود. این سقف بر Gradient ورودی Optimizer است، نه تضمین اندازهٔ تغییر نهایی AdamW.</p>
<p>فرمان Training در [[47-loop|درس حلقهٔ آموزش]] را سه بار با پوشه‌های تازهٔ <code>runs/lr-small</code>، <code>runs/lr-base</code> و <code>runs/lr-large</code> اجرا کنید. برای هر سه <code>--steps 30 --eval-every 10 --seed 1337</code> بگذارید و فقط <code>--learning-rate</code> را به‌ترتیب <code>3e-5</code>، <code>3e-4</code> و <code>3e-3</code> تغییر دهید. ستون <code>validation_loss</code> در سه گزارش، مقایسهٔ خواسته‌شده است؛ خروجی بهترین اجرا را جای هر سه عدد ننویسید.</p>""",
"سه اجرای کوتاه با نرخ‌های 3e-5،3e-4،3e-3 و داده/بذر ثابت بسازید. پیش از اجرا، روندی را که انتظار دارید بنویسید؛ سپس زمان و سه مقدار ارزیابی را ثبت کنید.",
"نتیجهٔ دقیق وابسته به داده است؛ معیار موفقیت مشاهدهٔ قابل مقایسه و تغییر فقط یک عامل است. برای lr بزرگ فوراً حکم به خرابی ندهید؛ رفتار واقعی منحنی را بخوانید.",
"چرا محدودکردن norm بعد از optimizer.step برای همان update دیر است؟",
"train.py خطای غیرمتناهی را متوقف و norm پیش از برش را ثبت می‌کند تا مشکل خاموش ادامه پیدا نکند.",
source="mini_gpt/train.py"),
Lesson("49b-schedule",8,"آموزش روزمره","آیا اندازهٔ گام باید تا آخر ثابت بماند؟",
"یک برنامهٔ روشن برای نرخ یادگیری می‌سازیم و اثر ادامهٔ آموزش بر زمان‌بندی آن را بررسی می‌کنیم.",
"""<p>در درس قبل یک نرخ ثابت انتخاب کردیم. اما ممکن است بخواهیم آموزش را با گام‌های کوچک شروع کنیم، مدتی نرخ بالاتری داشته باشیم و در پایان دوباره تغییرهای کوچک‌تری بدهیم. به‌جای عوض‌کردن دستی تنظیمات وسط اجرا، یک قاعده بر اساس شمارهٔ گام می‌نویسیم. <dfn>Learning-rate schedule (برنامهٔ تغییر نرخ یادگیری)</dfn> همین اندازهٔ پایهٔ هر گام را مشخص می‌کند. خود AdamW همچنان از مشتق‌ها و تاریخچه‌اش جهت و مقیاس سازگار هر پارامتر را می‌سازد؛ برنامهٔ نرخ جای آن را نمی‌گیرد.</p>
<p><dfn>Warmup (افزایش تدریجی آغازین نرخ)</dfn> به‌جای شروع ناگهانی با نرخ اوج، آن را طی چند گام بالا می‌برد. بعد می‌توان نرخ را آرام پایین آورد تا اصلاح‌های پایانی کوچک‌تر شوند. این انتخاب همیشه بهتر نیست؛ روی آموزش بسیار کوتاه ممکن است بخش بزرگی از بودجه را صرف نرخ‌های پایین کنیم. نخست اجرای ثابت را مرجع نگه دارید.</p>
<p>در برنامهٔ پروژه، گام‌ها از ۱ شروع می‌شوند. اگر اوج η، تعداد گام گرم‌کردن W و پایان کاهش D باشد، برای s≤W نرخ η×s/W است. پس از آن از قوس نیمهٔ کسینوس استفاده می‌کنیم؛ لازم نیست مثلثات حفظ کنید: این قوس از ۱ نرم به صفر می‌رسد. r نسبت نرخ پایانی به اوج است.</p>
<div class="math">u = min(1, (s−W)/(D−W))<br>lr(s) = η [ r + (1−r)(1+cos(πu))/2 ]</div>
<p>D باید از W بزرگ‌تر باشد. بعد از D، نرخ روی rη می‌ماند. با η=0.001، W=2، D=6 و r=0.1، دو گام اول 0.0005 و 0.001 هستند و گام ششم به 0.0001 می‌رسد. در مثال Python، <code>math.cos</code> کسینوس و <code>math.pi</code> عدد π است؛ خروجی هر گام را چاپ می‌کنیم تا قرارداد پنهان نماند.</p>
<p>فرمان آموزش با <code>--schedule cosine --warmup-steps 10 --decay-steps 100 --min-lr-ratio 0.1</code> این برنامه را فعال می‌کند. حالت پیش‌فرض <code>constant</code>، یعنی نرخ ثابت، است. ستون learning_rate گزارش نرخ مصرف‌شده در همان گام را ثبت می‌کند. در ادامهٔ آموزش، تنظیم برنامه و شمارهٔ گام از فایل ذخیره برمی‌گردند؛ گزینه‌ها را دوباره ندهید.</p>
<p>نکتهٔ مهم: D مستقل از --steps است. می‌توانید از ابتدا پایان کاهش را گام ۱۰۰ انتخاب کنید، اجرا را در گام ۴۰ متوقف و بعد تا ۱۰۰ ادامه دهید. اگر هنگام ادامه منحنی را با طول تازه از نو بسازیم، آزمایش دیگری ساخته‌ایم، نه ادامهٔ همان آزمایش. در درس ذخیره خواهیم دید چرا «کدام گام؟» بخشی از وضعیت مدل است.</p>
<p>وقتی W=0 است، شاخهٔ Warmup اجرا نمی‌شود و تقسیم بر صفر نداریم؛ کاهش کسینوسی از گام اول با u=s/D سنجیده می‌شود. در حالت <code>constant</code> نیز Warmup اختیاری مجاز است؛ پس از آن نرخ روی η می‌ماند. «ثابت» به بخش پس از Warmup اشاره می‌کند.</p>""",
"پیش از اجرا نرخ گام‌های ۱،۲،۴،۶ و۸ مثال را حدس بزنید. سپس دو آموزش هم‌بودجهٔ کوتاه با نرخ ثابت و برنامهٔ کسینوسی بسازید؛ هم منحنی نرخ و هم خطای ارزیابی را نگه دارید.",
"نرخ‌ها به‌ترتیب 0.0005،0.001،0.00055،0.0001 و0.0001 هستند. برتری کیفیت از فرمول نتیجه نمی‌شود؛ آن را باید با داده، بذر و بودجهٔ ثابت آزمود. برای مقایسهٔ ادامه با اجرای پیوسته، D در هر دو اجرا یکسان است.",
"چرا تغییر --steps در ادامه نباید خودکار گام‌های قبلیِ منحنی نرخ را دوباره تعریف کند؟",
"برنامهٔ نرخ بخشی از گردش آموزش v7 است؛ فرمول forward را عوض نمی‌کند، اما مسیر یادگیری وزن‌ها و در نتیجه امتیازهای مدل آموزش‌دیده را تغییر می‌دهد.",
code="""import math
def rate(step, peak=0.001, warmup=2, end=6, minimum=0.1):
    if step <= warmup:
        return peak*step/warmup
    progress = min(1.0, (step-warmup)/(end-warmup))
    return peak*(minimum+(1-minimum)*(1+math.cos(math.pi*progress))/2)
for step in range(1,9):
    print(step, rate(step))
assert math.isclose(rate(1),0.0005)
assert math.isclose(rate(4),0.00055)
assert math.isclose(rate(6),rate(8))
""",source="mini_gpt/train.py"),
Lesson("50-checkpoint",8,"ذخیره و ادامه","چه چیزی باید همراه وزن‌ها ذخیره شود؟",
"وزن‌ها را همراه اطلاعاتی ذخیره می‌کنیم که برای بازسازی مدل و ادامهٔ آموزش لازم‌اند.",
"""<p>فرض کنید شناسهٔ ۷ در آموزش «م» بوده و هنگام بارگذاری «ر» شده است. اندازهٔ جدول درست است و برنامه هم اجرا می‌شود؛ فقط معنی تمام محاسبه‌ها عوض شده. <dfn>Checkpoint (عکس قابل بازیابی از وضعیت آموزش)</dfn> برای جلوگیری از چنین ناسازگاری‌ای فقط وزن‌ها را نگه نمی‌دارد: تنظیم معماری و نگاشت واژگان هم لازم‌اند. برای ادامهٔ همان آموزش، وضعیت بهینه‌ساز، شمارهٔ گام، برنامهٔ نرخ و مولدهای تصادفی را نیز می‌خواهیم.</p>
<p><code>state_dict</code> مدل نگاشتی از نام پارامترها و Bufferهای پایدار به مقدار آن‌هاست؛ فقط فهرست نام‌ها نیست. state_dict optimizer میانگین‌های روش AdamW و شمارنده‌هایش را دارد. در قالب نسخهٔ دوم، tokenizer هم داخل فایل ذخیره می‌شود. <dfn>Metadata (اطلاعات توصیفی)</dfn> در اینجا شامل تنظیمات آموزش و اثر انگشت پیکره است؛ این اطلاعات اجرا را توصیف می‌کنند و وزن قابل آموزش نیستند.</p>
<p><code>torch.save</code> ساختار داده را ذخیره و <code>torch.load(...,map_location='cpu',weights_only=True)</code> آن را با محدودیت تنسورها و نوع‌های پایه بارگذاری می‌کند. map_location محل اولیهٔ بارگذاری را تعیین می‌کند. این محدودیت تضمین امن‌بودن هر فایل ناشناس نیست؛ فایل نامطمئن را اجرا نکنید. پروژه نیازی به بازسازی اشیای دلخواه Python از فایل ندارد: مدل از تنظیمات دوباره ساخته و state_dict در آن قرار می‌گیرد.</p>
<p>برای کاهش خطر نیمه‌نوشته‌ماندن، ابتدا فایل .tmp نوشته و پس از پایان جایگزین می‌شود. last.pt وضعیت مدل در آخرین ارزیابی و best.pt وضعیت مدل در بهترین ارزیابی همان اجرا را نگه می‌دارد. کم‌بودن خطای best فقط میان دفعات ارزیابی سنجیده می‌شود؛ به معنی بهترین لحظهٔ مشاهده‌نشده نیست.</p>
<p>مرجع زیر یک مدل کوچک با Vocabulary مشخص می‌سازد، یک گام آموزش می‌دهد، در پوشهٔ موقت ذخیره و بازیابی می‌کند و هم Logits و هم Vocabulary را می‌سنجد. پوشهٔ موقت در پایان پاک می‌شود؛ فایل‌های اجرای قبلی شما دست‌نخورده می‌مانند. این مثال قالب ذخیره را نشان می‌دهد؛ metadata کاملِ Resume را فرمان Training می‌سازد.</p>""",
"پس از یک اجرای کوتاه، فایل last.pt را بارگذاری و logits ورودی ثابت را با مدل قبل از ذخیره مقایسه کنید. ترتیب واژگان را نیز بسنجید.",
"با eval و ورودی یکسان، وزن‌های بازیابی‌شده باید خروجی یکسان بدهند. آزمون خودکار پروژه همین برابری را بررسی می‌کند.",
"چرا فایل tokenizer.json کنار checkpoint برای خواندن مفید است ولی مرجع بارگذاری مدل نیست؟",
"checkpoint.py نگاشت داخلی را مرجع می‌گیرد و قالب قدیمی فاقد آن را با پیام روشن رد می‌کند.",
code="""import tempfile
from pathlib import Path
import torch
from mini_gpt.config import ModelConfig
from mini_gpt.model import MiniGPT
from mini_gpt.tokenizer import CharacterTokenizer
from mini_gpt.checkpoint import save_checkpoint, load_checkpoint

torch.manual_seed(7)
tokenizer = CharacterTokenizer.from_text("abc")
model = MiniGPT(ModelConfig(tokenizer.vocab_size, 4, 8, 2, 1, 0.))
optimizer = torch.optim.AdamW(model.parameters(), lr=0.001)
x, y = torch.tensor([[1, 2, 3]]), torch.tensor([[2, 3, 1]])
optimizer.zero_grad(set_to_none=True)
loss = model(x, y)[1]
loss.backward()
optimizer.step()
model.eval()
with torch.no_grad():
    expected = model(x)[0].clone()
with tempfile.TemporaryDirectory() as directory:
    path = Path(directory)/"example.pt"
    save_checkpoint(path, model, tokenizer, optimizer, 1, {},
                    torch.Generator().manual_seed(5), loss.item())
    restored, restored_tokenizer, payload = load_checkpoint(path)
    restored.eval()
    with torch.no_grad():
        assert torch.equal(expected, restored(x)[0])
    assert tokenizer.id_to_token == restored_tokenizer.id_to_token
    assert payload["step"] == 1 and payload["optimizer"]["state"]
print("model restored; vocabulary and optimizer state retained in checkpoint")
""", source="mini_gpt/checkpoint.py"),
Lesson("51-resume",8,"ذخیره و ادامه","چطور ادامهٔ اجرا را با اجرای پیوسته مقایسه کنیم؟",
"توقف برنامه نباید تاریخچهٔ یادگیری را پاک کند؛ ادامه را با اجرای بدون توقف مقایسه می‌کنیم.",
"""<p>دو مدل با وزن یکسان الزاماً گام بعدی یکسانی برنمی‌دارند. اگر یکی تاریخچهٔ AdamW را فراموش کرده یا دستهٔ دیگری انتخاب کند، مسیرشان جدا می‌شود. <dfn>Resume (ادامهٔ آموزش از وضعیت ذخیره‌شده)</dfn> باید این وضعیت‌ها را هم برگرداند. بارگذاری وزن و شروع بهینه‌ساز تازه می‌تواند برای آزمایشی دیگر معتبر باشد، اما ادامهٔ دقیق همان اجرا نیست.</p>
<p>حلقهٔ آموزش پنجره‌ها را به‌صورت تصادفی و با جایگذاری انتخاب می‌کند؛ یک پنجره ممکن است چند بار دیده شود. وضعیت مولد batch در checkpoint ذخیره می‌شود. برای ادامهٔ انتخاب‌ها کافی است همین وضعیت را برگردانیم؛ لازم نیست به خاطر بسپاریم پیمایش داده در کدام پنجره متوقف شده بود. ارزیابی از DataLoader با مولدی جدا استفاده می‌کند تا ساخت پیمایشگر ارزیابی عددهای تصادفی آموزش را مصرف نکند.</p>
<p>فرمان ادامه، --steps را تعداد کل گام هدف می‌فهمد: اگر فایل در گام ۱۰۰ ذخیره شده، --steps 150 یعنی ۵۰ گام دیگر. معماری، نرخ، برنامهٔ نرخ و اندازهٔ دسته از فایل می‌آیند؛ بازتعریف هم‌زمانشان رد می‌شود. پایان کاهش نرخ همان D ذخیره‌شده می‌ماند. وضعیت تصادفی CPU، GPUهای موجود و انتخاب پنجره‌ها نیز برمی‌گردد و تغییر پیکره با اثر انگشت تشخیص داده می‌شود.</p>
<p><dfn>Reproducibility (بازتولیدپذیری)</dfn> به نسخهٔ کتابخانه، دستگاه و بعضی روال‌های محاسباتی سخت‌افزار وابسته است. در آزمون CPU همین محیط، اجرای شش گام با اجرای چهار گام+ادامه تا شش وزن‌های کاملاً یکسان داد. برای GPU یا نسخهٔ دیگر وعدهٔ برابری بیت‌به‌بیت نداریم؛ محیط و تنظیمات را ثبت کنید.</p><p>برای محافظت از سابقه، ادامه فقط از last.pt همان پوشهٔ خروجی پذیرفته می‌شود؛ گام آخر metrics.csv باید با آن برابر باشد. اگر اجرا را به رایانهٔ دیگر می‌برید، پوشهٔ کامل شامل best.pt و گزارش را انتقال دهید.</p>
<p>در محیط فعال و ریشهٔ پروژه، این سه فرمان را یک‌خطی اجرا کنید. دو پوشه باید تازه باشند. در فرمان سوم معماری یا نرخ را دوباره نمی‌دهیم: از Checkpoint برمی‌گردند. <code>--dropout 0.1</code> عمداً تصادف Training را فعال نگه می‌دارد تا بازیابی RNG هم آزموده شود.</p>
<pre><code>python -m mini_gpt.train --output runs/resume-full --steps 6 --eval-every 2 --context-length 8 --embedding-dim 16 --num-heads 2 --num-layers 1 --batch-size 2 --dropout 0.1 --device cpu --threads 1 --seed 17
python -m mini_gpt.train --output runs/resume-split --steps 4 --eval-every 2 --context-length 8 --embedding-dim 16 --num-heads 2 --num-layers 1 --batch-size 2 --dropout 0.1 --device cpu --threads 1 --seed 17
python -m mini_gpt.train --output runs/resume-split --resume runs/resume-split/last.pt --steps 6 --eval-every 2 --device cpu --threads 1</code></pre>
<p>سپس کد پاسخ را در ریشهٔ پروژه اجرا کنید. این مقایسه روی همهٔ Parameterها و Bufferهای ذخیره‌شده است، نه فقط Loss گرد‌شده. یکسان‌نبودن در محیط دیگر را ابتدا با نسخه، دستگاه و تعداد Thread بررسی کنید؛ ادعای عمومی برابری بیت‌به‌بیت نداریم.</p>""",
"دو اجرا بسازید: یکی ۶ گام پیوسته و دیگری ۴ گام و resume تا ۶. state_dictها را با torch.equal مقایسه کنید.",
"آزمون tests/test_training.py همین سناریو را با dropout فعال اجرا می‌کند. مقایسه فقط loss گرد‌شده در صفحه کافی نیست؛ همهٔ وزن‌ها را بسنجید.",
"چرا ساخت مدل تازه پیش از بازیابی وضعیت تصادفی می‌تواند چند عدد تصادفی مصرف کند؟",
"load_checkpoint مدل را می‌سازد؛ سپس train وضعیت مولد عدد تصادفی (RNG) را برمی‌گرداند. ترتیب این دو عمدی است.",
code="""import torch

full = torch.load("runs/resume-full/last.pt", map_location="cpu", weights_only=True)
split = torch.load("runs/resume-split/last.pt", map_location="cpu", weights_only=True)
assert full["step"] == split["step"] == 6
assert full["tokens"] == split["tokens"]
assert full["model"].keys() == split["model"].keys()
different = [name for name in full["model"]
             if not torch.equal(full["model"][name], split["model"][name])]
assert not different, different
print("all stored model tensors are identical")
""", code_kind="پس از اجرای سه فرمان این درس؛ از ریشهٔ پروژه", source="mini_gpt/train.py"),
Lesson("52-first-run",8,"خواندن نتیجه","یک اجرای کامل و قابل ثبت روی CPU",
"یک اجرای کوچک می‌سازیم که فردا بتوانیم فرمان، وزن‌ها و نتیجه‌اش را دوباره بررسی کنیم.",
"""<p>حلقهٔ آموزش، ارزیابی و ذخیره را جداگانه بررسی کرده‌ایم. حالا آن‌ها را در یک اجرای کامل کنار هم می‌گذاریم و نتیجه را ثبت می‌کنیم. از ریشهٔ پروژه، <code>python -m mini_gpt.smoke_test</code> را اجرا کنید. <dfn>Smoke test (آزمون کوتاه سلامت مسیر)</dfn> بررسی می‌کند قطعه‌های اصلی با هم کار می‌کنند. سپس پوشهٔ خروجی تازه‌ای انتخاب کنید؛ train برای مخلوط‌نشدن سابقه‌ها، آموزش تازه روی پوشهٔ غیرخالی را رد می‌کند.</p>
<pre><code>python -m mini_gpt.train --text data/sample.txt --output runs/first --steps 100 --context-length 32 --embedding-dim 32 --num-heads 4 --num-layers 2 --batch-size 8 --device cpu
python -m mini_gpt.evaluate --checkpoint runs/first/best.pt --text data/sample.txt --device cpu</code></pre>
<p>این تنظیم C=32 و H=4 دارد، پس D=8. کوچک‌کردن T و C هزینه را پایین می‌آورد. مقدار --threads تعداد رشته‌های محاسبهٔ CPU را تنظیم می‌کند؛ برای مدل کوچک افزایش بی‌حد آن ممکن است کندتر کند. زمان دقیق به دستگاه وابسته است؛ نخست تعداد گام کمی بگیرید.</p>
<p>metrics.csv فایل جدولی متنی با جداکنندهٔ ویرگول است. ستون train_batch_loss فقط خطای آخرین دستهٔ تصادفی را دارد، اما validation_loss میانگین همهٔ پنجره‌های کنارگذاشته‌شده است؛ هم نمونه‌ها و هم حالت dropout متفاوت‌اند. برای مقایسهٔ دقیق‌تر، روی بخش آموزش هم با eval میانگین بگیرید. learning_rate نرخ همان گام را ثبت می‌کند.</p>
<p>برای دیدن محاسبات درون مدل آموزش‌دیده، فرمان زیر را اجرا کنید. این فرمان نمایش‌ها و توجه را از همان فایل ذخیره استخراج می‌کند. <code>--layer 0 --head 0</code> بلوک و سر نخست را برای نمایش انتخاب می‌کند؛ شماره‌ها از صفرند. <code>--max-tokens 16</code> طول بررسی و <code>--generate-tokens 4</code> ادامهٔ کوتاه قابل ردیابی را محدود می‌کند. فایل خروجی JSON، یعنی متن ساخت‌یافتهٔ کلید/مقدار، را در [[lab.html|آزمایشگاه مدل]] وارد کنید.</p>
<pre><code>python -m mini_gpt.inspect --checkpoint runs/first/best.pt --prompt "مدل " --output runs/first/inspection.json --layer 0 --head 0 --max-tokens 16 --generate-tokens 4 --metrics runs/first/metrics.csv</code></pre>
<p>اگر نویسه‌ای از متن آغازین در آموزش نبوده، شناسهٔ ناشناخته را در گزارش ببینید. شکل‌های زیبا در صفحه به‌تنهایی کافی نیستند؛ نام فایل مدل، شناسه‌های واقعی و تنظیمات باید با اجرای شما یکی باشند. نتیجه و زمان را در [[journal.html|دفتر آزمایش]] ثبت و نسخهٔ خروجی دفتر را نگه دارید.</p>
<p>بازرسی با <code>--max-tokens 16</code> عمداً زمینه را از ظرفیت ۳۲تایی مدل کوتاه‌تر می‌کند. در نتیجه حتی با Seed یکسان، ادامهٔ آن الزاماً با فرمان معمول Generate برابر نیست. گزارش طول مؤثر زمینه را نگه می‌دارد؛ هنگام مقایسه آن را نیز ثابت کنید.</p><p><code>train_batch_loss</code> پیش از به‌روزرسانی همان گام محاسبه شده، اما <code>validation_loss</code> پس از آن اندازه‌گیری می‌شود؛ برای مقایسه، زمان سنجش و نوع داده را هم ثبت کنید.</p>""",
"قبل از اجرا، شکل logits را با توجه به واژگان واقعی حدس بزنید. بعد از اجرا فرمان، زمان، تعداد پارامتر و سه عدد گزارش را در learning-log.md بنویسید.",
"V از واژگان دادهٔ آموزش به دست می‌آید، نه از عدد دلخواه در خط فرمان. خروجی هر گام شکل (8,32,V) دارد. متن نمونه عمداً کوتاه است؛ کاهش loss شاهدی محدود از یادگیری الگوهای همین پیکره است.",
"اگر آموزش روی یک دستهٔ ثابت درست کار می‌کند ولی معیار متن کنارگذاشته‌شده بهتر نمی‌شود، چه بررسی‌هایی پیش از تغییر معماری انجام می‌دهید؟",
"این اجرای واقعی v7 است؛ داده، آموزش، ارزیابی و ذخیره متصل شدند. در بخش بعد از این وزن‌ها متن تولید می‌کنیم."),
Lesson("53-curves",8,"خواندن نتیجه","افت خطا چه چیزی را ثابت می‌کند؟",
"منحنی عددها را به ادعایی تبدیل می‌کنیم که شواهد واقعاً پشتیبانش باشند.",
"""<p>در یک اجرای فرضی، Loss آموزش از ۲ به ۱ رسیده، اما Loss دادهٔ کنارگذاشته‌شده از ۱٫۸ به ۲٫۱ رفته است. آیا مدل بهتر شده؟ روی دادهٔ آموزش بله، اما این دو عدد دربارهٔ یک چیز حرف نمی‌زنند. پیش از انتخاب Checkpoint باید مشخص کنیم کدام داده و کدام توانایی را می‌سنجیم.</p><p>کاهش میانگین Loss یعنی میانگینِ منفی لگاریتم احتمال هدف‌ها در همان داده کمتر شده است؛ لازم نیست احتمال تک‌تک هدف‌ها بیشتر شده باشد. اگر ارزیابیِ دادهٔ کنارگذاشته‌شده هم بهتر شود، شاهدی از تعمیم در همان تقسیم داریم. اگر خطای آموزش پایین برود ولی ارزیابی پس از رسیدن به یک کف بالا برود، بیش‌برازش محتمل است. ابتدا به تعریف ستون‌های گزارش دقت کنید؛ خطای یک دسته با میانگین کل داده یکی نیست.</p>
<p><dfn>Underfitting (کم‌برازش)</dfn> یعنی مدل هنوز رابطه‌های داده را خوب نگرفته و خطای هر دو بخش بالا مانده است. علت می‌تواند ظرفیت کم، آموزش ناکافی، نرخ نامناسب یا خطای برنامه باشد؛ نام‌گذاری این وضعیت جای تشخیص علت نیست.</p>
<p><dfn>Early stopping (توقف زودهنگام بر اساس ارزیابی)</dfn> یعنی پس از چند ارزیابی بدون بهبود متوقف شویم. این پروژه best.pt را نگه می‌دارد ولی توقف خودکار ندارد؛ افزودن آن تمرین مناسبی است. انتخاب بر اساس validation خود آن بخش را وارد فرایند تصمیم می‌کند، پس برای گزارش نهایی مستقل test جدا لازم است.</p>
<p>چرا خطای کمتر الزاماً متن چشم‌نوازتری نمی‌دهد؟ هنگام ارزیابی، پیشوند واقعی متن را به مدل می‌دهیم؛ این شیوه را <dfn>Teacher forcing (دادن پیشوند مرجع به‌جای خروجی قبلی مدل)</dfn> می‌نامند. اما در تولید، یک انتخاب نامناسب خودش وارد زمینهٔ گام بعد می‌شود. همچنین میانگین بهتر می‌تواند حاصل پیش‌بینی بهتر فاصله‌ها و ترکیب‌های پرتکرار باشد، نه بهترشدن تمام جمله‌ها. روش نمونه‌گیری نیز جدا روی متن اثر می‌گذارد.</p>
<p>با دادهٔ کوچک، بذر متفاوت هم می‌تواند نتیجه را عوض کند. در [[journal.html|دفتر آزمایش]] داده، تنظیمات، چند بذر، زمان و خطای ارزیابی را ثبت کنید و کنارشان خروجی‌های تولیدی با متن آغازین ثابت بگذارید. عدد و متن دو شاهد مکمل‌اند؛ هیچ‌کدام به‌تنهایی همهٔ کیفیت را نمی‌سنجند.</p>
<p>مثلاً کمترین Loss سه Seed اگر ۲٫۱، ۲٫۳ و ۲٫۲ باشد، میانگین ۲٫۲ و دامنهٔ مشاهده‌شده [2.1,2.3] است؛ سه اجرا به‌تنهایی فاصلهٔ اطمینان آماری معتبر نمی‌سازند.</p>
<p>پیش از انتخاب تنظیمات، سندهای مستقلی برای Test کنار بگذارید و به‌شکل UTF-8 در فایلی مانند <code>data/test.txt</code> ذخیره کنید. این فایل عمداً همراه نمونهٔ آموزشی نیست؛ متفاوت‌بودن نام فایل استقلال محتوا را ثابت نمی‌کند. Vocabulary تازه نسازید. فرمان زیر تمام فایل Test را با Tokenizer داخل Checkpoint ارزیابی می‌کند و نرخ ناشناخته را جدا گزارش می‌دهد.</p>
<pre><code>python -m mini_gpt.evaluate --checkpoint runs/first/best.pt --test-text data/test.txt --device cpu</code></pre>
<p>حالت <code>--text</code> همچنان فقط بخش Validation پیکرهٔ اصلی را می‌سنجد. <code>--test-text</code> برای دادهٔ مستقل است؛ پس از انتخاب تنظیمات استفاده کنید. اگر با نتیجهٔ Test دوباره تنظیمات را انتخاب کردید، آن داده دیگر آزمون نهایی مستقل نیست. فایل باید بیش از context_length نویسه داشته باشد.</p>""",
"سه اجرا با بذر متفاوت و تنظیمات یکسان انجام دهید. کمترین validation هرکدام را کنار هم بگذارید و نتیجه را با دامنهٔ مشاهده‌شدهٔ سه اجرا بنویسید.",
"اگر مقادیر نزدیک‌اند، می‌توانید روند نسبتاً پایدار همان آزمایش را گزارش کنید. اگر پراکندگی زیاد است، یک run برای ادعای برتری یک تنظیم کافی نیست؛ داده و بودجه را ثابت نگه دارید.",
"چرا انتخاب بهترین از صدها اجرای validation می‌تواند خود به بیش‌برازش روی ارزیابی منجر شود؟",
"دفتر آزمایش ابزار اصلی این مرحله است؛ مدل‌سازی فقط نوشتن کلاس نیست."),
]
