"""From a trained language model to explicitly assembled, sourced input."""
from .schema import Lesson


LESSONS = [
Lesson("68-context", 11, "ساخت ورودی", "مدل ثابت است؛ چه اطلاعاتی به درخواست می‌دهیم؟",
"ورودی یک دستیار مطالعه را از پرسش و شاهد بسازیم و برای پاسخ هم جا نگه داریم.",
"""<p>از Mini-GPT یک ادامه‌دهنده ساخته‌ایم. حالا می‌پرسیم «تمرین این درس را کجا اجرا کنم؟» پاسخ در راهنمای پروژه هست، اما لزوماً در دادهٔ آموزش مدل نبوده است. تغییر Temperature این اطلاعات را به وزن‌ها اضافه نمی‌کند. یک راه دیگر داریم: همان بند مرتبطِ راهنما را همراه پرسش به مدل بدهیم. این تصمیم را برنامهٔ پیرامون مدل می‌گیرد، نه یک Layer تازه در Transformer.</p>
<p>از اینجا یک دستیار کوچک برای مطالعهٔ همین دوره می‌سازیم. ابتدا پرسش و یک سند کوتاه را دستی انتخاب می‌کنیم؛ بعد انتخاب سند، نگهداری گفت‌وگو و ابزار را اضافه خواهیم کرد. <dfn>LLM system (سامانه‌ای پیرامون مدل زبانی)</dfn> در این کتاب یعنی مدل به‌علاوهٔ کدی که ورودی را آماده می‌کند، خروجی را بررسی می‌کند و وضعیت لازم را نگه می‌دارد. این نام، کیفیت یا قابلیت اعتماد آن مجموعه را تضمین نمی‌کند. Mini-GPT کوچک ما هنوز برای پیروی قابل اتکا از دستور آموزش ندیده است؛ آزمایش نخست دربارهٔ ساخت ورودی است، نه اثبات کیفیت پاسخ.</p>
<p><dfn>Context management (مدیریت اطلاعاتِ داخل ورودی)</dfn> یعنی انتخاب کنیم چه اطلاعاتی، با چه ترتیب و چه حجمی به اجرای فعلی مدل برسد. Context همان چیزی نیست که برنامه جایی ذخیره کرده است. سند روی دیسک تا وقتی به ورودی تبدیل نشده، در دسترس Forward pass نیست. در [[03-counts|نسخهٔ شمارشی]] فقط آخرین Token بر تصمیم اثر داشت؛ اینجا محدودیت تعداد Tokenهای پنجره را داریم، ولی انتخاب محتوای آن پنجره هم مسئله‌ای جداست.</p>
<table><thead><tr><th>چیزِ نگهداری‌شده</th><th>نقش آن در این مسیر</th></tr></thead><tbody><tr><td>Parameterهای مدل</td><td>عددهای آموخته‌شده؛ در اجرای درخواست ثابت‌اند.</td></tr><tr><td>Context درخواست</td><td>Tokenهایی که همین اجرای مدل می‌بیند؛ با درخواست بعدی می‌توانند عوض شوند.</td></tr><tr><td>Conversation history</td><td>پیام‌هایی که برنامه نگه داشته؛ فقط بخشِ افزوده‌شده به Context دیده می‌شود.</td></tr><tr><td>KV cache</td><td>نتیجهٔ میانی محاسبه برای استفادهٔ دوباره؛ نه پروندهٔ دائمی کاربر. جزئیاتش را در [[64-cache|درس Cache]] دنبال می‌کنیم.</td></tr><tr><td>External memory</td><td>اطلاعاتی بیرون وزن‌ها که برنامه باید انتخاب و دوباره وارد Context کند.</td></tr></tbody></table>
<p>بودجه را با عدد ببینید. اگر سقف مثال ۱۲۸ Token باشد و ۳۲ Token برای ادامه کنار بگذاریم، حداکثر ۹۶ Token برای ورودی داریم. پرسش، برچسب منبع، متن سند و جداکننده‌ها همگی جزو ورودی‌اند. <dfn>Token budget (بودجهٔ تعداد Token)</dfn> قرارداد شمارش همین هزینه‌هاست، نه تعداد کلمه‌ها. در Tokenizer کاراکترمحور پروژه هر Code point یک شناسه می‌شود؛ این برابری برای هر Tokenizer دیگری درست نیست.</p>
<div class="math">T_input + T_reserved ≤ T_max<br>128 − 32 = 96</div>
<p>در این نام‌گذاری، <code>T_input</code> طولِ ورودیِ کاملِ کدگذاری‌شده، <code>T_reserved</code> سقف درنظرگرفته‌شده برای ادامه و <code>T_max</code> سقف پنجره است. اینجا عمداً کل ورودی و ادامه را در یک پنجره جا می‌دهیم. خودِ تولید Mini-GPT می‌تواند با کنارگذاشتن ابتدای پنجره ادامه دهد؛ ولی آن رفتار ممکن است شاهدی را که برای پاسخ لازم بود حذف کند. کوتاه‌کردن بی‌خبرِ ابتدا، سیاست مدیریت Context ما نیست.</p>
<p>تابع <code>build_context</code> پرسش را اجباری نگه می‌دارد و قطعه‌ها را به ترتیب اولویتی که ما داده‌ایم امتحان می‌کند. قطعهٔ جا‌نشده را کامل کنار می‌گذارد؛ اگر قطعهٔ کوتاه‌ترِ بعدی جا شود، آن را می‌پذیرد. گزارش شناسه‌های پذیرفته و حذف‌شده اجازه می‌دهد بفهمیم مدل واقعاً چه چیزی خواهد دید. اولویت از امتیاز ارتباط می‌تواند بیاید، اما فعلاً خودمان آن را تعیین می‌کنیم.</p>
<p>ورودی نمونه به قالب JSON نوشته می‌شود تا مرز پرسش، شناسه و متن روشن باشد. این فقط قالب آموزشی ماست؛ Chat template استانداردِ همهٔ مدل‌ها نیست. شمارش را روی رشتهٔ نهایی انجام می‌دهیم: حتی جمع تعداد Tokenهای قطعه‌های جدا هم در همهٔ Tokenizerها برابر تعداد Tokenهای رشتهٔ چسبانده‌شده نیست. مرزبندی همچنین دفاع کامل در برابر دستورهای گمراه‌کنندهٔ داخل سند نیست؛ محتوای سند، مجوز اجرای ابزار یا تغییر سیاست برنامه نمی‌شود.</p>""",
"پرسش کوتاه و دو بند شاهد بسازید. با شمارندهٔ len یک بار بودجهٔ کافی و یک بار بودجهٔ محدود بدهید. پیش از اجرا مشخص کنید کدام شناسه‌ها جا می‌شوند. سپس هزینهٔ پرسش و قالب را عمداً نادیده بگیرید و اختلاف را نشان دهید.",
"len در این آزمایش فقط شمار Code pointهای قالب کامل است و با Tokenizer فعلی پروژه سازگار است. کاهش بودجه می‌تواند شاهد را حذف کند، بدون اینکه وزن مدل تغییر کند. اگر خود پرسش و قالب جا نشوند، تابع ValueError می‌دهد؛ نباید پرسش را بی‌خبر قطع کنیم.",
"وجود یک سند در فهرست Python، آیا ثابت می‌کند مدل آن را دیده است؟ کدام خروجی تابع شاهد دقیق‌تری می‌دهد؟",
"فایل mini_gpt/context.py مرز تازهٔ مدل و سامانه را می‌سازد. هنوز مدل را عوض نکرده‌ایم؛ فقط رشته‌ای را که بعداً به Tokenizer می‌رود با قراردادی قابل آزمون آماده کرده‌ایم.",
code='''from mini_gpt.context import ContextItem, build_context

items = [ContextItem("guide", "Run the exercise in Jupyter."),
         ContextItem("note", "Predict before running.")]
full = build_context("Where?", items, max_tokens=500, reserve_tokens=32, count_tokens=len)
limited = build_context("Where?", items, max_tokens=130, reserve_tokens=32, count_tokens=len)
assert full.included_ids == ("guide", "note")
assert limited.token_count+32 <= 130
assert limited.omitted_ids
assert '"question":"Where?"' in limited.prompt
print("Full:", full)
print("Limited:", limited)
''', source="mini_gpt/context.py",
review="اکنون می‌توانیم شاهد را وارد Context کنیم. اما اگر صد بند داشته باشیم، چطور بند مرتبط با پرسش را پیدا کنیم؟ درس بازیابی را با همین مسئله ادامه می‌دهیم."),

Lesson("69-chunks", 11, "یافتن شاهد", "سند بلند را کجا ببریم که پاسخ گم نشود؟",
"قطعه‌های قابل بازیابی بسازیم، مرز آن‌ها را ببینیم و هر قطعه را به محل اصلی‌اش وصل کنیم.",
"""<p>جست‌وجوی واژه‌ایِ [[67-rag|درس قبل]] یک سند مرتبط پیدا می‌کند، اما شاید سند از کل بودجهٔ ورودی بلندتر باشد. اگر فقط ابتدای آن را برداریم، ممکن است پاسخ در انتها بماند. به‌جای افزودن همهٔ سند، آن را به قطعه‌هایی تبدیل می‌کنیم که جدا قابل بازیابی باشند. این قطعه‌بندی، مسئلهٔ تازه‌ای هم می‌سازد: پاسخ ممکن است درست روی مرز دو قطعه بیفتد.</p>
<p><dfn>Chunk (قطعهٔ قابل بازیابی از سند)</dfn> بخشی از متن همراه هویت منبعش است. در این نسخه، هر قطعه حداکثر چند کلمهٔ جداشده با فاصله دارد. این روش را انتخاب کرده‌ایم چون مرزش با چشم پیداست؛ کلمهٔ این تابع با Token مدل یکی نیست. برای اطمینان از جاگرفتن قطعه در Context، همچنان باید Tokenizer مقصد آن را بشمارد.</p>
<p>دنبالهٔ هفت‌کلمه‌ای <code>one two three four five six seven</code> را با اندازهٔ ۴ ببرید. بدون هم‌پوشانی، قطعه‌ها «one two three four» و «five six seven» هستند. عبارت «four five» در هیچ‌کدام کامل نیست. با یک کلمه هم‌پوشانی، قطعهٔ دوم از «four» شروع می‌شود و عبارت کامل را دارد. <dfn>Chunk overlap (بخش مشترک قطعه‌های همسایه)</dfn> این فرصت را می‌سازد، ولی متن تکراری هم ذخیره و بازیابی می‌کند.</p>
<div class="math">stride = chunk_words − overlap_words<br>4 − 1 = 3</div>
<p><code>stride</code> تعداد کلمه‌هایی است که شروع قطعه جلو می‌رود؛ اندازهٔ قطعه ۴ و هم‌پوشانی ۱، شروع‌های صفر و ۳ را می‌دهند. هم‌پوشانی باید از اندازهٔ قطعه کوچک‌تر باشد تا پیشروی مثبت بماند. قطعهٔ آخر می‌تواند کوتاه‌تر باشد و نباید صرفاً به این دلیل حذف شود.</p>
<p>فقط متن قطعه را نگه ندارید. در <code>Chunk</code>، شناسهٔ سند، محل شروع و پایان و شناسهٔ قطعه هم هست. قرارداد پایان مانند برش Python باز است: <code>document.text[start:end]</code> باید دقیقاً متن قطعه را بدهد. این اندیس‌ها تعداد Code pointهای رشتهٔ Python را می‌شمارند، نه بایت فایل و نه Token مدل. همین قرارداد امکان برگشت از شاهد به متن اصلی را می‌دهد.</p>
<p>شناسهٔ قطعه از شناسهٔ سند و دو اندیس ساخته می‌شود. برای یک سندِ ثابت قابل تکرار است؛ بعد از ویرایش سند، همان جفت اندیس لزوماً به همان محتوا اشاره نمی‌کند. در سامانهٔ واقعی باید نسخه یا Hash سند را هم ثبت کنیم. اینجا سندهای آزمایش ثابت‌اند و آزمون، برابری برش با متن قطعه را می‌سنجد.</p>
<p>قطعهٔ بزرگ‌تر معمولاً جمله‌های بیشتری را کنار هم نگه می‌دارد، اما اطلاعات نامرتبط بیشتری وارد مقایسه و Context می‌کند. قطعهٔ بسیار کوچک ممکن است نام، شرط یا استثنای پاسخ را جدا کند. «هرچه کوچک‌تر بهتر» و «هم‌پوشانی بیشتر همیشه بهتر» قاعدهٔ آموزشی ما نیستند. در آزمایش، اندازه یا هم‌پوشانی را یکی‌یکی تغییر دهید و بپرسید شاهدِ کامل هنوز در دسترس هست یا نه.</p>
<p>دو قطعهٔ هم‌پوشان از یک سند، دو شاهد مستقل نیستند. اگر هر دو یک جمله را تکرار کنند، تعداد نتیجه‌ها زیاد شده است، نه تعداد منابع مستقل. بازیابی مرحلهٔ بعد باید علاوه بر امتیاز، محل و میزان تکرار شاهد را هم قابل مشاهده نگه دارد.</p>""",
"برای متن هفت‌کلمه‌ای، قطعه‌ها را با اندازهٔ ۴ و هم‌پوشانی صفر و یک دستی بنویسید. در هر حالت بررسی کنید «four five» داخل یک قطعه هست یا نه. سپس برش متن اصلی را با start و end هر قطعه مقایسه کنید.",
"بدون هم‌پوشانی دو قطعهٔ ۴ و ۳کلمه‌ای داریم. با هم‌پوشانی یک، دو قطعهٔ ۴کلمه‌ای داریم و «four five» در دومی کامل است. مجموع کلمه‌های قطعه‌ها هشت است، با اینکه سند هفت کلمه دارد؛ یک کلمه دوبار ذخیره شده است.",
"اگر سه نتیجه از یک بند تکراری آمده باشند، آیا سه منبع مستقل برای پاسخ داریم؟",
"mini_gpt/retrieval.py به قطعه‌ها شناسه و اندیس منبع می‌دهد. ContextItem بعداً همین شناسه را حمل می‌کند؛ از پاسخ نهایی باید بتوانیم تا قطعه و سند برگردیم.",
code='''from mini_gpt.retrieval import Document, chunk_document

document = Document("guide", "one two three four five six seven")
for overlap in [0, 1]:
    chunks = chunk_document(document, chunk_words=4, overlap_words=overlap)
    assert all(document.text[c.start:c.end] == c.text for c in chunks)
    print(overlap, [(c.id, c.text) for c in chunks])
    assert any("four five" in c.text for c in chunks) == (overlap == 1)
assert chunk_document(Document("empty", ""), chunk_words=4, overlap_words=1) == []
''', source="mini_gpt/retrieval.py"),

Lesson("70-vectors", 11, "یافتن شاهد", "امتیاز برداری از کجا معنا پیدا می‌کند؟",
"یک Index برداری کوچک بسازیم و تفاوت نمایش شمارشی با Embedding واقعاً آموزش‌دیده را ببینیم.",
"""<p>پرسش «ذخیره» است و سند مناسب فقط «نگهداری» را دارد. جست‌وجوی واژهٔ مشترک آن را پیدا نمی‌کند. آیا کافی است به هر واژه چند عدد تصادفی بدهیم و نزدیک‌ترین بردار را انتخاب کنیم؟ در [[25-embedding|درس Embedding]] دیدیم که جدول تازه هنوز رابطه‌ای یاد نگرفته است. اول یک نمایش کاملاً قابل توضیح می‌سازیم؛ بعد نشان می‌دهیم دادهٔ آموزشی چطور می‌تواند رابطهٔ دیگری وارد نمایش کند.</p>
<p>واژگان جست‌وجو را به ترتیب <code>["ثبت","ذخیره","مدل"]</code> بگذارید. متن «ذخیره ذخیره» بردار <code>[0,2,0]</code> و «ثبت ذخیره» بردار <code>[1,1,0]</code> می‌گیرد. هر مؤلفه شمار وقوع یک واژه است. <dfn>Bag-of-words (نمایش متن با شمار واژه‌ها)</dfn> ترتیب واژه‌ها را نگه نمی‌دارد. اینجا برخلاف ستون‌های Embedding مدل، معنای هر ستون را خودمان تعیین کرده‌ایم.</p>
<p>پرسش «ذخیره» بردار <code>[0,1,0]</code> دارد. Dot product آن با دو سند به‌ترتیب ۲ و ۱ است؛ تکرار یک واژه امتیاز را بزرگ کرده است. برای مقایسه‌ای که ضرب کل بردار در یک ضریب مثبت تغییرش ندهد، ضرب داخلی را بر حاصل‌ضرب Normها تقسیم می‌کنیم. <dfn>Cosine similarity (شباهت کسینوسی)</dfn> نام این امتیاز است. تعریف Norm را در [[06-dot|ضرب داخلی]] با مثال ۳ و ۴ دیده‌ایم.</p>
<div class="math">similarity(q,d) = (q·d) / (‖q‖ × ‖d‖)<br>q=[0,1,0], d₁=[0,2,0] → 1<br>q=[0,1,0], d₂=[1,1,0] → 1/√2 ≈ 0.707</div>
<p><code>q</code> بردار پرسش و <code>d</code> بردار سند است؛ دو علامت عمودی دور بردار، Norm اقلیدسی آن را نشان می‌دهند. امتیاز برای بردارهای غیرصفر بین منفی یک و یک است؛ در نمایش شمارشیِ نامنفیِ ما منفی نمی‌شود. اگر پرسش هیچ واژهٔ شناخته‌شده‌ای نداشته باشد، بردارش صفر می‌شود و مخرج تعریف نشده است. تابع آموزشی در این حالت امتیاز صفر می‌دهد و نتیجه‌ای برنمی‌گرداند. این تصمیمِ برنامه است، نه مقدار ریاضی Cosine برای بردار صفر.</p>
<p><dfn>Vector index (ساختار نگهداری بردارها برای جست‌وجو)</dfn> کوچک ما Vocabulary مشترک و بردار هر Chunk را نگه می‌دارد. پرسش با همان ترتیب ستون‌ها کدگذاری می‌شود؛ سپس امتیاز همهٔ قطعه‌ها را دقیق حساب می‌کنیم و بزرگ‌ترین‌ها را برمی‌داریم. <dfn>Ranking (مرتب‌کردن نتیجه‌ها با یک معیار)</dfn> فقط ترتیب می‌سازد؛ امتیاز ۰٫۸ یعنی «۸۰ درصد احتمال درستی پاسخ» نیست. برای چند سند به Vector database یا جست‌وجوی تقریبی نیاز نداریم.</p>
<p>این Index هنوز مشکل «ذخیره/نگهداری» را حل نکرده است. برای دیدن نقش یادگیری، آزمایش جداگانهٔ چهارتایی داریم: «ذخیره، نگهداری، حساب، ضرب». خودمان دو جفت مرتبط و دو جفت نامرتبط را برچسب می‌زنیم. جدول <code>nn.Embedding(4,2)</code> برای هر واژه دو عدد قابل آموزش دارد. امتیاز Cosine جفت‌های مرتبط را به ۱ و جفت‌های نامرتبط را به −۱ نزدیک می‌کنیم؛ Loss میانگین مربع فاصلهٔ امتیاز و برچسب است. این همان چرخهٔ Forward، Loss، Gradient و Step است، با هدفی متفاوت از پیش‌بینی Token بعدی.</p>
<p>تابع <code>train_tiny_embeddings</code> در فایل منبع، جفت‌ها، Loss و حلقهٔ SGD را آشکار نوشته است. عددهای آغاز ثابت‌اند تا بتوانید قبل و بعد را مقایسه کنید. این یک Embedding واقعاً آموزش‌دیده است، اما فقط رابطه‌هایی را تمرین کرده که خودمان داده‌ایم. موفقیت روی همین چهار واژه، شاهد درک عمومی فارسی یا کیفیت روی پرسش ندیده نیست. همهٔ بردارها را یکسان‌کردن هم راه‌حل نیست: جفت‌های نامرتبط در آن صورت جریمه می‌شوند.</p>
<p>برای بازیابی معناییِ گسترده‌تر، مدل نمایش باید روی داده و هدف مناسب آموزش دیده و جدا ارزیابی شده باشد. میانگین‌گرفتن تصادفی از Token Embeddingهای Mini-GPT چنین تضمینی نمی‌سازد. در پروژه، <code>VectorIndex</code> عمداً شمارشی می‌ماند؛ آزمایش یادگیری کوچک کنار آن، تفاوت منبع عددها را روشن می‌کند. در هر دو روش، پرسش و سند باید با نمایش سازگار مقایسه شوند.</p>""",
"امتیاز دو بردار شمارشی را دستی حساب کنید. سپس Index را روی دو سند بسازید و پرسشِ بدون واژهٔ مشترک بدهید. آزمایش چهار Embedding را اجرا کنید؛ کاهش Loss و تغییر رتبهٔ جفت مرتبط را ثبت کنید و دقیق بنویسید کدام رابطه‌ها از برچسب‌های خودتان آمده‌اند.",
"امتیازها ۱ و حدود ۰٫۷۰۷ هستند؛ دوبرابرکردن بردار غیرصفر امتیاز Cosine را عوض نمی‌کند. Index شمارشی برای واژهٔ ناآشنا نتیجه‌ای ندارد. در آزمایش یادگیری، پارامترهای جدول با جفت‌های مشخص تغییر می‌کنند؛ این تغییر در وزن‌های Mini-GPT یا Index شمارشی اعمال نمی‌شود.",
"چرا یک بردارِ تصادفی و یک بردارِ آموزش‌دیده با Shape یکسان، شواهد یکسانی دربارهٔ ارتباط معنایی ندارند؟",
"mini_gpt/retrieval.py هم Index دقیقِ شمارشی و هم آزمایش کوچک آموزش Embedding را دارد. این دو را در گزارش با نام جدا ثبت کنید؛ فایل آمادهٔ بیرونی یا سرویس شبکه‌ای در کار نیست.",
code='''from mini_gpt.retrieval import Document, chunk_document, VectorIndex
from mini_gpt.retrieval import cosine_similarity, train_tiny_embeddings

chunks = [chunk_document(Document(name, text), chunk_words=8, overlap_words=0)[0]
          for name, text in [("a", "ذخیره ذخیره"), ("b", "ثبت ذخیره")]]
index = VectorIndex(chunks)
print("Vocabulary:", index.vocabulary, "Vectors:", index.vectors)
hits = index.search("ذخیره")
assert [hit.chunk.document_id for hit in hits] == ["a", "b"]
assert index.search("نگهداری") == []
words, before, after, first, final = train_tiny_embeddings()
print("Pair:", words[:2], cosine_similarity(before[0], before[1]),
      cosine_similarity(after[0], after[1]))
print("Training-pair loss:", first, final)
assert final < first
assert cosine_similarity(after[0], after[1]) > cosine_similarity(after[0], after[2])
''', source="mini_gpt/retrieval.py"),

Lesson("71-grounding", 11, "پاسخ مستند", "منبع داریم؛ آیا پاسخ واقعاً از آن پشتیبانی می‌شود؟",
"بازیابی، ساخت پاسخ و وارسی شاهد را جدا کنیم؛ نبود شاهد و تعارض سندها را پنهان نکنیم.",
"""<p>دستیار دو سند پیدا کرده است. یکی می‌گوید «تمرین در Jupyter اجرا می‌شود» و دیگری «برای تمرین از فایل Python استفاده کنید». اگر فقط نخستین نتیجه را با لحن مطمئن تکرار کنیم، مسئلهٔ تعارض حل نشده است. حتی اگر نام هر دو سند را پایین پاسخ بنویسیم، هنوز معلوم نیست کدام ادعا از کدام متن آمده است.</p>
<p><dfn>Grounding (اتکای پاسخ به شاهدِ در دسترس)</dfn> در این مسیر یعنی بتوانیم ادعای پاسخ را به محتوایی مشخص وصل کنیم. <dfn>Citation (ارجاع به منبعِ یک ادعا)</dfn> نشانی آن شاهد را به خواننده می‌دهد. وجود شناسهٔ معتبر شرط مفیدی است، اما کافی نیست: می‌توان منبع واقعی را به ادعایی نامربوط وصل کرد. همچنین ممکن است خودِ منبع قدیمی یا نادرست باشد؛ نقل درستِ یک متن با درست‌بودن واقعیت بیرونی یکی نیست.</p>
<p>مسیر را به سه پرسش بشکنید. Retrieval می‌پرسد کدام قطعه‌ها به پرسش مربوط‌اند. ساخت Context می‌پرسد کدام قطعه‌ها با چه هویتی جا می‌شوند. پاسخ‌سازی می‌پرسد با اتکا به همین ورودی چه متنی تولید شود. در RAG، مدل مولد در مرحلهٔ آخر قرار می‌گیرد؛ فقط داشتن فهرست نتیجه‌های جست‌وجو، اجرای کامل RAG نیست.</p>
<p>برای آزمودن مرزها، ابتدا پاسخ‌سازی را ساده می‌کنیم: تابع <code>answer_from_hits</code> متن قطعه‌های منتخب را عیناً با شناسهٔ منبع نقل می‌کند. این <dfn>Extractive baseline (روش پایه با برداشتن متن از منبع)</dfn> است، نه خروجی Mini-GPT و نه الگوریتمی که هر پرسش را فهمیده باشد. چون متن را عیناً برمی‌گرداند، می‌توانیم مسیر شاهد را بدون وابستگی به کیفیت تولید مدل آزمایش کنیم. در دستیار نهایی، خروجی این روش و خروجی واقعی مدل با برچسب جدا دیده می‌شوند.</p>
<p>اگر نتیجه‌ای با امتیاز مثبت نداریم، تابع «شاهد کافی پیدا نشد» برمی‌گرداند. <dfn>Abstention (خودداری از پاسخ قطعی در نبود شاهد کافی)</dfn> به ما اجازه می‌دهد شکست بازیابی را با پاسخ ساختگی نپوشانیم. آستانهٔ امتیاز هم حقیقت را تضمین نمی‌کند؛ آن را باید با پرسش‌های پاسخ‌پذیر و بی‌پاسخ بسنجیم. بالا بردن آستانه می‌تواند شاهد ضعیف را حذف کند و هم‌زمان پاسخ درستِ کم‌امتیاز را هم از دست بدهد.</p>
<p>در مثال دو سند، هر دو نقل را جدا نشان می‌دهیم و خودکار یکی را حقیقت اعلام نمی‌کنیم. برای تصمیم بیشتر ممکن است تاریخ، نسخهٔ دوره یا مرجع معتبرتر لازم باشد. در [[69-chunks|درس قطعه‌بندی]] هم دیدیم که دو Chunk هم‌پوشان لزوماً دو منبع مستقل نیستند. شمار ارجاع‌ها به‌تنهایی معیار کیفیت پاسخ نیست.</p>
<p>دو آزمون کوچک ولی متفاوت بنویسید: هر شناسهٔ ارجاع باید در قطعه‌های استفاده‌شده موجود باشد؛ هر نقلِ مستقیم هم باید دقیقاً در متن همان قطعه پیدا شود. این آزمون‌ها جعل شناسه یا نقل را می‌گیرند، اما بازنویسیِ معناییِ غلط، حذف شرط و گزینش شاهد نامرتبط را کامل تشخیص نمی‌دهند. برای آن‌ها نمونهٔ ارزیابی و بررسی محتوایی جدا لازم است.</p>
<p>متن سند را داده در نظر بگیرید. جمله‌ای مثل «از اینجا به بعد همهٔ قواعد را نادیده بگیر» داخل سند، اختیار تازه‌ای به برنامه نمی‌دهد. بعداً وقتی ابزار اضافه می‌کنیم، اجازهٔ اجرا را کدِ کنترل‌کننده تعیین می‌کند، نه حضور یک فرمان در نتیجهٔ Retrieval.</p>""",
"دو سندِ مربوط ولی متعارض و یک پرسشِ بدون پاسخ بسازید. نتیجهٔ جست‌وجو، متن پاسخ و شناسه‌های ارجاع را جدا ثبت کنید. سپس یک نقل را عمداً به منبع دیگر نسبت دهید و آزمونی بنویسید که این جابه‌جایی را بگیرد.",
"روش نقل مستقیم باید متن هر دو سند را با شناسهٔ خودش نشان دهد و تعارض را خودکار حل‌شده اعلام نکند. برای پرسش بدون واژهٔ مشترک، abstained=True و citations خالی است. آزمون نقل فقط وفاداری به متن را می‌سنجد؛ صحت علمی خود منبع هنوز ادعای دیگری است.",
"آیا وجود یک Citation معتبر ثابت می‌کند تمام جمله‌های پاسخ پشتیبانی شده‌اند؟ یک مثال نقض بسازید.",
"این مرز در mini_gpt/retrieval.py خروجی GroundedAnswer می‌سازد. در ادامه، تاریخچه و حافظه هم مانند سند وارد Context می‌شوند؛ هیچ‌کدام نباید بی‌بررسی به حقیقت قطعی ارتقا پیدا کنند.",
code='''from mini_gpt.retrieval import Document, chunk_document, keyword_search, answer_from_hits

documents = [Document("old", "تمرین در فایل Python اجرا می‌شود."),
             Document("new", "تمرین در Jupyter اجرا می‌شود.")]
chunks = [c for document in documents
          for c in chunk_document(document, chunk_words=20, overlap_words=0)]
hits = keyword_search("تمرین", chunks)
answer = answer_from_hits(hits)
assert not answer.abstained and len(answer.citations) == 2
assert all(hit.chunk.text in answer.text for hit in hits)
unknown = answer_from_hits(keyword_search("هواشناسی", chunks))
assert unknown.abstained and unknown.citations == ()
print("Extractive evidence, not a model completion:", answer.text)
print(unknown.text)
''', source="mini_gpt/retrieval.py"),
]
