"""صفحه‌های راهنما؛ خارج از زنجیرهٔ پیش‌نیازهای درس‌ها."""

GUIDE = '''
<p>برای نصب روی Windows، [[windows.html|راهنمای گام‌به‌گام Windows]] را باز کنید؛ مطالعه، اجرای مدل و ساخت کتاب نیازهای جدا دارند.</p>
<p class="objective">این مسیر برای کسی است که Python می‌نویسد، اما هنوز نمی‌داند یک شبکه چگونه از متن یاد می‌گیرد. برای شروع نه کارت گرافیکی لازم است، نه حساب سرویس، نه دانش اصطلاح‌های هوش مصنوعی.</p>
<h2>این کتاب را چطور بخوانیم؟</h2>
<p>کتاب ۷۶ درس و ۱۰ ایستگاه پایان بخش دارد. هر درس یک سؤال اصلی را دنبال می‌کند: ابتدا مسئله را بخوانید، پیش‌بینی خود را بنویسید، تکلیف را انجام دهید و بعد صفحهٔ پاسخ را باز کنید. ۲۳ مرحلهٔ اجرایی پروژه، از شمارهٔ صفر تا ۲۲، قطعه‌های همین مسیر را قابل مشاهده می‌کنند؛ این شماره‌ها با شمارهٔ درس‌ها یکی نیستند. فایل نهایی از ابتدا موجود است، اما لازم نیست در روز اول تمامش را بفهمید.</p>
<p>نوبت اول حدود ۳۰ تا ۹۰ دقیقه برای یک درس کنار بگذارید. پس از هر دو یا سه نشست یک روز فاصله بدهید. در بازگشت، موضوع را با یک مثال تازه توضیح دهید. اگر نشد، فقط همان قطعه را دوباره بسازید. ایستگاه پایان بخش معیار عملی دارد؛ عجله برای رسیدن به انتهای فهرست کمکی نمی‌کند.</p>
<p>یک برنامهٔ منعطف: بخش‌های ۱ و ۲ در دو هفتهٔ نخست؛ بخش‌های ۳ و ۴ در دو تا سه هفتهٔ بعد؛ توجه و بلوک‌ها در دو تا چهار هفته؛ آموزش و آزمایش در دو تا چهار هفتهٔ دیگر. این زمان‌بندی فقط برای برنامه‌ریزی است، نه سنجش سرعت شما. اگر جابه‌جایی محورها یا پس‌انتشار زمان بیشتری گرفت، همان‌جا مکث کنید و مثال را دوباره بسازید.</p>
<p>هر یک از ۷۶ درس، [[notebooks.html|آزمایشگاه Jupyter مخصوص خود]] را دارد: مثال را اجرا کنید، تابع‌های TODO را بنویسید و خرابی را اصلاح کنید. ۱۲ دفتر مرور هم برای کنار هم گذاشتن چند ایده حفظ شده‌اند. کتاب توضیح می‌دهد و دفتر نیمهٔ عملی آن است؛ جواب‌ها جدا می‌مانند.</p><h2>پیش‌نیازهای مشخص</h2>
<p>باید بتوانید تابع، حلقه، فهرست، دیکشنری، کلاس ساده و خواندن فایل را در Python استفاده کنید. فرمول‌ها از بردار و ضرب شروع می‌شوند. واژهٔ تازه نخست در همان درس توضیح داده می‌شود؛ [[glossary.html|واژه‌نامه]] شما را به محل توضیح بازمی‌گرداند. <code>API</code> یعنی رابط برنامه‌نویسی: نام و قرارداد تابعی که از یک کتابخانه صدا می‌زنیم. اگر نام یا روش استفادهٔ تابعی را فراموش کردید، به [[api.html|راهنمای کد]] برگردید؛ لازم نیست همهٔ نام‌ها را حفظ کنید.</p>
<h2>برای شروع کامل، فقط یک بار نصب کنید</h2>
<p>اگر می‌خواهید کتاب و همهٔ دفترها را از همان درس اول داشته باشید، [[windows.html|راهنمای Windows]] را دنبال کنید: یک محیط، نصب <code>requirements-notebooks.txt</code> و سپس <code>python run.py</code>. فرمان‌های مدلِ زیر برای اجرای مستقل یا بررسی نصب هم قابل استفاده‌اند.</p>
<p>برای بخش‌های اول و دوم و مرحله‌های اجرایی صفر تا دو، خود Python کافی است. این بازبینی با Python 3.11 و PyTorch 2.14.0 نسخهٔ CPU آزموده شده؛ نیازمندی پروژه PyTorch از ۲٫۶ تا پیش از ۳ است. ابتدا [[downloads/mini-gpt-project.zip|فایل ZIP پروژه]] را باز کنید؛ ZIP بسته‌ای از فایل‌هاست. فرمان‌ها را در پوشه‌ای اجرا کنید که requirements.txt و پوشهٔ mini_gpt در آن قرار دارند.</p>
<p>محیط مجازی، پوشهٔ جدا برای بسته‌های همین پروژه است. <code>venv</code> آن را می‌سازد؛ <code>pip</code> بسته‌ها را نصب می‌کند؛ <code>-m</code> یعنی اجرای یک ماژول Python. در Windows لازم نیست محیط را فعال کنید:</p>
<pre><code>python -m venv .venv
.venv\\Scripts\\python.exe -m pip install --upgrade pip
.venv\\Scripts\\python.exe -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
.venv\\Scripts\\python.exe -m pip install -r requirements.txt
.venv\\Scripts\\python.exe -c "import torch; print(torch.__version__)"
.venv\\Scripts\\python.exe -m mini_gpt.smoke_test</code></pre>
<p>فرمان بالا برای CPU در Windows و Linux از مخزن رسمی بسته‌ها استفاده می‌کند. در Linux مسیر مفسر <code>.venv/bin/python</code> است. در macOS نیز همین مسیر را به کار ببرید، اما نصب را بدون گزینهٔ مخزن CPU انجام دهید: <code>.venv/bin/python -m pip install -r requirements.txt</code>. برای دستگاه‌های دیگر و GPU از <a href="https://pytorch.org/get-started/locally/">انتخاب‌گر رسمی نصب PyTorch</a> کمک بگیرید؛ سازگاری همهٔ کارت‌ها و نسخه‌های آینده در این پروژه آزموده نشده است. دربارهٔ دستگاه اجرا ابتدا [[14-index-device]] را بخوانید.</p>
<p>در باقی کتاب، <code>python</code> کوتاه‌شدهٔ مسیر همین مفسرِ دارای PyTorch است. نصب اینترنت می‌خواهد؛ صفحات کتاب و برنامه‌های نصب‌شده محلی کار می‌کنند. اگر کنسول یا خروجی هدایت‌شدهٔ Windows برای چاپ فارسی <code>UnicodeEncodeError</code> داد، حالت UTF-8، یعنی کدگذاری سازگار با نویسه‌های فارسی، را روشن کنید:</p>
<pre><code>.venv\\Scripts\\python.exe -X utf8 -m mini_gpt.milestones --stage 1</code></pre>
<p>این گزینه را می‌توانید پیش از <code>-m</code> در دیگر فرمان‌ها هم بگذارید؛ مشکل کدگذاری متن را حل می‌کند، نه خطای آموزش. فایل مدل ناشناس را هرگز بارگذاری نکنید. گزینهٔ محدودکنندهٔ <code>weights_only=True</code> مجوز اعتماد به هر checkpoint نیست؛ ابتدا منبع قابل اعتماد لازم است. [[50-checkpoint]] قرارداد ذخیره و بارگذاری را توضیح می‌دهد.</p>
<h2>چطور پیشرفت را نگه داریم؟</h2>
<p>صرفِ بازکردن صفحه، درس را «انجام‌شده» نمی‌کند. پس از تکلیف و خودسنجی دکمهٔ انجام را بزنید؛ با کلیک دوباره می‌توانید آن را لغو کنید. شمارنده ۸۶ واحد دارد: ۷۶ درس و ۱۰ ایستگاه. مرحله‌های اجرایی پروژه چک‌لیست جداگانه‌ای به این شمارنده اضافه نمی‌کنند.</p>
<p>وضعیت در مرورگر خودتان ذخیره می‌شود. اگر کتاب را با پروتکل، میزبان و درگاه یکسان در چند زبانه باز کنید، تغییر پیشرفت میان آن‌ها همگام می‌شود. آخرین تغییر هر واحد ملاک است: اگر انجام یک درس را لغو کرده باشید، بازکردن پیوند قدیمی نباید آن را دوباره انجام‌شده کند.</p><p>برای فایل محلی یا نبود ذخیره، پارامتر <code>progress</code> در نشانی، وضعیت زمان‌دارِ انجام و لغو را حمل می‌کند. در HTTP با سابقهٔ محلی، نشانی قدیمی بر آن سابقه تقدم ندارد. پارامتر قدیمی <code>done</code> فقط هنگام نبود سابقه برای مهاجرت خوانده می‌شود. اگر ذخیرهٔ ویرایش دوم در همان مرورگر باشد، موارد معتبر آن نیز منتقل می‌شوند.</p>
<p>مرتب خروجی JSON بگیرید؛ JSON قالب متنی برای نگهداری دادهٔ ساخت‌یافته است. فایل پیشرفت ویرایش دوم یا سوم قابل ورود است و موارد انجام‌شده را با وضع فعلی ادغام می‌کند، نه اینکه موارد فعلی را پاک کند. پاک‌کردن دادهٔ مرورگر، حالت خصوصی یا عوض‌شدن نشانی می‌تواند ذخیره را از دسترس خارج کند؛ فایل خروجی راه بازگشت است.</p>
<h2>دفتر، فقط تیک‌زدن نیست</h2>
<p>در [[journal.html|دفتر آزمایش مرورگر]] ابتدا پیش‌نویس بنویسید و بعد «ثبت یادداشت» را بزنید تا برگه‌ای قابل بازگشت داشته باشید. سؤال، پیش‌بینی، تنظیمات، فرمان، مشاهده و نتیجه را جدا نگه دارید. پیش‌نویس هر صفحه جدا نگهداری می‌شود؛ بخش «پیش‌نویس‌های قابل بازیابی» راه بازگشت به نوشتهٔ ثبت‌نشده است. دادهٔ دفتر در مرورگر ذخیره می‌شود و ارسال به سرور ندارد. خروجی تمام دفتر و ورود دوبارهٔ آن برای انتقال یا پشتیبان‌گیری فراهم است؛ این فایل با خروجی پیشرفت درس‌ها فرق دارد.</p>
<p>ترجیحاً ثبت دفتر را در یک زبانه انجام دهید. اگر دو ویرایش یک یادداشت ناسازگار باشند، هر دو نسخه نگه داشته می‌شوند؛ این به معنی ادغام خودکار معنی متن نیست. خروجی، برگه‌های ثبت‌شده و تغییر ثبت‌نشدهٔ فرم فعلی را دارد؛ برای پیش‌نویس زبانهٔ دیگر، نخست آن را از بخش بازیابی باز و بعد خروجی بگیرید. پیش از جابه‌جایی مرورگر یا پاک‌کردن داده‌ها، بازشدن فایل خروجی را بررسی کنید. اگر ثبت کنار کد را ترجیح می‌دهید، [[code/mini_gpt/learning-log.md.html|دفتر متنی پروژه]] نیز آماده است.</p>
<h2>دو روش بازکردن کتاب</h2>
<p>برای خواندن ساده می‌توانید dist/index.html را مستقیم باز کنید. برای کتاب و Jupyter در یک محیط، از ریشهٔ پروژه <code>python run.py</code> را اجرا و <code>http://127.0.0.1:8000/start.html</code> را باز کنید. هر دو سرور فقط از روی رایانهٔ خودتان در دسترس‌اند و با Ctrl+C متوقف می‌شوند. نشانی و درگاه را ثابت نگه دارید؛ مرورگر نشانی متفاوت را فضای ذخیرهٔ دیگری می‌بیند.</p>
<p>برای مسیر خواندن، فهرست مرکزی و نقشهٔ بازشدنی کتاب را دارید؛ قرار نیست یک نوار کناری دائمی فضای متن را اشغال کند. جست‌وجوی نقشه در عنوان درس‌هاست، نه موتور جست‌وجوی تمام متن. نشانگر درس فعلی، مسیر بخش/فصل و پیوند قبلی/بعدی جای شما را روشن می‌کنند. صفحهٔ پروژه نیز راه بازگشت به مرحلهٔ اجرایی است.</p>
<p>اگر کپی خودکار مجاز نباشد، متن کد انتخاب می‌شود تا Ctrl+C بزنید. کد و فرمول چپ‌به‌راست‌اند و متن فارسی راست‌به‌چپ. انیمیشن ضروری برای خواندن وجود ندارد؛ تنظیم کاهش حرکت سیستم نیز رعایت می‌شود.</p>
<p>آماده‌اید؟ [[01-model]] را باز کنید. امروز لازم نیست بدانید Transformer چیست.</p>
'''

PROJECT = '''
<p class="objective">Mini-GPT نام پروژهٔ کوچک آموزشی ماست: برنامه‌ای که نویسهٔ بعدی را حدس می‌زند. نه دستیار پرسش‌وپاسخ است، نه مدل هم‌اندازهٔ سرویس‌های بزرگ، و نه ابزار قابل اتکا برای تصمیم مهم.</p>
<p>[[downloads/mini-gpt-project.zip|دریافت کد و دادهٔ نمونه]] · [[code/index.html|مرجع فایل‌ها]] · [[code/mini_gpt/README.md.html|راهنمای کامل اجرا]] · [[lab.html|آزمایشگاه مدل]] · [[journal.html|دفتر آزمایش]]</p>
<h2>۲۳ مرحله، روی قطعه‌های یک پروژه</h2>
<p>هر ردیف یک خروجی قابل دیدن دارد: شناسه، تنسور، معیار یا فایل. نام فنی را نخست در درس پیوندشده بیاموزید؛ این جدول نقشهٔ راه است، نه فهرست اصطلاح‌های لازم برای روز اول. همهٔ مرحله‌ها از قطعه‌های مشترک Mini-GPT استفاده می‌کنند و هر اجرا با ورودی کوچک و بذر ثابت آغاز می‌شود. اجرای مرحلهٔ بعد، وزن‌های مرحلهٔ قبل را از جایی پنهانی ادامه نمی‌دهد.</p>
<pre><code>python -X utf8 -m mini_gpt.milestones --stage 0
python -X utf8 -m mini_gpt.milestones --stage 7 --output runs/milestone-07.json</code></pre>
<p>عدد <code>--stage</code> را از جدول انتخاب کنید. <code>--output</code> اختیاری است و گزارش JSON، یعنی متن ساخت‌یافتهٔ عددها و نام‌ها، را در مسیر تازه ذخیره می‌کند. بدون آن گزارش در کنسول چاپ می‌شود. فایل موجود بازنویسی نمی‌شود. مرحله‌های صفر تا دو PyTorch لازم ندارند.</p>
<table><tr><th>مرحله</th><th>قطعه‌ای که باز می‌کنیم</th><th>شاهد خروجی</th><th>درس همراه</th></tr>
<tr><td>۰</td><td>شمارش ادامهٔ نویسه</td><td>شمارش جفت‌ها و احتمال تجربی بدون هموارسازی</td><td>[[03-counts]]</td></tr>
<tr><td>۱</td><td>نشانه‌بند</td><td>متن، نشانه‌ها، encode/decode و یک نویسهٔ ناشناخته</td><td>[[21-tokenizer]]</td></tr>
<tr><td>۲</td><td>واژگان و شناسه</td><td>نگاشت دوطرفهٔ نویسه/شناسه</td><td>[[02-token]]</td></tr>
<tr><td>۳</td><td>بردار نمایش</td><td>خواندن سطرهای جدول؛ خروجی (1,8,16)</td><td>[[25-embedding]]</td></tr>
<tr><td>۴</td><td>اطلاعات موقعیت</td><td>بردارهای موقعیت و جمع آن‌ها با نمایش نشانه</td><td>[[26-positions]]</td></tr>
<tr><td>۵</td><td>توجه تک‌سر بدون پوشش</td><td>جدول وزن و مجموع وزن‌های آینده؛ نمونهٔ عمدیِ نامعتبر برای آموزش علّی</td><td>[[32-self]]</td></tr>
<tr><td>۶</td><td>پوشش علّی</td><td>صفرشدن وزن‌های بالای قطر</td><td>[[33-mask]]</td></tr>
<tr><td>۷</td><td>توجه چندسر</td><td>دو جدول توجه با شکل (1,2,8,8)</td><td>[[35-split-heads]]</td></tr>
<tr><td>۸</td><td>شبکهٔ پیش‌خور</td><td>تبدیل مستقل ویژگی‌های هر موقعیت</td><td>[[37-ffn]]</td></tr>
<tr><td>۹</td><td>مسیر جمع</td><td>x+FFN(x) و اندازهٔ ورودی/اصلاح</td><td>[[38-residual]]</td></tr>
<tr><td>۱۰</td><td>نرمال‌سازی ویژگی‌ها</td><td>میانگین و واریانس سطرها بعد از LayerNorm</td><td>[[39-layernorm]]</td></tr>
<tr><td>۱۱</td><td>یک بلوک کامل</td><td>توجه و پیش‌خور با دو نرمال‌سازی پیشین و دو مسیر جمع</td><td>[[40-block]]</td></tr>
<tr><td>۱۲</td><td>چند بلوک</td><td>عبور از دو بلوک مستقل و حفظ شکل</td><td>[[41-stack]]</td></tr>
<tr><td>۱۳</td><td>سر مدل زبان</td><td>امتیاز همهٔ نشانه‌ها و خطای هدف شیفت‌یافته</td><td>[[43-lm-head]]</td></tr>
<tr><td>۱۴</td><td>حلقهٔ آموزش کوچک</td><td>خطای پیش/پس از ۲۰ گام روی یک دستهٔ ثابت</td><td>[[47-loop]]</td></tr>
<tr><td>۱۵</td><td>ارزیابی بدون تغییر وزن</td><td>معیار روی متنی مصنوعی و هم‌خانواده، با حالت ارزیابی</td><td>[[48-evaluate]]</td></tr>
<tr><td>۱۶</td><td>ذخیره و بارگذاری</td><td>آزمون برابری وزن‌ها پس از رفت‌وبرگشت فایل</td><td>[[50-checkpoint]]</td></tr>
<tr><td>۱۷</td><td>تولید پیاپی</td><td>چهار نشانهٔ تازه با انتخاب حریصانه، شناسه و متن</td><td>[[54-generate]]</td></tr>
<tr><td>۱۸</td><td>نمونه‌گیری</td><td>توزیع انتخاب و یک شناسهٔ نمونه‌گرفته‌شده</td><td>[[55-temperature]]</td></tr>
<tr><td>۱۹</td><td>دما</td><td>توزیع انتخاب با دمای ۰٫۵</td><td>[[55-temperature]]</td></tr>
<tr><td>۲۰</td><td>نگه‌داشتن k گزینه</td><td>توزیع محدود به سه نامزد</td><td>[[56-topkp]]</td></tr>
<tr><td>۲۱</td><td>جرم احتمال p</td><td>توزیع محدود با آستانهٔ ۰٫۷</td><td>[[56-topkp]]</td></tr>
<tr><td>۲۲</td><td>مدل آموزشی یکپارچه</td><td>کل مسیر کوچک، شمار پارامترها و فرمان ورود به آموزش پیکره</td><td>[[52-first-run]]</td></tr></table>
<p>از مرحلهٔ ۱۴ به بعد، گزارش شامل یک آموزش تشخیصیِ ۲۰گامی روی دستهٔ ثابت است. دادهٔ ارزیابی نیز مصنوعی و بسیار نزدیک به دادهٔ تمرین است؛ این عدد شاهد تعمیم عمومی نیست. نقش این مرحله‌ها دیدن اتصال قطعه‌هاست. آموزش واقعی پیکره با فرمان train در ادامهٔ همین صفحه انجام می‌شود.</p>
<p>از مرحلهٔ ۱۶ می‌توانید <code>--checkpoint runs/milestone-16.pt</code> را برای نگهداری فایل تازه بدهید؛ بدون این گزینه، ذخیره/بارگذاری در فایل موقت آزموده و سپس پاک می‌شود. فایل مرحلهٔ کوچک، ذخیرهٔ قابل ادامهٔ آموزش پیکره با train نیست؛ اطلاعات کامل آن گردش کار را ندارد. گزارش JSON مرحله‌ها نیز با JSON ابزار بازرسی مدل تفاوت قالب دارد؛ فقط خروجی inspect را در بخش ورود مدل واقعی آزمایشگاه وارد کنید.</p>
<details><summary>نام‌های قدیمی v0 تا v8 چه هستند؟</summary>
<p>این ۹ نام برای سازگاری با درس‌ها و یادداشت‌های ویرایش پیشین حفظ شده‌اند؛ با شمارهٔ مرحله‌های صفر تا ۲۲ یکی نیستند. آن‌ها مسیر خلاصهٔ قدیمی‌اند، نه ۹ معماری تازه و مستقل.</p>
<table><tr><th>نام قدیمی</th><th>کاربرد</th><th>اجرا</th></tr>
<tr><td>v0</td><td>ادامه‌دهندهٔ شمارشی</td><td><code>python -m mini_gpt.stages.v0</code></td></tr>
<tr><td>v1</td><td>فقط نمایش نشانه و سر خروجی</td><td><code>python -m mini_gpt.stages.run --stage 1</code></td></tr>
<tr><td>v2</td><td>توجه تک‌سر بی‌پوشش</td><td><code>python -m mini_gpt.stages.run --stage 2</code></td></tr>
<tr><td>v3</td><td>توجه تک‌سر علّی</td><td><code>python -m mini_gpt.stages.run --stage 3</code></td></tr>
<tr><td>v4</td><td>چندسر و موقعیت</td><td><code>python -m mini_gpt.stages.run --stage 4</code></td></tr>
<tr><td>v5</td><td>یک بلوک کامل</td><td><code>python -m mini_gpt.stages.run --stage 5</code></td></tr>
<tr><td>v6</td><td>مدل نهایی کوچک دو‌بلوکی</td><td><code>python -m mini_gpt.stages.run --stage 6</code></td></tr>
<tr><td>v7</td><td>درگاه قدیمی فرمان آموزش</td><td><code>python -m mini_gpt.stages.v7 --help</code></td></tr>
<tr><td>v8</td><td>درگاه قدیمی فرمان تولید</td><td><code>python -m mini_gpt.stages.v8 --help</code></td></tr></table>
<p>فرمان‌های قدیمی ۱ تا ۶ شکل و حساسیت به آینده را نشان می‌دهند. v7 و v8 کپی تازه‌ای از معماری نمی‌سازند؛ همان ابزارهای train و generate را فراخوانی می‌کنند.</p></details>
<h2>معماری در پایان بخش هفتم</h2>
<div class="figure">text → tokenizer → IDs (B,T)
                       ↓ table lookup + position
                     (B,T,C)
                       ↓ Transformer blocks × L
                       ↓ final LayerNorm
                       ↓ Linear C → V
                 logits (B,T,V)
                  ↙              ↘
       shifted targets         last position
             ↓                      ↓
      cross entropy         probabilities → next ID</div>
<p>پیکان نخست متن را به شمارهٔ سطرهای جدول تبدیل می‌کند. خواندن سطرها و افزودن موقعیت، نمایش Cتایی می‌سازد. هر بلوک اطلاعات گذشته را ترکیب می‌کند و شکل را نگه می‌دارد. تبدیل آخر امتیاز V نشانه را می‌سازد؛ آموزش آن را با پاسخ یک‌خانه جلوتر می‌سنجد، تولید از آخرین موقعیت یک نشانه برمی‌گزیند. [[45-trace]] هر پیکان را باز می‌کند.</p>
<div class="figure">x (B,T,C) ────────────────────┐
  ↓ LayerNorm                 │
  ↓ causal multi-head attention
  ↓ + ←───────────────────────┘
  y ──────────────────────────┐
  ↓ LayerNorm                 │
  ↓ Linear C→4C → GELU → 4C→C │
  ↓ + ←───────────────────────┘
output (B,T,C)</div>
<p>ترتیب این مدل Pre-Norm است: نرمال‌سازی پیش از هر زیرلایه انجام می‌شود، نه بعد از جمع. توجه اطلاعات مجاز گذشته را می‌آورد و میان‌بُر، ورودی همان زیرلایه را مستقیم اضافه می‌کند. شبکهٔ پیش‌خور ویژگی‌های هر موقعیت را غیرخطی تبدیل می‌کند. حذف تصادفیِ آموزش نیز در ورودی و داخل زیرلایه‌ها هست؛ نمودار فقط مسیر اصلی را نشان می‌دهد. [[40-block]] ساخت بلوک و [[lab.html|آزمایشگاه مدل]] نمایش قابل بررسی آن را دارند.</p>
<h2>نخستین آموزش و تولید</h2>
<p>این دستورها برای پایان بخش هشتم‌اند. معنای همهٔ گزینه‌ها در راهنمای پروژه و درس‌های آن بخش آمده است. پوشهٔ خروجی تازه انتخاب کنید؛ آموزش تازه پوشهٔ پُر را بازنویسی نمی‌کند.</p>
<pre><code>python -m mini_gpt.train --text data/sample.txt --output runs/first --steps 100 --context-length 16 --embedding-dim 32 --num-heads 4 --num-layers 2 --batch-size 8 --eval-every 20 --device cpu
python -m mini_gpt.evaluate --checkpoint runs/first/best.pt --text data/sample.txt --device cpu
python -m mini_gpt.train --text data/sample.txt --output runs/first --resume runs/first/last.pt --steps 150 --device cpu
python -m mini_gpt.generate --checkpoint runs/first/best.pt --prompt "مدل " --tokens 80 --temperature 0.9 --top-k 10 --device cpu</code></pre>
<p>دادهٔ نمونه عمداً کوچک است. با این اجرای کوتاه بررسی می‌کنیم که آموزش، ارزیابی و تولید درست به هم وصل شده‌اند؛ از آن انتظار یک مدل مسلط به زبان نداریم. پیکرهٔ شخصی را با اجازه و بدون اطلاعات حساس تهیه کنید. واژگان فقط از بخش آموزش ساخته می‌شود؛ ناشناخته‌های ارزیابی جدا گزارش می‌شوند.</p>
<p>نرخ پیش‌فرض ثابت است. برای آزمایش برنامهٔ نرخ، در اجرای تازه گزینه‌های <code>--schedule cosine --warmup-steps 10 --decay-steps 100 --min-lr-ratio 0.1</code> را اضافه کنید. پایان کاهش مستقل از تعداد گام اجرای فعلی است و هنگام ادامه از ذخیره برمی‌گردد؛ [[49b-schedule]] دلیل این قرارداد را توضیح می‌دهد.</p>
<h2>عددهای همین مدل را ببینید</h2>
<p>پس از آموزش، ابزار inspect همان مسیر واقعی forward را با حذف تصادفی خاموش ثبت می‌کند: نشانه‌ها و شناسه‌ها، نمایش‌ها، Q/K/V، امتیاز خام/مقیاس‌شده، پوشش، وزن توجه، امتیاز خروجی، احتمال و چند انتخاب تازه. این فایل تصویر ساختگی از معماری نیست؛ نام و اثر انگشت checkpoint و شمارهٔ گام را هم دارد.</p>
<pre><code>python -X utf8 -m mini_gpt.inspect --checkpoint runs/first/best.pt --prompt "مدل " --output runs/first/inspection.json --layer 0 --head 0 --max-tokens 16 --generate-tokens 4 --metrics runs/first/metrics.csv</code></pre>
<p>فایل inspection.json را در [[lab.html|آزمایشگاه مدل]] وارد کنید. بلوک و سر از صفر شماره‌گذاری می‌شوند. تعداد نشانه‌های بررسی حداکثر ۶۴ و تولید کوتاه از صفر تا چهار نشانه است؛ زمینهٔ تولید این ابزار هم به --max-tokens محدود می‌شود. گزارش دلخواه metrics.csv منحنی‌های اجرای شما را همراه می‌آورد. برای خروجی نام تازه انتخاب کنید؛ تنها با <code>--force</code> می‌توان بازنویسی صریح خواست. هرگز checkpoint ناشناس بارگذاری نکنید.</p>
<p>از یک سطر توجه شروع کنید: آیا بالای قطر صفر است؟ جمع وزن‌ها چقدر است؟ چرا گزینهٔ برگزیده همیشه بیشترین احتمال خام را ندارد؟ فرق احتمال مدل و احتمال پس از قانون انتخاب را در [[55-temperature]] و [[56-topkp]] مرور کنید و مشاهده را در [[journal.html|دفتر آزمایش]] ثبت کنید. وزن توجه به‌تنهایی دلیل کامل پاسخ مدل نیست.</p>
<h2>آزمایش‌ها و ادامهٔ مستقل</h2>
<p><code>python -m mini_gpt.experiments future</code> نشت آینده را آشکار می‌کند. نام‌های دیگر: <code>attention</code> محاسبهٔ عددی Attention، <code>positions</code> حذف موقعیت، <code>residual</code> حذف مسیر جمع، <code>normalization</code> مقایسهٔ نرمال‌سازی با تبدیل همانی، <code>gradients</code> گام‌های نامناسب، <code>shapes</code> خطای نوع و شکل، <code>overfit</code> حفظ یک Batch، <code>sampling</code> آزمایش Temperature و <code>network</code> اولین شبکه. پیش از اجرا [[58-ablation]] و [[60-bug-clinic]] را بخوانید؛ نتیجهٔ یک Batch تصادفی را حکم عمومی کیفیت معرفی نکنید.</p>
<p>در پایان، فقط یک قابلیت اضافه کنید و برایش آزمون بنویسید. گزارش خوب نتیجهٔ منفی و محدودیت را هم نگه می‌دارد. [[67-rag]] پایان نظری کتاب است، نه پایان پروژهٔ شما.</p>
'''

API_ROWS = [
    ("import torch / from torch import nn / functional as F", "torch نام کتابخانه، nn مجموعهٔ لایه‌ها، و F نام کوتاه دلخواه برای تابع‌های بدون شیء لایه است. مثال: F.cross_entropy تابع Loss را صدا می‌زند؛ F متغیر ریاضی نیست.", "13-torch"),
    ("tensor / zeros / ones / randn / arange", "tensor از مقدارها می‌سازد؛ zeros و ones شکل داده‌شده را با صفر و یک پُر می‌کنند؛ randn عدد نرمال با میانگین صفر و انحراف معیار یک می‌سازد؛ arange(4) برابر [0,1,2,3] است. torch.manual_seed(7) جریان تصادف همین محیط را از بذر مشخص شروع می‌کند.", "13-torch"),
    ("shape / ndim / numel / size / dtype / device", "برای x با شکل (2,3)، ndim=2 و numel()=6 است. size(1)=3 اندازهٔ محور دوم است. dtype نوع عدد و device محل اجرا را نشان می‌دهند. long برای شناسه و float32 برای محاسبهٔ اعشاری‌اند.", "14-index-device"),
    ("to / float / long / cpu / tolist / item", "x.to(device) تنسور مناسب دستگاه مقصد را برمی‌گرداند؛ نتیجه را به متغیر بدهید. float و long نوع عدد را تبدیل می‌کنند. cpu انتقال به پردازندهٔ مرکزی است. tolist نسخهٔ فهرستی و item مقدار یک تنسور تک‌عنصری را می‌دهد. tolist و item برای گزارش خروجی مناسب‌اند؛ item را جای خود loss برای backward نگذارید.", "14-index-device"),
    ("dim / keepdim / sum / mean / var / std / norm", "dim محور عملیات است و -1 آخرین محور. keepdim=True محور خلاصه‌شده را با اندازهٔ یک نگه می‌دارد. sum جمع، mean میانگین، var واریانس، std انحراف معیار و norm اندازهٔ بردار را حساب می‌کنند. برای واریانس جمعیتی correction=0 می‌گذاریم؛ LayerNorm همین قرارداد را دارد.", "15-broadcast"),
    ("reshape / view / transpose / permute / contiguous", "reshape(2,3) همان شش عنصر را گروه‌بندی می‌کند. transpose(0,1) دو محور و permute ترتیب همهٔ محورها را عوض می‌کند. view نیازمند چیدمان سازگار است؛ contiguous در صورت نیاز کپی پیوسته می‌سازد. ترتیب داده را با مثال غیرمربعی بسنجید.", "16-reshape"),
    ("unsqueeze / squeeze / None / cat / stack / chunk", "unsqueeze(0) یا x[None] محور یک‌تایی اضافه می‌کند. squeeze فقط محور یک‌تایی را حذف می‌کند. cat تنسورها را در محور موجود می‌چسباند؛ stack محور تازه می‌سازد. در QKV پروژه، chunk(3,dim=-1) محور آخرِ 3Cتایی را به سه قطعهٔ Cتایی تقسیم می‌کند؛ آن را با جابه‌جایی محور اشتباه نگیرید.", "35-split-heads"),
    ("@ / matmul / sqrt / exp / log / softmax", "@ و matmul برای دو بردار ضرب داخلی و برای ماتریس‌ها ضرب ماتریسی انجام می‌دهند؛ در دستهٔ ماتریس‌ها، دو محور آخر محورهای ضرب‌اند. sqrt ریشهٔ دوم، exp نمایی و log لگاریتم طبیعی است؛ نسخهٔ math این سه تابع عدد تکی و نسخهٔ torch تنسور می‌گیرد. softmax(dim=-1) امتیازهای هر سطر را به احتمال با جمع یک تبدیل می‌کند.", "09-softmax"),
    ("requires_grad / grad / backward / no_grad / detach", "requires_grad=True این Tensor را برای محاسبهٔ گرادیان علامت می‌زند؛ Autograd عملیات وابسته را در گراف ثبت می‌کند. backward گرادیان را حساب و در grad جمع می‌کند. no_grad ثبت این گراف را در یک محدوده خاموش می‌کند؛ detach اتصال یک تنسور به مسیر قبلی را قطع می‌کند، نه اینکه وزن‌ها را آموزش بدهد.", "17-autograd"),
    ("nn.Module / nn.Parameter / forward / parameters / apply", "Module ظرف لایه‌هاست؛ model(x) مسیر استاندارد فراخوانی forward را اجرا می‌کند. Parameter تنسور ثبت‌شدهٔ پارامتر است. parameters پارامترهای ثبت‌شده را می‌دهد و named_parameters نامشان را هم؛ پارامتر با requires_grad=False نیز می‌تواند در این فهرست باشد. apply(fn) تابع fn را روی مدل و زیرلایه‌ها اجرا می‌کند؛ پروژه برای مقداردهی اولیه از آن استفاده می‌کند.", "18-module"),
    ("nn.Linear(in,out,bias=True)", "برای هر بردار inتایی، ضرب در وزن و در صورت درخواست جمع با bias انجام می‌دهد. ورودی (...,in) به (...,out) می‌رود؛ weight شکل (out,in) دارد. از نظر ریاضی با bias یک تبدیل affine یعنی خطی به‌علاوهٔ ثابت است.", "18-module"),
    ("nn.Sequential / nn.ModuleList", "Sequential لایه‌ها را به ترتیب خودش اجرا می‌کند. ModuleList آن‌ها را ثبت می‌کند ولی حلقهٔ اجرا را شما می‌نویسید. یک list معمولی جایگزین ثبت زیرلایه‌ها برای parameters و to نیست.", "41-stack"),
    ("ReLU / Tanh / GELU", "توابع فعال‌سازی‌اند: ReLU منفی را صفر می‌کند، Tanh خروجی را نرم بین -1 و 1 می‌برد، و GELU ورودی را نرم با تابع تجمعی نرمال وزن می‌دهد. بدون تابع غیرخطی، ترکیب تبدیل‌های affine هنوز affine است.", "19-network"),
    ("torch.optim.SGD / lr", "SGD بدون گزینه‌های افزوده، گام w←w−lr×grad را پیاده می‌کند؛ مثال SGD(model.parameters(),lr=0.01). تصادفی یا کوچک‌دسته‌بودن از روش انتخاب داده در حلقه می‌آید، نه اینکه این شیء خودش داده انتخاب کند. قاعدهٔ ساده را در درس نزول گرادیان کوچک‌دسته‌ای می‌سازیم.", "12-sgd"),
    ("Adam / AdamW / zero_grad / step", "Adam میانگین‌های متحرک مشتق و مربع مشتق را برای تنظیم گام هر پارامتر نگه می‌دارد؛ AdamW کاهش وزن جداگانه دارد. zero_grad مشتق قبلی را پاک می‌کند؛ step پارامترها را با وضعیت فعلی به‌روز می‌کند. lr نرخ پایه و weight_decay شدت کشش وزن به صفر است.", "47-loop"),
    ("F.cross_entropy", "امتیازهای خام (N,V) و شناسه‌های هدف (N,) را می‌گیرد و پیش‌فرض میانگین -log احتمال کلاس درست را می‌دهد. کلاس در محور دوم است؛ کتاب B و T را برای خوانایی ادغام می‌کند. پیش از آن softmax نگذارید.", "43-lm-head"),
    ("Dataset / DataLoader / __len__ / __getitem__", "Dataset قرارداد مجموعهٔ نمونه‌هاست: len تعداد، و [i] نمونهٔ i را می‌دهد. DataLoader نمونه‌ها را به دسته جمع می‌کند. batch_size تعداد نمونه در دسته و shuffle جابه‌جایی تصادفی ترتیب است. collate پیش‌فرض یعنی روی‌هم‌گذاشتن نمونه‌های هم‌شکل.", "20-loader"),
    ("nn.Embedding(V,C)", "جدول V سطر و C ستون است؛ ورودی شناسه‌ای (B,T) سطرها را انتخاب می‌کند و خروجی (B,T,C) می‌سازد. وزن‌های جدول یادگرفتنی‌اند، اما خود شمارهٔ سطر مشتق نمی‌گیرد.", "25-embedding"),
    ("tril / bool / masked_fill / register_buffer", "tril مثلث پایین، شامل قطر را نگه می‌دارد. bool نوع درست/نادرست است. masked_fill(mask,-inf) محل‌های True را با منفی بی‌نهایت پُر می‌کند. register_buffer یک Tensor کمکیِ غیرپارامتری، مثل Mask را همراه مدل ثبت و جابه‌جا می‌کند، بدون تبدیل آن به پارامتر.", "33-mask"),
    ("nn.Dropout / train / eval", "Dropout(p) در آموزش بعضی مقدارها را صفر و باقی را بر 1-p تقسیم می‌کند. train آن را فعال و eval خاموش می‌کند. این دو روش، ثبت گراف Autograd را خاموش نمی‌کنند؛ برای ارزیابی از no_grad هم استفاده می‌شود.", "34-causal-test"),
    ("nn.LayerNorm(C) / eps", "در هر موقعیت میانگین و واریانس C ویژگی را حساب می‌کند و با ضریب و ثابت یادگرفتنی برمی‌گرداند. eps عدد کوچک مثبت داخل ریشه برای جلوگیری از تقسیم بر صفر است. محور زمان را با ویژگی اشتباه نگیرید.", "39-layernorm"),
    ("nn.init.normal_ / zeros_ / copy_ / زیرخط پایانی", "normal_ وزن موجود را با عدد نرمال پُر می‌کند؛ zeros_ با صفر، و copy_ مقدار تنسور دیگر را کپی می‌کند. پسوند _ معمولاً تغییر درجا را نشان می‌دهد. تغییر دستی وزن قابل مشتق را داخل no_grad انجام دهید.", "44-parameters"),
    ("isfinite / all / allclose / equal / abs / max / clone / count_nonzero", "isfinite می‌گوید مقدار نه بی‌نهایت و نه NaN است؛ NaN یعنی نتیجهٔ عددی تعریف‌نشده. all درست‌بودن همهٔ خانه‌ها را می‌سنجد. equal برابری دقیق و allclose برابری تقریبی را. abs قدرمطلق، max بیشینه و clone کپی مستقل داده می‌سازد؛ clone به‌تنهایی اتصال مشتق را قطع نمی‌کند. count_nonzero تعداد خانه‌های غیرصفر را می‌شمارد.", "46-gradient-path"),
    ("clip_grad_norm_", "اندازهٔ مشترک همهٔ مشتق‌ها را در صورت عبور از سقف با یک ضریب کوچک می‌کند. پس از backward و پیش از step استفاده می‌شود. خروجی تابع اندازهٔ پیش از برش است، نه پس از آن.", "49-rate"),
    ("ScheduleConfig / learning_rate(base_rate, step)", "این کلاس در mini_gpt.schedule تعریف شده و API خود پروژه است. نرخ گام شمارهٔ step را از نرخ اوج و برنامهٔ ذخیره‌شده محاسبه می‌کند؛ step از ۱ شروع می‌شود. پایان کاهش در decay_steps ثابت است، نه طول اجرای فعلی. گزینه‌های فرمان --schedule و --warmup-steps همین قرارداد را تنظیم می‌کنند.", "49b-schedule"),
    ("state_dict / load_state_dict / torch.save / torch.load", "state_dict نگاشت نام‌ها به وضعیت مدل یا بهینه‌ساز است؛ load_state_dict آن را بازمی‌گرداند. save فایل می‌نویسد؛ load با map_location='cpu' ابتدا روی CPU می‌خواند. پروژه weights_only=True را صریح می‌گذارد تا نوع‌های قابل بارگذاری محدود باشند. این محافظ جای اعتماد به منبع را نمی‌گیرد؛ checkpoint ناشناس را هرگز بارگذاری نکنید.", "50-checkpoint"),
    ("Generator / randint / get_rng_state / set_rng_state", "Generator مولد عدد تصادفی جداست؛ randint اندیس صحیح تصادفی می‌دهد. rng مخفف مولد عدد تصادفی است. get و set وضعیت جریان را ذخیره و بازیابی می‌کنند؛ فقط بذر اولیه برای ادامهٔ وسط آموزش کافی نیست.", "51-resume"),
    ("topk / sort / cumsum / scatter / full_like / zeros_like", "topk مقدار و اندیس k بزرگ‌ترین را می‌دهد؛ sort مرتب می‌کند؛ cumsum مجموع تجمعی است. scatter مقدارها را در اندیس مقصد می‌گذارد؛ مثال: اندیس [2,0] و مقدار [8,9]، خانهٔ 2 را 8 و خانهٔ 0 را 9 می‌کند. full_like شکل ورودی را با یک مقدار و zeros_like با صفر می‌سازد.", "56-topkp"),
    ("argmax / multinomial", "argmax اندیس بیشترین امتیاز را می‌دهد و در تساوی اولین اندیس را می‌گیرد. multinomial با وزن‌های نامنفی نمونهٔ تصادفی انتخاب می‌کند؛ با num_samples=1 یک شناسه برای هر سطر می‌گیریم. خروجی انتخاب با خروجی خود شبکه متفاوت است.", "55-temperature"),
    ("trace / inspect_model / mini_gpt.inspect", "trace ابزار اختیاری خود پروژه است: ابتدا record={} بسازید و آن را به model(ids,trace=record) بدهید تا مقدارهای میانی همان forward در آن ثبت شوند. inspect_model این رد را برای مشاهده آماده می‌کند؛ فرمان mini_gpt.inspect مدل قابل اعتماد ذخیره‌شده را می‌خواند و JSON محلی می‌سازد. آزمایشگاه این گزارش را نمایش می‌دهد، نه اینکه checkpoint را در مرورگر اجرا کند.", "45-trace"),
    ("Path / argparse / csv / json / hashlib / dataclass", "ابزارهای استاندارد Python هستند: Path مسیر فایل، argparse خواندن گزینهٔ فرمان، csv نوشتن جدول متنی، json ذخیرهٔ ساختار ساده، hashlib اثر انگشت محتوا، و dataclass ساخت کلاس تنظیمات با فیلدهای مشخص. csv گزارش آموزش است، نه ورودی شبکه؛ json واژگان است، نه وزن مدل.", "24-data-contract"),
    ("B,T,C,H,D,V,L و نام‌های تابع پروژه", "B تعداد متن‌ها، T طول پنجره، C تعداد ویژگی، H تعداد سر، D=C/H ویژگی هر سر، V اندازهٔ واژگان و L تعداد بلوک است. نام‌هایی مانند build،prepare_corpus،evaluate،future تابع‌های تعریف‌شده در خود پروژه‌اند، نه API پنهان؛ از import بالای مثال، فایل تعریف را پیدا کنید.", "45-trace"),
]
API = '''<p class="objective">وسط خواندن کد به تابعی رسیده‌اید که کاربردش را یادتان نیست؟ این صفحه برای همین است. لازم نیست نام‌ها را از بر کنید؛ مهم‌تر این است که بدانید تابع چه می‌گیرد، چه برمی‌گرداند و چرا آن را به کار برده‌ایم.</p>
<p>API یعنی رابط برنامه‌نویسی: راه مشخصی برای صداکردن یک قابلیت. نام‌های انگلیسی کد را ترجمه نمی‌کنیم، چون باید عیناً قابل اجرا باشند؛ معنی آن‌ها را پیش از استفاده می‌آموزیم. مثال <code>x.mean(dim=-1,keepdim=True)</code> می‌گوید «میانگین آخرین محور را بگیر و محور را نگه دار».</p>
<p>نام‌های خطای رایج: <code>ValueError</code> یعنی مقدار ورودی با قرارداد سازگار نیست؛ <code>RuntimeError</code> خطای زمان اجرا؛ <code>ModuleNotFoundError</code> پیدا نشدن بسته؛ <code>KeyError</code> کلید غایب در دیکشنری. پیام انگلیسی را از روی نام متغیر و شکل‌های نمایش‌داده‌شده بخوانید؛ [[60-bug-clinic]] نمونهٔ تشخیص و اصلاح دارد.</p>
<table><tr><th>نام در کد</th><th>معنی، قرارداد و مثال</th><th>درس زمینه</th></tr>''' + ''.join(
    '<tr><td><code>'+name+'</code></td><td>'+meaning+'</td><td>[['+lesson+']]</td></tr>'
    for name,meaning,lesson in API_ROWS
) + '''</table><h2>برای خواندن جزئیات بیشتر</h2><p>برای فهم درس‌ها مراجعهٔ بیرونی لازم نیست. تعریف تابع Loss را در <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html">مستندات رسمی CrossEntropyLoss</a>، قرارداد بارگذاری را در <a href="https://docs.pytorch.org/docs/stable/generated/torch.load">torch.load</a> و معماری اصلی توجه را در <a href="https://arxiv.org/abs/1706.03762">مقالهٔ Attention Is All You Need</a> ببینید. مدل آموزشی ما نسخهٔ عین‌به‌عین معماری رمزگذار–رمزگشای آن مقاله نیست؛ از خانوادهٔ علّیِ فقط رمزگشا است.</p>'''
