from .schema import Lesson

LESSONS = [
Lesson("62b-lifecycle",10,"فراتر از رایانهٔ شخصی","از مدل پایه تا دستیار گفت‌وگویی",
"معماری، هدف آموزش و محصول را از هم جدا می‌کنیم تا معلوم شود چه ساخته‌ایم و چه چیزهایی هنوز باقی مانده است.",
"""<p>تا اینجا مدلی ساخته‌ایم که از روی گذشته، Token بعدی را پیش‌بینی می‌کند. این دستاورد با ساخت دستیار گفت‌وگویی کامل یکی نیست. GPT نام یک خانواده از مدل‌هاست؛ ChatGPT نام یک محصول است که مدل را در یک محیط گفت‌وگو، همراه با دستورها و امکانات دیگر به کار می‌گیرد. بنابراین ساخت Transformer علّی به‌تنهایی بازسازی ChatGPT نیست. <a href="https://developers.openai.com/api/docs/models/all">فهرست مدل‌ها</a> و <a href="https://learn.chatgpt.com/docs/use-chatgpt">راهنمای محصول</a> این دو سطح را جدا نشان می‌دهند.</p>
<p>در این بحث، Base model وضعیت مدل را پیش از تنظیم برای دستیاربودن توصیف می‌کند. <dfn>Foundation model (مدل بنیادین)</dfn> اصطلاح گسترده‌تری برای مدلی است که روی دادهٔ متنوع و در مقیاس بزرگ آموزش دیده و برای کارهای گوناگون قابل سازگارکردن است. این دو نام مترادف دقیق نیستند. Mini-GPT ما یک آزمایش کوچکِ یادگیری مدل پایه است؛ معماری Transformer به‌تنهایی آن را به مدل بنیادین عمومی تبدیل نمی‌کند. <a href="https://arxiv.org/abs/2108.07258">گزارش مدل‌های بنیادین</a> مبنای این تمایز است.</p>
<h2>یک مسیر رایج، نه دستور ساخت همهٔ دستیارها</h2>
<ol><li><b>Pretraining:</b> متن خام، هدفِ Token بعدی را فراهم می‌کند؛ وزن‌ها الگوهای آن داده را یاد می‌گیرند.</li><li><b>SFT:</b> نمونه‌های دستور و پاسخ، رفتار مطلوب را نشان می‌دهند و وزن‌ها دوباره آموزش می‌بینند.</li><li><b>Preference optimization:</b> دادهٔ مقایسهٔ پاسخ‌ها برای تنظیم رفتار به کار می‌رود. RLHF و DPO دو مسیر متفاوت برای این کارند؛ لازم نیست پشت‌سرهم اجرا شوند.</li></ol>
<p>اگر کاربر خلاصهٔ یک متن را بخواهد و مدل پاسخی روان اما بی‌ربط بدهد، توانایی تولید متن کافی نبوده است. <dfn>Alignment (هم‌راستاسازی رفتار)</dfn> در این بحث یعنی نزدیک‌کردن رفتار مدل به خواسته‌ها و معیارهای انسانیِ مشخص، مانند انجام درست درخواست، صداقت و ایمنی. باید روشن کنیم معیارها را چه کسانی و برای چه کاربردی تعیین کرده‌اند. SFT و آموزش مبتنی بر ترجیح ابزارهایی برای این هدف‌اند، نه گواهِ حل کامل آن؛ درستی پاسخ و رعایت محدودیت‌ها همچنان به ارزیابی جداگانه نیاز دارند. <a href="https://arxiv.org/abs/2203.02155">پژوهش InstructGPT</a> نمونه‌ای از این تلاش و محدودیت‌های باقی‌مانده است.</p>
<p>این مرحله‌ها را با دو ابزار دیگر مخلوط نکنید. LoRA به انتخاب پارامترهای قابل آموزش مربوط است و می‌تواند کنار هدفی مانند SFT به کار رود. RAG سندهای مرتبط را هنگام پاسخ‌دادن به ورودی اضافه می‌کند؛ در مرحلهٔ بازیابی و تولید، وزن‌ها را آموزش نمی‌دهد. درس‌های بعد جزئیات هرکدام را باز می‌کنند.</p>
<p>نقشهٔ این درس جزو مسیر اصلی فهم کتاب است. پیاده‌سازی آموزش توزیع‌شده، Cache و روش‌های تنظیم پیشرفته اختیاری است و در Mini-GPT فعلی پنهانی فعال نشده است.</p>""",
"چهار تغییر را دسته‌بندی کنید: آموزش روی متن خام؛ آموزش روی دستور و پاسخ؛ آموزش با انتخاب پاسخ بهتر؛ افزودن سند به ورودی هنگام پاسخ‌دادن. برای هرکدام نوع داده، تغییر یا ثبات وزن‌ها و یک محدودیت را بنویسید.",
"سه مورد نخست به‌ترتیب Pretraining، SFT و آموزش مبتنی بر ترجیح‌اند و وزن‌ها را تغییر می‌دهند. مورد چهارم زمینهٔ RAG است و هنگام پاسخ‌دادن وزن‌ها ثابت‌اند. هیچ‌کدام به‌تنهایی حقیقت، پوشش همهٔ موضوع‌ها یا پیروی بی‌نقص از درخواست را تضمین نمی‌کند.",
"چرا LoRA را نباید جایگزینِ نامِ هدف آموزش بدانیم؟ چرا Mini-GPT کوچک، ChatGPT نیست؟ آیا تمام‌شدن SFT برای ادعای موفقیت در Alignment کافی است؟",
"حدود پروژه روشن است: محاسبه و آموزش یک مدل زبان کوچک را می‌بینیم، نه تمام اجزای یک دستیار عمومی."),
Lesson("63-scale",10,"فراتر از رایانهٔ شخصی","مدل بزرگ چه هزینه‌های تازه‌ای دارد؟",
"فرمول‌ها آشنا می‌مانند، اما حافظه، داده و هماهنگی دستگاه‌ها به مسئله‌های اصلی مهندسی تبدیل می‌شوند.",
"""<p>پیاده‌سازی مهندسی این درس اختیاری است: هدف شناخت مرزهای مدل کوچک است، نه الزام به داشتن GPU یا Cluster. نقشهٔ مفهومی درس قبل بخشی از مسیر اصلی بود. مسیر اصلی ساخت و آموزش Mini-GPT در بخش قبل کامل شده است.</p><p>آیا با اضافه‌کردن چند صفر به C و L به یک دستیار بزرگ می‌رسیم؟ بخش مهمی از محاسبه آشناست، اما مهندسی و داده هم باید تغییر کنند. <dfn>Large language model یا LLM (مدل زبانی بزرگ)</dfn> در خانوادهٔ مولد مورد بحث ما، توزیع نشانهٔ بعدی را با پارامتر، داده و محاسبهٔ بسیار بیشتر می‌آموزد؛ همهٔ مدل‌های بزرگ الزاماً همین هدف را ندارند. <dfn>Scaling (افزایش هماهنگ مقیاس)</dfn> یعنی اندازهٔ مدل، حجم داده و بودجهٔ آموزش را با هم ببینیم، نه اینکه فقط یک عدد تنظیمات را بزرگ کنیم.</p>
<p>یک میلیون پارامتر float32 تقریباً چهار میلیون بایت برای خود وزن‌ها می‌خواهد. در آموزش، مشتق‌ها، دو وضعیت Adam و نمایش‌های میانی هم حافظه مصرف می‌کنند. بنابراین تخمین «تعداد پارامتر ضرب‌در چهار» فقط وزن است، نه حافظهٔ کامل آموزش.</p>
<p><dfn>Distributed training (آموزش توزیع‌شده)</dfn> کار را میان چند دستگاه تقسیم می‌کند. در موازی‌سازی داده، هر دستگاه بخشی از دسته و نسخه‌ای از مدل دارد و مشتق‌ها هماهنگ می‌شوند. در موازی‌سازی مدل، خود پارامترها یا لایه‌ها تقسیم می‌شوند. هزینهٔ ارتباط نیز مهم است؛ چند GPU خودبه‌خود چند برابر سرعت نمی‌دهد.</p>
<p><dfn>Mixed precision (دقت عددی ترکیبی)</dfn> بعضی محاسبات را با عددهای کم‌دقت‌تر و بخش‌های حساس را با دقت بیشتر انجام می‌دهد. float16 و bfloat16 هر دو قالب ۱۶بیتی‌اند، اما دامنه و دقت یکسان ندارند؛ کمترشدن بیت‌ها را با هم‌ارزبودن تمام قالب‌ها اشتباه نگیرید. در آموزش، علاوه بر ضرب‌ها باید پایداری مشتق و وضعیت بهینه‌ساز را هم مدیریت کرد. نسخهٔ آموزشی ما این مسیر را خودکار پیاده نکرده است.</p>
<p><dfn>Kernel (روال محاسباتی سطح پایین)</dfn> اجرای یک عملیات روی سخت‌افزار است. <dfn>FlashAttention (توجه سریع با مدیریت رفت‌وآمد حافظه)</dfn> خانواده‌ای از روش‌هاست که توجه دقیق را قطعه‌بندی می‌کنند تا خواندن و نوشتن بین سطح‌های حافظه کمتر شود. مسئلهٔ منطقی همان ضرب و وزن‌دهی است؛ جدول بزرگ الزاماً به شکل سادهٔ پروژهٔ ما در حافظه ساخته نمی‌شود. <a href="https://arxiv.org/abs/2205.14135">مقالهٔ اصلی</a> مسیر اختیاری است. فعلاً مهم است بدانید کدام هزینه از خود مسئله می‌آید و کدام از روش اجرای آن.</p>""",
"حافظهٔ خود وزن‌ها را برای ۱ میلیون و ۱۰۰ میلیون پارامتر float32 حساب کنید. سپس یک تخمین جدا برای وزن+مشتق+دو وضعیت float32 optimizer بنویسید.",
"وزن‌ها 4 MB و 400 MB ده‌دهی‌اند. چهار مجموعهٔ هم‌اندازه تقریباً 16 و 1600 MB می‌شوند؛ نمایش‌های میانی و جزئیات واقعی هنوز اضافه‌اند.",
"چرا کاهش دقت عددی می‌تواند حافظه را کم کند بی‌آنکه تعداد پارامترها عوض شود؟",
"پروژهٔ CPU همین قطعه‌های ریاضی را دارد؛ نیاز به خوشهٔ چند رایانه پیش‌نیاز فهم آن‌ها نیست."),
Lesson("64-cache",10,"فراتر از رایانهٔ شخصی","کدام محاسبهٔ تولید را می‌توان دوباره استفاده کرد؟",
"محاسبهٔ تکراری را پیدا می‌کنیم؛ فقط نتیجه‌ای را دوباره مصرف می‌کنیم که شرایط ساختنش عوض نشده باشد.",
"""<p>حلقهٔ سادهٔ تولید با هر نشانهٔ تازه، همهٔ زمینه را دوباره پردازش می‌کند. اگر وزن‌ها، شناسه‌ها و موقعیت‌های قبلی ثابت باشند، در مدل علّی و حالت ارزیابی، افزودن آینده نمایش گذشته را عوض نمی‌کند. <dfn>KV cache (حافظهٔ موقت کلید و مقدار)</dfn> از همین خاصیت استفاده می‌کند: K و V قبلی هر لایه را نگه می‌داریم و فقط محاسبهٔ موقعیت تازه را اضافه می‌کنیم.</p>
<p>برای هر لایه، هر کدام از K و V شکل (B,H,T,D) دارند. پرسش تازه (B,H,1,D) با همهٔ کلیدها ضرب می‌شود و امتیاز (B,H,1,T) می‌دهد. محاسبهٔ دوبارهٔ projection گذشته حذف می‌شود، اما مقایسه با گذشته و حافظهٔ ذخیره‌شده همچنان هزینه دارند.</p>
<p>در مدل ما پنجره هنگام بلندشدن از چپ بریده و موقعیت‌ها از صفر بازشماری می‌شود؛ در این وضعیت بردارهای قدیمی با موقعیت تازه یکسان نیستند. پس چسباندن سادهٔ cache به کد فعلی دقیق نیست. مدیریت موقعیت، حذف از حافظه و پوشش باید با هم طراحی شوند. به همین دلیل این بهینه‌سازی را به‌عنوان پیش‌نیاز پروژه وارد نکردیم.</p>
<p>راه دیگری برای کاهش هزینه، تغییر نمایش عددهاست. <dfn>Quantization (ذخیرهٔ تقریبی عددها با بیت کمتر)</dfn> وزن‌ها یا برخی نمایش‌ها را با بیت کمتر و ضرایب مقیاس ذخیره می‌کند. این کار تعداد پارامتر را کم نمی‌کند و با حذف بلوک فرق دارد. کیفیت و سرعت به روش و روال سخت‌افزاری وابسته‌اند، نه صرف برچسب «۴ بیت». حافظهٔ K/V و کم‌بیت‌سازی دو ابزار متفاوت‌اند و هیچ‌کدام در generate سادهٔ ما پنهانی فعال نیست.</p>
<p>در این قرارداد T تعداد کل موقعیت‌ها پس از افزودن Token تازه است. اگر Cache قبلی T_old موقعیت داشته باشد، طول تازه T_old+1 و Shape امتیاز Query تازه (B,H,1,T_old+1) است. نام‌گذاری طول قدیمی و تازه را هنگام پیاده‌سازی مخلوط نکنید.</p>""",
"برای B=1,H=4,T=128,D=24,L=3 تعداد عناصر K و V همهٔ لایه‌ها را حساب کنید. آن را در دو بایت برای ذخیرهٔ ۱۶بیتی ضرب کنید.",
"تعداد عناصر 2×1×4×128×24×3=73728 و حافظهٔ خام 147456 بایت است. این فقط cache است، نه وزن یا سربار سرویس.",
"چرا ثابت‌بودن گذشته در مدل علّی، شرط لازم برای استفادهٔ دوباره از K/V است؟",
"در پروژه فعلی محاسبهٔ دوباره عمدی است تا مسیر درست و خوانا بماند؛ افزودن cache پروژهٔ پیشرفتهٔ مستقلی خواهد بود."),
Lesson("65-sft",10,"از ادامه‌دهنده به دستیار","آموزش روی دستور چه چیزی را تغییر می‌دهد؟",
"ادامه‌دادن متن با پاسخ‌دادن به درخواست یکی نیست؛ نقش دادهٔ آموزشی را در این تغییر رفتار جدا می‌کنیم.",
"""<p>در Pretraining هم هدف داریم: خودِ متن، Token بعدی را در اختیارمان می‌گذارد. به همین دلیل این روش را Self-supervised learning می‌نامیم؛ لازم نیست انسان برای تک‌تک موقعیت‌ها پاسخ جداگانه بنویسد. در SFT نمونه‌ها رفتار مطلوب را نشان می‌دهند. در هر دو می‌توان از پیش‌بینی Token بعدی و Cross-Entropy استفاده کرد، اما داده و سهم موقعیت‌ها در Loss فرق می‌کند.</p><p>به یک ادامه‌دهنده بنویسید «این جمله را خلاصه کن: ...». ممکن است درخواست را پاسخ دهد، آن را ادامه دهد یا یک گفت‌وگوی خیالی بسازد؛ هدف اولیه‌اش لزوماً دستورپذیری نبوده است. <dfn>Pretraining (پیش‌آموزش)</dfn> آموزش اولیه روی متن فراوان با هدفی مانند نشانهٔ بعدی است. حاصل را <dfn>Base language model (مدل زبانی پایه)</dfn> می‌نامیم. <dfn>Fine-tuning (تنظیم تکمیلی)</dfn> ادامهٔ آموزش از وزن‌های موجود برای داده یا رفتار مشخص است.</p>
<p><dfn>Instruction tuning (تنظیم روی دستور)</dfn> از نمونهٔ درخواست و پاسخ استفاده می‌کند. <dfn>Supervised fine-tuning یا SFT (تنظیم تکمیلی با پاسخ مرجع)</dfn> یعنی نمونه‌های مطلوب به‌عنوان هدف مشخص در اختیار مدل‌اند. معماری الزاماً عوض نمی‌شود؛ توزیع داده و جاهایی که loss می‌گیریم تغییر می‌کند.</p>
<p>مثلاً ورودی «درخواست: دو جمع دو؟ پاسخ: چهار» است. معمولاً loss فقط روی بخش پاسخ گرفته می‌شود؛ بخش درخواست زمینه است. <dfn>Loss mask (پوشش انتخاب محل خطا)</dfn> مشخص می‌کند کدام targetها در میانگین وارد شوند. این با causal mask که جریان اطلاعات را می‌بندد فرق دارد.</p>
<p>هدف این مرحله یک <dfn>Instruction-following model (مدل پیرو دستور)</dfn> است، نه صرفاً ادامه‌دهندهٔ هر متن. اما نام مرحله تضمین نتیجه نیست: پاسخ مرجع غلط یا قالب نامناسب همان رفتار را آموزش می‌دهد. SFT به‌تنهایی درستی پاسخ‌ها را تضمین نمی‌کند. Mini-GPT کوچک ما نیز با چند نمونهٔ پرسش‌وپاسخ به دستیار عمومی تبدیل نمی‌شود؛ چنین آزمایشی فقط سازوکار تغییر هدف و داده را روشن می‌کند.</p>
<p>مرز دقیق را با پنج Token ببینیم: <code>[q0,q1,sep,a0,a1]</code>. دو Token اول درخواست، sep جداکننده و دو Token آخر پاسخ‌اند. پس از شیفت، <code>x=[q0,q1,sep,a0]</code> و <code>y=[q1,sep,a0,a1]</code> داریم. Loss mask برابر <code>[0,0,1,1]</code> است: خروجی موقعیت sep باید نخستین Token پاسخ a0 را پیش‌بینی کند، پس در Loss حساب می‌شود. Mask را بر اساس Target می‌سازیم، نه فقط برچسب بخش ورودی. Causal Mask مستقل است و همچنان آینده را می‌بندد.</p><p>حذف Loss درخواست، درخواست را از محاسبه حذف نمی‌کند. پاسخ همچنان از آن زمینه می‌خواند و خطای پاسخ می‌تواند از همین مسیر بر وزن‌های پردازش درخواست اثر بگذارد. اگر Loss چهار هدف <code>[0.8,0.4,0.6,1.0]</code> و Mask برابر <code>[0,0,1,1]</code> باشد، میانگین پاسخ <code>(0.6+1.0)/2=0.8</code> است؛ بر دو هدف انتخاب‌شده تقسیم می‌کنیم، نه چهار موقعیت اولیه. MiniGPT فعلی آرگومان Response mask ندارد و Target منفی را نمی‌پذیرد؛ دادن <code>-100</code> به آن، آموزش SFT را فعال نمی‌کند.</p>""",
"یک نمونهٔ درخواست/پاسخ را نشانه‌بندی کنید و برای هر target علامت بزنید که آیا در loss پاسخ حساب می‌شود. causal mask را جدا رسم کنید.",
"پوشش خطا برخی موقعیت‌ها را از میانگین کنار می‌گذارد؛ پوشش علّی همچنان گذشته/آینده را محدود می‌کند. دو قرارداد مستقل‌اند.",
"چرا اجازهٔ دیدن متن درخواست با حذف loss درخواست تناقض ندارد؟",
"هستهٔ Mini-GPT قابل استفاده برای چنین آزمایش کوچکی است، اما فرایند کامل آماده‌سازی داده و آموزش SFT در نسخهٔ فعلی پیاده نشده است."),
Lesson("65b-lora",10,"از ادامه‌دهنده به دستیار","برای تنظیم رفتار، لازم است همهٔ وزن‌ها تغییر کنند؟",
"یک اصلاح کم‌رتبهٔ کوچک می‌سازیم و دقیقاً می‌شماریم کدام عددها آموزش می‌بینند و کدام ثابت‌اند.",
"""<p>در تنظیم کامل، بهینه‌ساز می‌تواند همهٔ وزن‌ها را تغییر دهد. اگر مدل بزرگ باشد، نگهداری مشتق و تاریخچهٔ بهینه‌ساز برای همهٔ آن‌ها پرهزینه است. <dfn>Parameter-efficient fine-tuning یا PEFT (تنظیم تکمیلی با پارامترهای قابل آموزش کمتر)</dfn> نام خانواده‌ای از روش‌هاست که فقط بخشی کوچک‌تر را آموزش می‌دهند. این اصطلاح هدف یادگیری تازه‌ای مثل SFT نیست؛ SFT می‌تواند با تنظیم کامل یا با روشی کم‌پارامتر انجام شود.</p>
<p><dfn>Low-rank adaptation یا LoRA (سازگارسازی با اصلاح کم‌رتبه)</dfn> وزن پایهٔ W را ثابت نگه می‌دارد و اصلاح را با دو ماتریس باریک می‌سازد: ΔW=B A. <dfn>Rank (رتبهٔ ماتریس)</dfn> تعداد جهت‌های مستقل آن را بیان می‌کند؛ حاصل این ضرب حداکثر r جهت مستقل دارد. یعنی آزادی تغییر را محدود می‌کنیم، نه اینکه همهٔ تغییرهای ممکن را با همان ظرفیت ذخیره کنیم. <a href="https://arxiv.org/abs/2106.09685">مقالهٔ اصلی LoRA</a> این انتخاب را برای تنظیم مدل‌های زبانی بررسی می‌کند.</p>
<div class="math">W : (M,C), A : (r,C), B : (M,r)<br>y = xWᵀ + (α/r)(xAᵀ)Bᵀ</div><p>حرف <code>B</code> در این فرمول نام ماتریس اصلاح است، نه Batch size. SFT و DPO نوع داده و هدف یادگیری را تعیین می‌کنند؛ LoRA مشخص می‌کند کدام پارامترها اجازهٔ تغییر دارند و می‌تواند در کنار این هدف‌ها به کار رود.</p>
<p>این یک تمرین ابعاد است: با C=12، M=8 و r=2، وزن پایه 96 عدد دارد، اما A و B فقط 24+16=40 عدد قابل آموزش دارند. ضریب α/r اندازهٔ شاخهٔ اصلاح را کنترل می‌کند؛ در مثال ما آن را یک می‌گیریم. برای یک بردار ورودی دوازده‌ویژگی، شاخهٔ تازه اول دو عدد و سپس هشت عدد می‌سازد و به خروجی اصلی اضافه می‌کند. نمونهٔ کوچک ما فقط محاسبه را شفاف می‌کند؛ نسبت صرفه‌جویی در اندازه‌های دیگر متفاوت است.</p>
<p>در کد، W یک تنسور ثابت است و فقط A/B را به optimizer می‌دهیم. A تصادفی کوچک و B صفر آغاز می‌شوند، پس خروجی آغازین دقیقاً خروجی مدل پایه است. چرا هر دو را صفر نکنیم؟ چون در آن حالت مشتق هرکدام در عامل صفر دیگر ضرب می‌شود و این شاخه راهی برای شروع یادگیری ندارد. با B=0 و A غیرصفر، B در گام اول می‌تواند مشتق غیرصفر بگیرد؛ مشتق A در همان گام صفر است و این رفتار طبیعی است. برای این آزمایش عددی از میانگین مربع اختلاف با یک هدف ثابت استفاده می‌کنیم، نه خطای مدل زبان. <code>torch.count_nonzero</code> تعداد خانه‌های غیرصفر را می‌شمارد تا این پیش‌بینی را بسنجیم.</p>
<p>کم‌شدن پارامترهای قابل آموزش به معنی حذف مدل پایه از حافظه یا حذف محاسبهٔ آن نیست. همچنین رتبهٔ کم تضمین نمی‌کند هر وظیفه با همان کیفیت تنظیم کامل آموخته شود. پروژهٔ اصلی LoRA آمادهٔ استفاده ندارد؛ مثال مستقل زیر فقط مسیر مشتق و شمارش وزن‌ها را نشان می‌دهد. پیش از افزودنش به Q/K/V واقعی، قرارداد ذخیرهٔ وزن پایه و شاخهٔ اصلاح را هم طراحی کنید.</p>""",
"پیش از اجرا شکل دو خروجی میانی و تعداد پارامترهای قابل آموزش را بنویسید. سپس پیش‌بینی کنید در اولین backward کدام‌یک از W،A،B مشتق None، صفر یا غیرصفر خواهد داشت.",
"W ثابت است و grad=None دارد. A و B پارامترند؛ در گام اول A مشتق صفر می‌گیرد چون B صفر است، ولی B می‌تواند مشتق غیرصفر بگیرد. 40 پارامتر قابل آموزش داریم و وزن پایه پس از step عوض نمی‌شود. این شاهد فقط همین مثال عددی است، نه کیفیت تنظیم یک مدل زبان.",
"چه تفاوتی میان کاهش تعداد وزن‌های قابل آموزش و کاهش تعداد کل وزن‌هایی که برای تولید لازم‌اند وجود دارد؟",
"این مسیر اختیاری پس از Mini-GPT کامل است. اگر آن را گسترش دهید، ابتدا یک Linear را جایگزین و برابری خروجی آغازین و ثابت‌ماندن وزن پایه را آزمون کنید.",
code="""import torch
from torch import nn
torch.manual_seed(12)
W = torch.randn(8,12)
A = nn.Parameter(torch.randn(2,12)*0.01)
B = nn.Parameter(torch.zeros(8,2))
optimizer = torch.optim.Adam([A,B],lr=0.01)
x = torch.randn(3,12)
target = torch.ones(3,8)
base_before = W.clone()
base = x @ W.T
output = base + (x @ A.T) @ B.T
assert torch.allclose(output,base)
assert A.numel()+B.numel() == 40
loss = ((output-target)**2).mean()
loss.backward()
assert W.grad is None
assert A.grad is not None and torch.count_nonzero(A.grad).item() == 0
assert B.grad is not None and B.grad.norm().item() > 0
optimizer.step()
assert torch.equal(W,base_before)
assert torch.count_nonzero(B).item() > 0
print("trainable parameters:", A.numel()+B.numel())
print("base unchanged:", torch.equal(W,base_before))
"""),
Lesson("66-preference",10,"از ادامه‌دهنده به دستیار","ترجیح دو پاسخ چه سیگنالی به مدل می‌دهد؟",
"به‌جای یک پاسخ مرجع، دو پاسخ داریم و می‌دانیم کدام را ترجیح می‌دهیم؛ این شاهد تازه چه چیزی را آموزش می‌دهد؟",
"""<p>دو جواب به یک سؤال از نظر دستور زبان درست‌اند، اما یکی روشن‌تر، دقیق‌تر یا مفیدتر است. <dfn>Preference data (دادهٔ ترجیح)</dfn> این مقایسه را ثبت می‌کند: برای درخواست یکسان، کدام پاسخ ترجیح داده شده است؟ پیش از الگوریتم باید معیار روشن باشد؛ کوتاهی، حقیقت، ادب و ایمنی همیشه به یک انتخاب واحد نمی‌رسند.</p>

<p><dfn>Preference optimization (بهینه‌سازی بر اساس ترجیح)</dfn> احتمال پاسخ‌های ترجیح‌داده‌شده را نسبت به پاسخ‌های ضعیف‌تر شکل می‌دهد. <dfn>RLHF (یادگیری تقویتی با بازخورد انسانی)</dfn> خانواده‌ای است که از قضاوت انسانی برای ساخت سیگنال پاداش و اصلاح رفتار استفاده می‌کند. پاداش عددی برای بهتر یا بدتر بودن خروجی است؛ روش اصلاح آن با loss سادهٔ SFT متفاوت است.</p>

<p>در مسیر کلاسیک RLHF، انسان‌ها چند پاسخ به یک درخواست را مقایسه می‌کنند. سپس یک Reward Model آموزش می‌دهیم تا از درخواست و پاسخ، امتیازی نزدیک به این ترجیح‌ها بسازد. مدل زبانی پاسخ تولید می‌کند و با یک روش یادگیری تقویتی مانند PPO به سمت امتیاز بیشتر تغییر می‌کند؛ معمولاً فاصله از مدل مرجع نیز محدود می‌شود. این امتیاز پیش‌بینی ترجیح است، نه دستگاه سنجش حقیقت. <a href="https://arxiv.org/abs/2203.02155">مقالهٔ InstructGPT</a> یک نمونه از این مسیر است.</p>
<p>در <dfn>Direct preference optimization یا DPO (بهینه‌سازی مستقیم ترجیح)</dfn>، در صورت‌بندی اصلی، زوج پاسخ ترجیح‌داده‌شده و پاسخ دیگر مستقیماً در تابع هدف به کار می‌روند و مدل مرجع مبنای مقایسه است. مدل پاداش جدا و حلقهٔ تولید و به‌روزرسانی روش بالا لازم نیست؛ اما دادهٔ ترجیح و ارزیابی همچنان لازم‌اند. RLHF و DPO دو مسیر برای این مرحله‌اند، نه دو ایستگاه اجباری پشت‌سرهم. <a href="https://arxiv.org/abs/2305.18290">مقالهٔ اصلی DPO</a> این تفاوت را توضیح می‌دهد.</p>
<p>این سیگنال‌ها معیار طراحی‌شده را دنبال می‌کنند، نه حقیقت مطلق. اگر دادهٔ ترجیح پاسخ‌های مطمئن ولی غلط را تشویق کند، مدل ممکن است همان سبک را یاد بگیرد. ارزیابی مستقل همچنان ضروری است. پیاده‌سازی فرمول DPO ادامهٔ اختیاری مستقلی است، نه قطعهٔ مخفی Mini-GPT.</p>""",
"برای یک سؤال دو پاسخ بنویسید و سه معیار روشن برای ترجیح تعیین کنید. نشان دهید چگونه تغییر معیار، انتخاب را عوض می‌کند.",
"پاسخ معیارمحور باید دلیل قابل مشاهده داشته باشد: مثلاً صحت محاسبه، پوشش سؤال و روشنی. «دوست داشتم» برای سنجش قابل بازتولید کافی نیست.",
"چرا موفقیت در معیار پاداش الزاماً تضمین نمی‌کند همهٔ خواسته‌های واقعی کاربر برآورده شده‌اند؟",
"در پروژه فعلی فقط پیش‌آموزش کوچک برای نشانهٔ بعدی داریم؛ این درس جای آن را در سامانهٔ بزرگ‌تر روشن می‌کند."),
Lesson("67-rag",10,"از ادامه‌دهنده به دستیار","چرا آوردن سند با تغییر وزن‌ها فرق دارد؟",
"یک سند تازه را به زمینه اضافه می‌کنیم و فرق این کار با یادگیری وزن تازه را روشن نگه می‌داریم.",
"""<p>فرض کنید پاسخ در یادداشتی است که دیروز نوشته‌اید؛ لازم نیست برای هر سؤال ابتدا مدل را دوباره آموزش دهید. <dfn>Retrieval (بازیابی)</dfn> یعنی پیدا‌کردن سند مرتبط. <dfn>Retrieval-augmented generation یا RAG (تولید با کمک متن بازیابی‌شده)</dfn> قطعه‌های مرتبط را به زمینهٔ مدل اضافه می‌کند و سپس از مدل پاسخ می‌گیرد. در مرحلهٔ پاسخ‌دادن به این درخواست، وزن‌ها معمولاً تغییر نمی‌کنند؛ ورودی تازه شده است.</p>
<div class="flow">پرسش → یافتن قطعه‌های مرتبط → ساخت زمینه با سندها → مدل زبان → پاسخ و ارجاع</div>
<p>پیکان اول می‌تواند جست‌وجوی واژه‌ای یا مقایسهٔ بردارهای سند باشد. انتخاب قطعه‌های مرتبط و اندازهٔ آن‌ها جدا از معماری Transformer است. سند طولانی ممکن است جا برای خود سؤال یا پاسخ کم کند؛ محدودیت T هنوز وجود دارد.</p>
<p>RAG تضمین نمی‌کند مدل فقط از سند استفاده کند یا نتیجه درست باشد. سند نامرتبط، قدیمی یا ناسازگار می‌تواند پاسخ را خراب کند. ارجاع باید واقعاً جملهٔ پاسخ را پشتیبانی کند. تنظیم تکمیلی وزن‌ها رفتار مدل را تغییر می‌دهد؛ RAG اطلاعات زمینهٔ همین درخواست را. این دو می‌توانند مکمل باشند. <a href="https://arxiv.org/abs/2005.11401">مقالهٔ اصلی RAG</a> ترکیب حافظهٔ مدل و بازیابی را بررسی می‌کند؛ نمودار ما نمای سادهٔ مرحلهٔ استفاده است، نه همهٔ جزئیات آموزش آن مقاله.</p>
<p>برای جمع‌بندی به مدل خودتان برگردید: با یک متن آغازین، فایل بازرسی بگیرید؛ یک سطر توجه و سه احتمال برتر را در [[lab.html|آزمایشگاه مدل]] بخوانید؛ سپس فقط یک عامل مانند دما را تغییر دهید و نتیجه را در [[journal.html|دفتر آزمایش]] ثبت کنید. اگر می‌توانید بگویید چه چیزی عوض شد، چه چیزی ثابت ماند و کدام ادعا هنوز بی‌مدرک است، ابزار اصلی ادامهٔ مسیر را دارید.</p>
<p>پروژهٔ بعدی را کوچک انتخاب کنید: نشانه‌بندی زیرواژه، اشتراک وزن جدول ورودی و سر خروجی، یا حافظهٔ K/V. پیش از کدنویسی قرارداد ورودی/خروجی و یک آزمون بنویسید. تمام‌شدن فصل‌ها پایان سؤال‌ها نیست؛ اکنون می‌توانید سؤال دقیق‌تری از مدل بپرسید و برای جوابش آزمایش بسازید.</p>
<p>محتوای بازیابی‌شده دادهٔ بیرونی است، نه دستور معتبر برای سامانه. ممکن است اطلاعات نادرست یا متنِ شبیه فرمان داشته باشد. در کاربرد واقعی باید مرز دستور و سند، منبع و تازگی داده و ارزیابی پاسخ را جدا نگه داشت؛ تمرین کوچک این درس تضمین امنیت یا صحت عمومی نیست.</p><h2>ارزیابی دستیار فراتر از Loss است</h2><p>Perplexity نشان می‌دهد مدل روی دادهٔ مشخص، Tokenهای مرجع را چقدر محتمل می‌داند. این عدد به‌تنهایی درستی، ارتباط پاسخ با درخواست یا پشتیبانی ادعاها با منبع را نمی‌سنجد. برای دستیار، این معیارها و برخورد با اطلاعات ناکافی را جدا بررسی کنید. نمونه‌های ارزیابی باید از دادهٔ آموزش و انتخاب تنظیمات جدا باشند.</p><p>تمرین تکمیلی، با چهار پاسخ کاملاً فرضی و نه خروجی Mini-GPT: پاسخ درستِ «۲+۲=۴»؛ پاسخ روان اما غلطِ «۲+۲=۵»؛ پاسخ دارای منبعی که ادعا در آن نیست؛ پاسخ به دو سند متناقض بدون اشاره به تناقض. برای هر مورد، مشخص کنید دربارهٔ صحت چه می‌دانیم و کدام داوری دربارهٔ انجام درخواست یا اتکا به منبع، بدون دیدن درخواست و سند واقعی ممکن نیست. اطلاعات لازم برای آن داوری را فهرست کنید. یک نمرهٔ کلیِ «خوب» اختلاف این معیارها را پنهان می‌کند.</p>""",
"با سه سند کوتاه و شمارش واژه‌های مشترک، بازیابی ساده بسازید. قطعهٔ انتخاب‌شده را همراه سؤال در یک رشته قرار دهید. این فقط شبیه‌سازی مرحلهٔ بازیابی است، نه سامانهٔ کامل پاسخ‌گو.",
"رتبه‌بندی ساده محدودیت دارد: هم‌معنی‌ها را نمی‌فهمد و واژه‌های مشترکِ کم‌اهمیت ممکن است رتبه‌بندی را گمراه کنند. هدف جداکردن retrieval از generation است؛ مدل کوچک را بی‌دلیل دستیار اسناد معرفی نکنید.",
"کدام مشکلات با بهبود بازیابی حل می‌شوند و کدام به خود مدل یا دادهٔ آموزشی مربوط‌اند؟",
"گام نهایی: از صفر مسیر متن تا خطا و مسیر prompt تا نشانهٔ تازه را توضیح دهید و یک تغییر کوچک را با آزمون سالم اعمال کنید.",
code="""documents = ["مدل با داده آموزش می‌بیند", "باران امروز شدید است", "داده و خطا در آموزش مهم‌اند"]
question = "مدل با چه داده آموزش می‌بیند"
words = set(question.split())
ranked = sorted(documents,key=lambda text:len(words & set(text.split())),reverse=True)
context = "سند: " + ranked[0] + "\\nپرسش: " + question
print(context)
""",
review="یک هفته بعد، به نسخهٔ صفر برگردید و تفاوت توان آن و مدل نهایی را با سه مثال و شکل تنسورها توضیح دهید."),
]
