"""ایستگاه‌ها: تکلیف قابل تحویل، معیار عبور، و راه بازگشت."""

CHECKPOINTS = [
    ("یک ادامه‌دهنده، بدون شبکه", "03-counts", [
        "مفهومی: برای <code>baba</code> داده، ویژگی ورودی، هدف، پارامتر شمارشی و پیش‌بینی را جدا کنید. چرا پیش‌بینی نشانهٔ بعدی انتخاب میان گزینه‌هاست، نه حدس یک عدد پیوسته؟ تفاوت حفظ‌کردن و تعمیم را با متن خودتان توضیح دهید.",
        "محاسباتی: جدول جفت‌ها را بشمارید؛ به هر گزینه یک واحد اضافه کنید و سهم هر گزینه پس از <code>b</code> را حساب کنید.",
        "پیاده‌سازی: از یک متن ده‌سطری، هشت سطر را برای ساخت جدول و دو سطر را فقط برای بررسی کنار بگذارید. رفت‌وبرگشت شناسه‌ها را با <code>assert</code> بسنجید.",
        "پژوهشی: مدل را روی دو متن هم‌طول ولی با تکرار متفاوت بسازید. پنج ادامه با بذرهای یکسان مقایسه و توضیح دهید چرا خروجی آشنا الزاماً یادگیری زبان نیست."
    ], "تحویل: کد شمارش، جدول دستی، دو متن جدا و پنج خروجی ثبت‌شده. واژگان باید فقط از متن آموزش ساخته شده باشد؛ طبیعی است بعضی کاراکترهای ارزیابی قبلاً در آموزش هم دیده شده باشند. محدودیت زمینهٔ یک‌نشانه‌ای را با مثال نشان دهید و هر ادعای تعمیم را به دادهٔ ندیده وصل کنید.",
     "برای <code>baba</code> شمارش <code>b→a</code> برابر ۲ و <code>a→b</code> برابر ۱ است. در مثال دستی با فقط دو گزینهٔ <code>{b,a}</code>، پس از <code>b</code> وزن‌های هموارشدهٔ <code>b</code> و <code>a</code> برابر ۱ و ۳ و احتمال‌ها ۱/۴ و ۳/۴ هستند. Regression هدف کمّی مانند زمان خواندن دارد؛ Classification گزینه‌ای مانند شناسهٔ نشانه را انتخاب می‌کند. ویژگی مدل شمارشی، نشانهٔ فعلی و هدف آن نشانهٔ بعدی است؛ بزرگی عدد شناسه هدف عددیِ پیوسته نمی‌سازد. خروجی تجربی جواب ثابتی ندارد؛ تفاوت‌ها باید مستند باشند."),
    ("از امتیاز تا حرکت وزن", "09-softmax", [
        "مفهومی: فرق شناسه، بردار و احتمال را با سه شیء متفاوت نشان دهید. چرا ضرب داخلی فقط زاویه را نمی‌سنجد؟ توضیح دهید در SGD با Batchهای کوچک، کدام انتخاب تصادفی است و چرا یک گام ممکن است Loss کل داده را پایین نیاورد.",
        "محاسباتی: برای <code>logits</code>=[0,<code>log(2)</code>] احتمال و Loss هدف دوم را حساب کنید. سپس برای دو احتمال شرطی ۰٫۵ و ۰٫۲۵، احتمال دنباله و مجموع منفی لگاریتم‌ها را بیابید. برای L=(w-2)² در w=0 مشتق و گام نرخ ۰٫۱ چیست؟",
        "پیاده‌سازی: <code>softmax</code> پایدار را بدون PyTorch بنویسید؛ روی [1000,1001] و همان ورودی به‌اضافهٔ ۵۰۰ امتحان کنید.",
        "پژوهشی: نرخ‌های ۰٫۱، ۱ و ۱٫۱ را برای تابع مربعی ده گام اجرا کنید. به‌جای حکم کلی دربارهٔ همهٔ شبکه‌ها، رفتار همین تابع را از فرمول به‌روزرسانی توضیح دهید."
    ], "تحویل: محاسبات دستی، برنامهٔ <code>softmax</code> و جدول مسیر w. جمع احتمال‌ها باید نزدیک یک باشد و افزودن ثابت به امتیازها اثری نگذارد. حاصل‌ضرب احتمال‌های شرطی را به مجموع منفی لگاریتم احتمال‌ها وصل کنید و تفاوت یک دسته با کل داده را توضیح دهید.",
     "احتمال‌ها ۱/۳ و ۲/۳ و Loss هدف دوم حدود 0.4055 است. احتمال دنبالهٔ دوم ۰٫۱۲۵ و مجموع منفی لگاریتم‌ها <code>log(8)</code>≈2.0794 می‌شود؛ میانگین دو هدف نصف آن است. برای تابع مربعی، مشتق −۴ و وزن بعدی ۰٫۴ است. فاصلهٔ علامت‌دار وزن از جواب، یعنی w−2، هر بار در (1−2η) ضرب می‌شود؛ در η=1 نوسان بدون کاهش و در η=1.1 رشد دامنه داریم. دستهٔ تصادفی، برآوردی از گرادیان کل می‌دهد، نه تعهد به کاهش یکنواخت هر گام."),
    ("اولین شبکه‌ای که واقعاً یاد می‌گیرد", "19-network", [
        "مفهومی: بدون اجرا مسیر <code>forward</code>، <code>loss</code>، <code>backward</code> و <code>step</code> را توضیح دهید. کدام عدد پاسخ را می‌سازد و کدام وزن را تغییر می‌دهد؟ اگر شیء بهینه‌ساز SGD باشد، آیا خودش نمونه‌های تصادفی انتخاب می‌کند یا این مسئولیت حلقهٔ داده است؟",
        "محاسباتی: شکل ورودی چهار نمونهٔ دودویی، لایهٔ ۲→۸، و خروجی دو کلاس را بنویسید. تعداد پارامترهای دو لایهٔ دارای <code>bias</code> را حساب کنید.",
        "پیاده‌سازی: شبکهٔ مسئلهٔ «دو ورودی متفاوت‌اند؟» را از صفحهٔ سفید بنویسید. چهار پیش‌بینی، Loss ابتدا و انتها و تغییر واقعی یک وزن را ثبت کنید. یک بار <code>backward</code> را حذف کنید و تفاوت «مشتق داشتن» و «به‌روزرسانی شدن» را با شاهد نشان دهید.",
        "پژوهشی: Tanh را حذف کنید و با بذر و تعداد گام برابر دوباره آموزش دهید. چرا ترکیب دو تبدیل affine مرز غیرخطی لازم را نمی‌سازد؟"
    ], "تحویل: برنامهٔ مستقل و چهار پاسخ درست نسخهٔ غیرخطی، شکل‌های (4,2)→(4,8)→(4,2)، و توضیح نقش فعال‌سازی. اگر فقط کد کپی‌شده اجرا شد، پس از یک روز بدون مرجع بازنویسی کنید.",
     "تعداد پارامترها (2×8+8)+(8×2+2)=42 است. حذف تابع غیرخطی، ترکیب را به یک تبدیل آفین تقلیل می‌دهد؛ XOR با مرز خطی جدا نمی‌شود. انتخاب دسته وظیفهٔ حلقهٔ داده است؛ SGD با مشتق دریافت‌شده پارامترها را تغییر می‌دهد. نتیجهٔ یک آموزش ناموفق به‌تنهایی اثبات محدودیت نظری نیست."),
    ("قرارداد سالم متن", "23-shift", [
        "مفهومی: چرا بزرگی شناسه، شباهت معنایی نیست؟ یک کاراکترِ ندیده چطور به Unknown token تبدیل می‌شود؟",
        "محاسباتی: برای شناسه‌های [1,2,3,4,5] و T=3، همهٔ جفت‌های <code>x</code>/y را بنویسید. برای V=20 و C=8 شکل جدول و تعداد وزن‌های نمایش را حساب کنید.",
        "پیاده‌سازی: متن را پیش از ساخت واژگان جدا کنید، دو پنجره بسازید و نمایش نشانه و موقعیت را جمع کنید. مرحله‌های اجرایی ۱ تا ۴ را با <code>python -m mini_gpt.milestones --stage N</code> ببینید و یکی از خروجی‌ها را با محاسبهٔ خود تطبیق دهید؛ N شمارهٔ مرحله است.",
        "پژوهشی: یک کاراکتر را فقط در بخش ارزیابی بگذارید و نرخ ناشناخته را اندازه بگیرید. سپس نشانه‌بندی کاراکترمحور را با یک ادغام زیرواژه‌ای دستی از نظر طول دنباله مقایسه کنید."
    ], "تحویل: رفت‌وبرگشت متن شناخته‌شده، رفتار مشخص کاراکترِ ندیده، دو جفت درست، و نمایش با شکل (B,T,C). اگر واژگان را از کل متن ساخته‌اید یا <code>y=x</code> گذاشته‌اید، پیش از ادامه علت اشتباه را پیدا و اصلاح کنید.",
     "جفت‌ها [1,2,3]→[2,3,4] و [2,3,4]→[3,4,5] هستند. جدول نمایش (20,8) و دارای 160 وزن است. آخرین نمونه هم باید با هدف یک خانه جلوتر ساخته شود؛ هدف بیرون متن مجاز نیست."),
    ("توجه را هم حساب کنید، هم به چالش بکشید", "29-scores", [
        "مفهومی: نقش Query، Key و Value را با یافتن اطلاعات در چند کارت توضیح دهید. سپس مسیر رسیدن اطلاعاتِ موقعیت‌های دور در RNN را با خواندن مستقیم چند موقعیت در توجه مقایسه کنید؛ Gate در LSTM/GRU چه چیزی را آسان‌تر می‌کند و چه چیزی را تضمین نمی‌کند؟",
        "محاسباتی: مثال Q,K,V درس امتیازها را کامل کنید: ترانهاده، تقسیم بر √2، Causal Mask، <code>softmax</code> و جمع وزن‌دار.",
        "پیاده‌سازی: Single-Head Attention بدون Mask و سپس علّی را بنویسید. فقط دو نشانهٔ آخر را عوض کنید و خروجی گذشته را بسنجید. گزارش مرحلهٔ اجرایی ۵ را با ۶ مقایسه کنید؛ مجموع وزن‌های آینده در کدام باید صفر باشد؟",
        "پژوهشی: Loss آموزش نسخهٔ بدون Mask را با تولید واقعی مقایسه کنید. توضیح دهید چرا نشت پاسخ می‌تواند معیار ظاهراً خوبی بسازد که در تولید تکرارشدنی نیست."
    ], "تحویل: ماتریس‌ها، جمع سطر احتمال برابر یک، وزن‌های بالای قطر صفر، و آزمون عدم تغییر گذشته در حالت <code>eval</code>. بدون توضیح فرق «دسترسی به آینده» و «پیش‌بینی آینده» ادامه ندهید.",
     "QKᵀ=[[1,0],[1,1]] است. پس از Mask، وزن‌ها [[1,0],[0.5,0.5]] و خروجی [[10,0],[5,10]] می‌شوند. K متقارن نیست؛ حذف ترانهاده پاسخ را عوض می‌کند. حالت بازگشتی از زنجیرهٔ گام‌ها می‌گذرد و Gate حفظ یا بازنویسی آن را کنترل می‌کند؛ توجه مسیر خواندن مستقیم می‌سازد، در عوض هزینهٔ جفت‌های موقعیت را می‌پردازد. هیچ‌کدام تضمین فهم انسانی نیست."),
    ("یک بلوک را بدون نگاه‌کردن به کد ردیابی کنید", "40-block", [
        "مفهومی: کدام عملیات میان موقعیت‌ها اطلاعات جابه‌جا می‌کند و کدام فقط ویژگی‌های یک موقعیت را تغییر می‌دهد؟ مسیر جمع چه راه مستقیمی می‌سازد؟",
        "محاسباتی: برای B=2,T=5,C=12,H=3، شکل Q، امتیازها، خروجی ترکیب Headهای Attention و لایهٔ میانی FFN را بنویسید.",
        "پیاده‌سازی: مرحله‌های ۷ تا ۱۲ را به قطعه‌های کد وصل کنید. تقسیم و اتصال Headها را با دادهٔ ترتیبی و <code>assert</code> معکوس‌بودن آزمایش کنید؛ سپس یک بلوک با نرمال‌سازی پیش از زیرلایه و دو بلوک با وزن مستقل بسازید.",
        "پژوهشی: حذف مسیر جمع و جایگزینی نرمال‌سازی با تبدیل همانی را در دو آزمایش جدا امتحان کنید. اندازهٔ Gradient، پراکندگی امتیازها و Loss را ثبت کنید؛ چرا یک دستهٔ تصادفی برای حکم کیفیت آموزش کافی نیست؟"
    ], "تحویل: نمودار دو میان‌بُر، پنج شکل درست و خروجی دو بلوک. نرمال‌سازی روی محور زمان و <code>reshape</code> بدون جابه‌جایی Headها را عمداً بسازید؛ رفع پیام خطا کافی نیست، معنای محورها را با آزمون تأیید کنید.",
     "D=4؛ Q:(2,3,5,4)، امتیاز:(2,3,5,5)، خروجی توجه:(2,5,12)، میانی پیش‌خور:(2,5,48)، خروجی بلوک:(2,5,12). حذف نمایش موقعیت، تمام اطلاعات ترتیب را الزاماً حذف نمی‌کند؛ Causal Mask هنوز نامتقارن است."),
    ("از شناسه تا مشتق", "45-trace", [
        "مفهومی: چرا خروجی مدل برای هر موقعیت V امتیاز دارد؟ چرا نباید پیش از <code>cross_entropy</code> روی آن <code>softmax</code> بگذاریم؟",
        "محاسباتی: تعداد پارامترهای V=12,Tmax=8,C=16,L=2 را از اجزا جمع بزنید، نه فقط از دستور <code>numel</code>.",
        "پیاده‌سازی: یک دستهٔ (2,5) را وارد مدل کنید. <code>logits</code>:(2,5,12)، Loss تک‌عددی و مشتق پارامترهای استفاده‌شده را وارسی کنید. مرجع [[45-trace|ردیابی مسیر مدل]] را از روی جریان واقعی بازنویسی کنید؛ ورودی توجه باید بعد از جمع موقعیت و نرمال‌سازی باشد.",
        "پژوهشی: مسیر <code>token_embedding</code> تا <code>loss</code> را با نام فایل‌ها رسم کنید. یک <code>detach</code> عمدی وسط مسیر بگذارید و از روی <code>grad</code>های <code>None</code> محل قطع را پیدا کنید."
    ], "تحویل: جدول شکل‌ها، شمارش مستقل پارامتر، برابری مسیر دستی با <code>forward</code> و نتیجهٔ بررسی رسیدن مشتق به پارامترها. یک سطر توجه و سه احتمال آخرین موقعیت را هم ثبت کنید؛ این مدل هنوز می‌تواند تصادفی باشد. متناهی‌بودن مشتق کافی نیست، صفر یا <code>None</code> شدن ناخواسته نیز باید توضیح داشته باشد.",
     "هر بلوک 12C²+13C پارامتر دارد. جمع مدل 2VC+TmaxC+<code>L(12C²+13C)</code>+2C=7104 است. در این معماری Bias مربوط به Language-<code>model</code> head و اشتراک وزن میان ورودی/خروجی نداریم؛ تغییر این قرارداد شمارش را عوض می‌کند."),
    ("آموزش قابل بازیابی", "51-resume", [
        "مفهومی: فرق <code>best.pt</code> و <code>last.pt</code> چیست؟ چرا وزن‌ها بدون واژگان، وضعیت بهینه‌ساز و برنامهٔ نرخ برای ادامهٔ همان آزمایش کافی نیستند؟ چرا پایان کاهش نرخ نباید با ادامهٔ اجرا دوباره تعریف شود؟",
        "محاسباتی: میانگین Loss یک Batch هشت‌هدفه با Loss برابر ۱ و یک Batch دوهدفه با Loss برابر ۳ را حساب کنید؛ پاسخ را با میانگین سادهٔ دو دسته مقایسه کنید.",
        "پیاده‌سازی: با برنامهٔ کسینوسی، Warmup پنج‌گامی و پایان کاهش در گام ۳۰، مدل را تا گام ۲۰ آموزش دهید، ارزیابی کنید و تا گام کل ۳۰ ادامه دهید. فایل گزارش، منحنی نرخ و ذخیره را نگه دارید. با <code>inspect</code> از checkpoint خودتان گزارش بگیرید و در آزمایشگاه وارد کنید.",
        "پژوهشی: اجرای ۳۰گامی پیوسته و اجرای ۲۰+۱۰ را با CPU و تنظیمات یکسان مقایسه کنید؛ پایان کاهش در هر دو ۳۰ باشد. سپس فقط پیکره را عوض کنید و پیام رد ادامه را بررسی کنید. مقایسهٔ وزن‌ها از مقایسهٔ Loss گرد‌شده دقیق‌تر است."
    ], "تحویل: دو گزارش، ذخیرهٔ قابل اعتماد، ارزیابی بدون تغییر وزن، دستور ادامه و فایل بازرسی. نرخ استفاده‌شده در هر گام و عددهای بازرسی باید به همان اجرا تعلق داشته باشند. هدف، چرخهٔ سالم و شواهد قابل بازسازی است، نه متن ادبی پس از چند گام.",
     "میانگین درست ۱٫۴ است؛ میانگین سادهٔ دو دسته، ۲، به دستهٔ کوچک بیش از سهمش وزن می‌دهد. ادامه وضعیت بهینه‌ساز، مولدهای تصادفی، شمارهٔ گام و تنظیم برنامهٔ نرخ را برمی‌گرداند؛ بذر اولیه کافی نیست. در این تمرین <code>--schedule cosine --warmup-steps 5 --decay-steps 30</code> را فقط در اجرای تازه بدهید. روی CPU و محیط همسان برابری اجرای پیوسته و قطعه‌قطعه آزموده شده؛ بین سخت‌افزارهای متفاوت تضمین بیت‌به‌بیت نداریم."),
    ("دفتر آزمایش شخصی", "62-journal", [
        "مفهومی: افزایش دما چه چیزی را تغییر می‌دهد و چه چیزی را آموزش نمی‌دهد؟ چرا ادامهٔ روان لزوماً پاسخ درست نیست؟",
        "محاسباتی: روی احتمال‌های [0.6,0.25,0.15] برای top-<code>p</code>=0.8 مجموعهٔ مجاز و توزیع بازنرمال‌شده را حساب کنید.",
        "پیاده‌سازی: از checkpoint خودتان با greedy، top-<code>k</code> و top-<code>p</code> خروجی بگیرید؛ متن آغازین، طول و بذرها را ثابت نگه دارید. سهم دنباله‌های سه‌کاراکتری تکراری را اندازه بگیرید و انسجام را جدا توصیف کنید. یک خرابی کارگاه را تا اصلاح در دفتر ثبت کنید.",
        "پژوهشی: برای Cهای 16 و 32 با T,H,L و بودجهٔ گام برابر آزمایش کنید. زمان، تعداد وزن، Loss ارزیابی و سه خروجی را ثبت کنید؛ افزایش پارامتر را با تضمین کیفیت اشتباه نگیرید."
    ], "تحویل: حداقل شش برگهٔ دفتر شامل پیش‌بینی، یک تغییر، شاهد عددی/متنی و محدودیت. از دفتر خروجی بگیرید و قابل خواندن‌بودن فایل پشتیبان را بررسی کنید. نتیجهٔ خلاف انتظار را حذف نکنید؛ بگویید آزمایش بعدی کدام ابهام را جدا می‌کند.",
     "دو گزینهٔ اول لازم‌اند تا مجموع ۰٫۸۵ از آستانه بگذرد. پس از بازنرمال‌سازی احتمال‌ها حدود ۰٫۷۰۵۹ و ۰٫۲۹۴۱ هستند. حذف گزینهٔ عبورکننده، تعریف این روش را عوض می‌کند. کیفیت خروجی آزمایش شما پاسخ یکتایی ندارد."),
    ("رفتار تازه، با چه شاهدی؟", "65a-sft-lab", [
        "مفهومی: Pretraining، SFT و هدف ترجیح را با نوع داده و سهم موقعیت‌های Loss مقایسه کنید. چرا LoRA انتخاب پارامتر قابل آموزش است، نه هدف تازه؟",
        "محاسباتی: برای توالی <code>[q0,q1,sep,a0,a1]</code> ورودی، Target و Mask پاسخ را بنویسید. شمار ۹۶ وزن پایه و ۴۰ وزن اصلاح مثال LoRA را مستقل حساب کنید.",
        "پیاده‌سازی: از وزن پایهٔ یکسان دو کپی بسازید؛ فقط یکی را با پاسخ مرجع تنظیم کنید. Loss پاسخ، خروجی خام و انجام درخواست را برای مثال‌های دیده‌شده و صورت‌بندی کنارگذاشته‌شده ثبت کنید.",
        "پژوهشی: یک پاسخ مرجع غلط وارد نسخهٔ جداگانهٔ داده کنید. کاهش Loss آن داده چه چیزی دربارهٔ حقیقت ثابت نمی‌کند؟ دادهٔ نهایی آزمون را برای انتخاب تنظیمات مصرف نکنید."
    ], "تحویل: جدول قبل/بعد، Mask هم‌راستا با Target، شاهد ثابت‌ماندن کپی پایه و یک شکست مدل. قابلیت عمومی ادعا نکنید.",
     "Mask برابر [0,0,1,1] است. هدف نخست پاسخ از موقعیت sep پیش‌بینی می‌شود. افت Loss شاهد نزدیک‌شدن توزیع به پاسخ‌های داده است؛ صحت و تعمیم معیار جدا دارند. RLHF و DPO دو مسیرند، نه دو مرحلهٔ اجباری."),
    ("پاسخ از کدام شاهد آمد؟", "71-grounding", [
        "مفهومی: بازیابی سند را از تولید پاسخ جدا کنید. سؤال بی‌پاسخ و سند متناقض چه رفتار متفاوتی می‌خواهند؟",
        "محاسباتی: برای سؤال و سه بردار سند، یک شباهت کسینوسی را دستی حساب کنید؛ سپس بودجهٔ ورودی با سهم جداگانهٔ پاسخ را بنویسید.",
        "پیاده‌سازی: چهار یادداشت درس را به قطعه‌های دارای شناسه تبدیل کنید. یک سؤال تازه، یک هم‌معنی و یک مورد بدون شاهد را با جست‌وجوی واژه‌ای و برداری مقایسه کنید. سند انتخاب‌شده و سند حذف‌شده از بودجه را گزارش کنید.",
        "پژوهشی: جمله‌ای شبیه فرمان در سند بگذارید. برنامه نباید از آن مجوز اجرای ابزار یا تغییر سیاست بگیرد. جدا بررسی کنید که آیا پاسخ واقعاً به شاهد تکیه دارد."
    ], "تحویل: پرسش، رتبه‌بندی، ورودی نهایی، پاسخ و ارجاع، یک خطای بازیابی و یک امتناع درست. Fixture را خروجی مدل معرفی نکنید.",
     "بردار صفر سیاست مشخص می‌خواهد. قطعه‌های هم‌پوشان شاهد مستقل نیستند. جاگذاری شناسهٔ منبع فقط ارجاع را معتبر می‌کند، نه استنتاج همهٔ جمله‌ها از آن منبع را. مرزبندی متن تضمین امنیت مدل نیست."),
    ("چه چیزی را نگه می‌داریم؟", "74-memory", [
        "مفهومی: وزن، Context Window، تاریخچه، خلاصه، KV Cache و حافظهٔ پایدار را با محل نگهداری و زمان تغییر مقایسه کنید.",
        "محاسباتی: برای چهار نوبت گفتگو، بودجه‌ای انتخاب کنید که فقط دو نوبت آخر جا شوند. کدام واقعیت قدیمی حذف می‌شود؟",
        "پیاده‌سازی: با دادهٔ ساختگی و فایل موقت، یک واقعیت با اجازه ذخیره، بارگذاری، اصلاح و حذف کنید. کاربر دوم نباید آن را ببیند. منبع خلاصه را نیز ثبت کنید.",
        "پژوهشی: خلاصه‌ای را که یک استثنا را حذف کرده، با اصل گفتگو مقایسه کنید. آیا بازیابی حافظهٔ غلط خطا را بیشتر می‌کند؟"
    ], "تحویل: چرخهٔ نگهداری کامل، آزمون جداسازی دو کاربر، نتیجهٔ بدون اجازه و فهرست اطلاعات ازدست‌رفته در خلاصه.",
     "تاریخچه در برنامه است و فقط هنگام درج در ورودی دیده می‌شود. خلاصه فشرده‌سازیِ بی‌اتلاف نیست. حافظهٔ انتخاب‌شده نیز باید در همان بودجهٔ محدود جا شود؛ ذخیرهٔ بیرونی مدل را دوباره آموزش نمی‌دهد."),
    ("محاسبه انجام شد یا فقط نوشته شد؟", "76-reasoning", [
        "مفهومی: متن فراخوانی، درخواست معتبر، اجرای واقعی و نتیجهٔ وارسی را به چهار مرحله جدا کنید.",
        "محاسباتی: هزینهٔ سه جلسهٔ ۲۵دقیقه‌ای و یک استراحت ۱۰دقیقه‌ای را دستی حساب کنید. یک Trace خوش‌ظاهر اما غلط بسازید و محل خطا را مشخص کنید.",
        "پیاده‌سازی: نام ابزار نامعتبر، آرگومان غلط و خروجی نامعتبر را امتحان کنید. درخواست معتبر را اجرا و جواب نهایی را با نتیجهٔ ثبت‌شده مقایسه کنید.",
        "پژوهشی: یک نامزد و چند نامزد را روی مجموعهٔ ثابت مقایسه کنید. هزینه و تعداد پاسخ درست را جدا ثبت کنید؛ یک رأی اکثریتِ غلط هم بسازید."
    ], "تحویل: محاسبهٔ ۸۵ دقیقه، خطاهای کنترل‌شده، شاهد اجرای واقعی و محدودیت وارسی. طول توضیح نمرهٔ درستی نیست.",
     "سه ضرب‌در ۲۵ به‌علاوهٔ ۱۰ برابر ۸۵ است. ابزار هم می‌تواند با آرگومان نادرست مسئلهٔ دیگری را درست حل کند. هم‌رأیی نامزدها در صورت خطای مشترک ضمانت نمی‌دهد؛ Trace بیرونی را توضیح قطعی محاسبهٔ درونی مدل ننامید."),
    ("یک درخواست از ابتدا تا توقف", "81-system-eval", [
        "مفهومی: کدام تصمیم را برنامه و کدام پیشنهاد را Backend می‌سازد؟ چرا تعریف Agent باید به قرارداد همین چرخه محدود بماند؟",
        "محاسباتی: در Trace سه‌گامی، بودجهٔ ابزار، تعداد تولید و هزینهٔ ورودی‌های تکراری را بشمارید.",
        "پیاده‌سازی: یک سؤال مستند، یک محاسبه و یک درخواست بی‌شاهد را تا توقف اجرا کنید. همان کنترل‌گر را یک بار با Fixture و یک بار با Mini-GPT واقعی امتحان کنید؛ رد قالب مدل نیز نتیجهٔ معتبر آزمایش است.",
        "پژوهشی: یک خطا را فقط در بازیابی، سپس فقط در پیشنهاد ابزار ایجاد کنید. چرا گزارش جداگانهٔ اجزا از یک نمرهٔ کلی مفیدتر است؟"
    ], "تحویل: Trace وضعیت‌ها، سقف گام، علت توقف و گزارش Regression با نام Backend. قبولی Fixture به معنی دستورپذیری مدل نیست.",
     "اجرای محدود باید در نبود جواب یا تکرار درخواست هم خاتمه یابد. Loss/PPL دربارهٔ مدل‌اند؛ درستی وظیفه، بازیابی، پشتیبانی منبع و قرارداد ابزار سنجه‌های دیگرند. هیچ امتیاز کوچک، قابلیت عمومی را ثابت نمی‌کند."),
    ("تحویل یک سامانهٔ کوچک، با حدود روشن", "84-capstone", [
        "مفهومی: یک نمودار از مدل، زمینه، بازیابی، حافظه، ابزار و وارسی بکشید. هر دادهٔ پایدار را به مالک و روش حذف وصل کنید.",
        "محاسباتی: وزن و KV Cache فرضی را جدا حساب کنید. برای B=1,L=2,H=2,T=32,D=8 و چهار بایت، Cache خام چقدر می‌شود؟ چرا پروژهٔ فعلی آن را مصرف نمی‌کند؟",
        "پیاده‌سازی: یک سند ساختگی تازه و دو مورد آزمونِ ندیده اضافه کنید. گزارش نهایی شامل محیط، نسخهٔ داده، Backend، خروجی خام، ارجاع، نتیجهٔ ابزار، امتناع و زمانِ اندازه‌گیری‌شده باشد.",
        "پژوهشی: یک تغییر برای بهبود انتخاب کنید و تمام آزمون‌های قبلی را دوباره اجرا کنید. بهبود سرعت را تنها با حفظ قرارداد درستی بپذیرید؛ اجرای محلی را آماده‌بودن سرویس عمومی معرفی نکنید."
    ], "تحویل: کد قابل اجرا، مجموعهٔ آزمون، گزارش قبل/بعد، حداقل یک شکست توضیح‌داده‌شده و راهنمای بازسازی. این تکلیفِ ترکیبی افزوده بر دفتر درس است.",
     "Cache خام فرضی ۸۱۹۲ بایت است؛ مدل ساده هر بار گذشته را دوباره محاسبه می‌کند و Cache کامل ندارد. تحویل خوب مرز شبیه‌سازی، اندازه‌گیری واقعی و قابلیت آموخته‌شده را حفظ می‌کند. سامانه می‌تواند اتصال درست اجزا داشته باشد و با این حال مدل ضعیفی برای پاسخ آزاد داشته باشد."),
]
